// GENERATED by tools/codegen/intvec.py - do not edit by hand.
//! Port of glam-rs `i32/ivec2.rs` @ 0.33.8: a 2-dimensional `i32` vector.
//!
//! The six integer vector modules are generated from one template (`tools/codegen/intvec.py`),
//! as in glam-rs. Every formulation is the cheapest of the candidates measured in
//! `gas/ivec2.snap`; the losing candidates live in `benches::alt::ivec2`.
//!
//! Overflow: the operators and the plain methods panic (corelib messages, or `'IVec2: overflow'`
//! for the fused kernels); the `checked_*` / `wrapping_*` / `saturating_*` families never do. They
//! exist for their semantics: they are slower than the panicking operators.

use core::integer::i32_diff;
use core::num::traits::{
    CheckedAdd, CheckedSub, SaturatingAdd, SaturatingSub, WideMul, WrappingAdd, WrappingSub,
};
use core::ops::index::IndexView;
use core::ops::{AddAssign, DivAssign, MulAssign, RemAssign, SubAssign};
use core::traits::{BitAnd, BitNot, BitOr, BitXor, DivRem};
use crate::bvec2::{BVec2, BVec2Trait};
use crate::ivec3::IVec3;
use crate::uvec2::UVec2;

/// A 2-dimensional `i32` vector.
///
/// Mirrors `glam::IVec2`.
/// #### Deviations
/// * `Debug` is the derived Cairo formatting; `Display` is not implemented.
/// * Not ported: `map` (a closure parameter cannot be force-inlined, E2143), `from_slice` /
///   `write_to_slice` (no `Span` in fixed-size math), `Sum` / `Product`, `IndexMut`, the
///   by-reference operator overloads, the scalar-on-the-left operators (`2 * v`), the scalar
///   bit operators (use `v & IVec2Trait::splat(s)`), shifts by a vector or by another integer
///   width, and the casts to types that do not exist in glam.cairo (`as_i64vec2`, ...).
/// * `as_vec2` (and `as_dvec2`) will land with the `Vec2` port.
#[derive(Copy, Drop, Serde, PartialEq, Debug, Default, Hash)]
pub struct IVec2 {
    pub x: i32,
    pub y: i32,
}

/// Creates a 2-dimensional `i32` vector.
///
/// Mirrors `glam::ivec2`.
/// #### Panics
/// * Never.
/// #### Deviations
/// * None.
#[inline(always)]
pub fn ivec2(x: i32, y: i32) -> IVec2 {
    IVec2 { x, y }
}

pub trait IVec2Trait {
    /// All zeroes.
    ///
    /// Mirrors `glam::IVec2::ZERO`.
    const ZERO: IVec2;
    /// All ones.
    ///
    /// Mirrors `glam::IVec2::ONE`.
    const ONE: IVec2;
    /// All negative ones.
    ///
    /// Mirrors `glam::IVec2::NEG_ONE`.
    const NEG_ONE: IVec2;
    /// All `i32::MIN`.
    ///
    /// Mirrors `glam::IVec2::MIN`.
    const MIN: IVec2;
    /// All `i32::MAX`.
    ///
    /// Mirrors `glam::IVec2::MAX`.
    const MAX: IVec2;
    /// A unit vector pointing along the positive X axis.
    ///
    /// Mirrors `glam::IVec2::X`.
    const X: IVec2;
    /// A unit vector pointing along the positive Y axis.
    ///
    /// Mirrors `glam::IVec2::Y`.
    const Y: IVec2;
    /// A unit vector pointing along the negative X axis.
    ///
    /// Mirrors `glam::IVec2::NEG_X`.
    const NEG_X: IVec2;
    /// A unit vector pointing along the negative Y axis.
    ///
    /// Mirrors `glam::IVec2::NEG_Y`.
    const NEG_Y: IVec2;
    /// The unit axes.
    ///
    /// Mirrors `glam::IVec2::AXES`.
    const AXES: [IVec2; 2];
    /// Creates a new vector.
    ///
    /// Mirrors `glam::IVec2::new`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn new(x: i32, y: i32) -> IVec2;
    /// Creates a vector with all elements set to `v`.
    ///
    /// Mirrors `glam::IVec2::splat`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn splat(v: i32) -> IVec2;
    /// Creates a vector from the elements in `if_true` and `if_false`, selecting which to
    /// use for each element of `self`.
    ///
    /// A true element in the mask uses the corresponding element from `if_true`, and false
    /// uses the element from `if_false`.
    ///
    /// Mirrors `glam::IVec2::select`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn select(mask: BVec2, if_true: IVec2, if_false: IVec2) -> IVec2;
    /// Creates a new vector from an array.
    ///
    /// Mirrors `glam::IVec2::from_array`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn from_array(a: [i32; 2]) -> IVec2;
    /// Converts `self` to `[x, y]`.
    ///
    /// Mirrors `glam::IVec2::to_array`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn to_array(self: IVec2) -> [i32; 2];
    /// Creates a 3D vector from `self` and the given `z` value.
    ///
    /// Mirrors `glam::IVec2::extend`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn extend(self: IVec2, z: i32) -> IVec3;
    /// Creates a 2D vector from `self` with the given value of `x`.
    ///
    /// Mirrors `glam::IVec2::with_x`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn with_x(self: IVec2, x: i32) -> IVec2;
    /// Creates a 2D vector from `self` with the given value of `y`.
    ///
    /// Mirrors `glam::IVec2::with_y`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn with_y(self: IVec2, y: i32) -> IVec2;
    /// Computes the dot product of `self` and `rhs`.
    ///
    /// Mirrors `glam::IVec2::dot`.
    /// #### Panics
    /// * `'IVec2: overflow'` if the result does not fit `i32`.
    /// #### Deviations
    /// * Only the final result is range-checked: an intermediate product or sum that
    ///   leaves the `i32` range does not panic when the result fits (glam-rs panics in
    ///   debug builds and wraps in release builds).
    fn dot(self: IVec2, rhs: IVec2) -> i32;
    /// Returns a vector where every component is the dot product of `self` and `rhs`.
    ///
    /// Mirrors `glam::IVec2::dot_into_vec`.
    /// #### Panics
    /// * `'IVec2: overflow'` if the result does not fit `i32`.
    /// #### Deviations
    /// * Only the final result is range-checked: an intermediate product or sum that
    ///   leaves the `i32` range does not panic when the result fits (glam-rs panics in
    ///   debug builds and wraps in release builds).
    fn dot_into_vec(self: IVec2, rhs: IVec2) -> IVec2;
    /// Returns a vector containing the minimum values for each element of `self` and
    /// `rhs`.
    ///
    /// In other words this computes `[min(x, rhs.x), min(self.y, rhs.y), ..]`.
    ///
    /// Mirrors `glam::IVec2::min`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn min(self: IVec2, rhs: IVec2) -> IVec2;
    /// Returns a vector containing the maximum values for each element of `self` and
    /// `rhs`.
    ///
    /// In other words this computes `[max(self.x, rhs.x), max(self.y, rhs.y), ..]`.
    ///
    /// Mirrors `glam::IVec2::max`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn max(self: IVec2, rhs: IVec2) -> IVec2;
    /// Component-wise clamping of values, similar to `i32::clamp`.
    ///
    /// Each element in `min` must be less-or-equal to the corresponding element in `max`.
    /// The direct if-chain is 10 to 30 % cheaper than composing `max` and `min`.
    ///
    /// Mirrors `glam::IVec2::clamp`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * The `glam_assert!(min <= max)` precondition is not checked. When it is violated
    ///   the result differs from the `self.max(min).min(max)` of glam-rs: an element below
    ///   `min` clamps to `min` (glam-rs: to `max`).
    fn clamp(self: IVec2, min: IVec2, max: IVec2) -> IVec2;
    /// Returns the horizontal minimum of `self`.
    ///
    /// In other words this computes `min(x, y, ..)`.
    ///
    /// Mirrors `glam::IVec2::min_element`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn min_element(self: IVec2) -> i32;
    /// Returns the horizontal maximum of `self`.
    ///
    /// In other words this computes `max(x, y, ..)`.
    ///
    /// Mirrors `glam::IVec2::max_element`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn max_element(self: IVec2) -> i32;
    /// Returns the index of the first minimum element of `self`.
    ///
    /// Mirrors `glam::IVec2::min_position`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn min_position(self: IVec2) -> usize;
    /// Returns the index of the first maximum element of `self`.
    ///
    /// Mirrors `glam::IVec2::max_position`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn max_position(self: IVec2) -> usize;
    /// Returns the sum of all elements of `self`.
    ///
    /// In other words, this computes `self.x + self.y + ..`.
    ///
    /// Mirrors `glam::IVec2::element_sum`.
    /// #### Panics
    /// * `'i32_add Overflow'` / `'i32_add Underflow'` if a partial sum overflows.
    /// #### Deviations
    /// * None.
    fn element_sum(self: IVec2) -> i32;
    /// Returns the product of all elements of `self`.
    ///
    /// In other words, this computes `self.x * self.y * ..`.
    ///
    /// Mirrors `glam::IVec2::element_product`.
    /// #### Panics
    /// * `'i32_mul Overflow'` if a partial product overflows.
    /// #### Deviations
    /// * None.
    fn element_product(self: IVec2) -> i32;
    /// Returns a vector mask containing the result of a `==` comparison for each element
    /// of `self` and `rhs`.
    ///
    /// In other words, this computes `[self.x == rhs.x, self.y == rhs.y, ..]` for all
    /// elements.
    ///
    /// Mirrors `glam::IVec2::cmpeq`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn cmpeq(self: IVec2, rhs: IVec2) -> BVec2;
    /// Returns a vector mask containing the result of a `!=` comparison for each element
    /// of `self` and `rhs`.
    ///
    /// In other words, this computes `[self.x != rhs.x, self.y != rhs.y, ..]` for all
    /// elements.
    ///
    /// Mirrors `glam::IVec2::cmpne`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn cmpne(self: IVec2, rhs: IVec2) -> BVec2;
    /// Returns a vector mask containing the result of a `>=` comparison for each element
    /// of `self` and `rhs`.
    ///
    /// In other words, this computes `[self.x >= rhs.x, self.y >= rhs.y, ..]` for all
    /// elements.
    ///
    /// Mirrors `glam::IVec2::cmpge`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn cmpge(self: IVec2, rhs: IVec2) -> BVec2;
    /// Returns a vector mask containing the result of a `>` comparison for each element of
    /// `self` and `rhs`.
    ///
    /// In other words, this computes `[self.x > rhs.x, self.y > rhs.y, ..]` for all
    /// elements.
    ///
    /// Mirrors `glam::IVec2::cmpgt`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn cmpgt(self: IVec2, rhs: IVec2) -> BVec2;
    /// Returns a vector mask containing the result of a `<=` comparison for each element
    /// of `self` and `rhs`.
    ///
    /// In other words, this computes `[self.x <= rhs.x, self.y <= rhs.y, ..]` for all
    /// elements.
    ///
    /// Mirrors `glam::IVec2::cmple`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn cmple(self: IVec2, rhs: IVec2) -> BVec2;
    /// Returns a vector mask containing the result of a `<` comparison for each element of
    /// `self` and `rhs`.
    ///
    /// In other words, this computes `[self.x < rhs.x, self.y < rhs.y, ..]` for all
    /// elements.
    ///
    /// Mirrors `glam::IVec2::cmplt`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn cmplt(self: IVec2, rhs: IVec2) -> BVec2;
    /// Returns a vector containing the absolute value of each element of `self`.
    ///
    /// Mirrors `glam::IVec2::abs`.
    /// #### Panics
    /// * `'i32_neg Underflow'` if an element is `i32::MIN`.
    /// #### Deviations
    /// * None.
    fn abs(self: IVec2) -> IVec2;
    /// Returns a vector with elements representing the sign of `self`.
    ///
    /// - `0` if the number is zero
    /// - `1` if the number is positive
    /// - `-1` if the number is negative
    ///
    /// Mirrors `glam::IVec2::signum`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn signum(self: IVec2) -> IVec2;
    /// Returns a bitmask with the lowest 2 bits set to the sign bits from the elements of
    /// `self`.
    ///
    /// A negative element results in a `1` bit and a positive element in a `0` bit.
    /// Element `x` goes into the first lowest bit, element `y` into the second, etc.
    ///
    /// Mirrors `glam::IVec2::is_negative_bitmask`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn is_negative_bitmask(self: IVec2) -> u32;
    /// Returns a mask indicating which components are negative.
    ///
    /// Mirrors `glam::IVec2::is_negative_mask`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn is_negative_mask(self: IVec2) -> BVec2;
    /// Computes the squared length of `self`.
    ///
    /// Mirrors `glam::IVec2::length_squared`.
    /// #### Panics
    /// * `'IVec2: overflow'` if the result does not fit `i32`.
    /// #### Deviations
    /// * Only the final result is range-checked: an intermediate product or sum that
    ///   leaves the `i32` range does not panic when the result fits (glam-rs panics in
    ///   debug builds and wraps in release builds).
    fn length_squared(self: IVec2) -> i32;
    /// Compute the squared euclidean distance between two points in space.
    ///
    /// Mirrors `glam::IVec2::distance_squared`.
    /// #### Panics
    /// * `'IVec2: overflow'` if the result does not fit `i32`.
    /// #### Deviations
    /// * Only the final result is range-checked: an intermediate product or sum that
    ///   leaves the `i32` range does not panic when the result fits (glam-rs panics in
    ///   debug builds and wraps in release builds).
    fn distance_squared(self: IVec2, rhs: IVec2) -> i32;
    /// Returns the element-wise quotient of Euclidean division of `self` by `rhs`.
    ///
    /// Mirrors `glam::IVec2::div_euclid`.
    /// #### Panics
    /// * `'Division by 0'` if any `rhs` element is 0.
    /// * `'attempt to divide with overflow'` for `i32::MIN / -1`.
    /// #### Deviations
    /// * None.
    fn div_euclid(self: IVec2, rhs: IVec2) -> IVec2;
    /// Returns the element-wise remainder of Euclidean division of `self` by `rhs`.
    ///
    /// Mirrors `glam::IVec2::rem_euclid`.
    /// #### Panics
    /// * `'Division by 0'` if any `rhs` element is 0.
    /// #### Deviations
    /// * `i32::MIN.rem_euclid(-1)` is 0, the mathematical result: `i32::rem_euclid` of
    ///   Rust panics with an overflow there (it computes `MIN % -1`).
    fn rem_euclid(self: IVec2, rhs: IVec2) -> IVec2;
    /// Computes the manhattan distance between two points.
    ///
    /// See also `checked_manhattan_distance`.
    ///
    /// Mirrors `glam::IVec2::manhattan_distance`.
    /// #### Panics
    /// * `'u32_add Overflow'` if the sum overflows `u32`.
    /// #### Deviations
    /// * None.
    fn manhattan_distance(self: IVec2, rhs: IVec2) -> u32;
    /// Computes the manhattan distance between two points.
    ///
    /// This will return `None` if the result is greater than `u32::MAX`.
    ///
    /// Mirrors `glam::IVec2::checked_manhattan_distance`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn checked_manhattan_distance(self: IVec2, rhs: IVec2) -> Option<u32>;
    /// Computes the chebyshev distance between two points.
    ///
    /// Mirrors `glam::IVec2::chebyshev_distance`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn chebyshev_distance(self: IVec2, rhs: IVec2) -> u32;
    /// Returns a vector that is equal to `self` rotated by 90 degrees.
    ///
    /// Mirrors `glam::IVec2::perp`.
    /// #### Panics
    /// * `'i32_neg Underflow'` if `self.y` is `i32::MIN`.
    /// #### Deviations
    /// * None.
    fn perp(self: IVec2) -> IVec2;
    /// The perpendicular dot product of `self` and `rhs`. Also known as the wedge product,
    /// 2D cross product, and determinant.
    ///
    /// Mirrors `glam::IVec2::perp_dot`.
    /// #### Panics
    /// * `'IVec2: overflow'` if the result does not fit `i32`.
    /// #### Deviations
    /// * Only the final result is range-checked: an intermediate product or sum that
    ///   leaves the `i32` range does not panic when the result fits (glam-rs panics in
    ///   debug builds and wraps in release builds).
    fn perp_dot(self: IVec2, rhs: IVec2) -> i32;
    /// Returns `rhs` rotated by the angle of `self`. If `self` is normalized, then this
    /// just rotation. This is what you usually want. Otherwise, it will be like a rotation
    /// with a multiplication by `self`'s length.
    ///
    /// This can be used to rotate by 90 degree increments, e.g. `[sin(90), cos(90)]` =
    /// `[1, 0]` or `[sin(180), cos(180)]` = `[0, -1]`.
    ///
    /// Mirrors `glam::IVec2::rotate`.
    /// #### Panics
    /// * `'IVec2: overflow'` if the result does not fit `i32`.
    /// #### Deviations
    /// * Only the final result is range-checked: an intermediate product or sum that
    ///   leaves the `i32` range does not panic when the result fits (glam-rs panics in
    ///   debug builds and wraps in release builds).
    fn rotate(self: IVec2, rhs: IVec2) -> IVec2;
    /// Casts all elements of `self` to `u32`.
    ///
    /// Like the Rust `as` cast this is the two's complement reinterpretation: `-1` becomes
    /// `u32::MAX`. The checked conversion is `TryInto<IVec2, UVec2>`.
    ///
    /// Mirrors `glam::IVec2::as_uvec2`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn as_uvec2(self: IVec2) -> UVec2;
    /// Returns a vector containing the checked addition of `self` and `rhs`.
    ///
    /// In other words this computes `Some([self.x + rhs.x, self.y + rhs.y, ..])` but
    /// returns `None` on any overflow.
    ///
    /// Mirrors `glam::IVec2::checked_add`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn checked_add(self: IVec2, rhs: IVec2) -> Option<IVec2>;
    /// Returns a vector containing the checked subtraction of `self` and `rhs`.
    ///
    /// In other words this computes `Some([self.x - rhs.x, self.y - rhs.y, ..])` but
    /// returns `None` on any overflow.
    ///
    /// Mirrors `glam::IVec2::checked_sub`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn checked_sub(self: IVec2, rhs: IVec2) -> Option<IVec2>;
    /// Returns a vector containing the checked multiplication of `self` and `rhs`.
    ///
    /// In other words this computes `Some([self.x * rhs.x, self.y * rhs.y, ..])` but
    /// returns `None` on any overflow.
    ///
    /// Mirrors `glam::IVec2::checked_mul`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn checked_mul(self: IVec2, rhs: IVec2) -> Option<IVec2>;
    /// Returns a vector containing the checked division of `self` and `rhs`.
    ///
    /// In other words this computes `Some([self.x / rhs.x, self.y / rhs.y, ..])` but
    /// returns `None` on any division by zero or overflow.
    ///
    /// Mirrors `glam::IVec2::checked_div`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn checked_div(self: IVec2, rhs: IVec2) -> Option<IVec2>;
    /// Returns a vector containing the wrapping addition of `self` and `rhs`.
    ///
    /// In other words this computes `[self.x.wrapping_add(rhs.x),
    /// self.y.wrapping_add(rhs.y), ..]`.
    ///
    /// Mirrors `glam::IVec2::wrapping_add`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn wrapping_add(self: IVec2, rhs: IVec2) -> IVec2;
    /// Returns a vector containing the wrapping subtraction of `self` and `rhs`.
    ///
    /// In other words this computes `[self.x.wrapping_sub(rhs.x),
    /// self.y.wrapping_sub(rhs.y), ..]`.
    ///
    /// Mirrors `glam::IVec2::wrapping_sub`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn wrapping_sub(self: IVec2, rhs: IVec2) -> IVec2;
    /// Returns a vector containing the wrapping multiplication of `self` and `rhs`.
    ///
    /// In other words this computes `[self.x.wrapping_mul(rhs.x),
    /// self.y.wrapping_mul(rhs.y), ..]`.
    ///
    /// Mirrors `glam::IVec2::wrapping_mul`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn wrapping_mul(self: IVec2, rhs: IVec2) -> IVec2;
    /// Returns a vector containing the wrapping division of `self` and `rhs`.
    ///
    /// In other words this computes `[self.x.wrapping_div(rhs.x),
    /// self.y.wrapping_div(rhs.y), ..]`.
    ///
    /// Mirrors `glam::IVec2::wrapping_div`.
    /// #### Panics
    /// * `'Division by 0'` if any `rhs` element is 0.
    /// #### Deviations
    /// * None.
    fn wrapping_div(self: IVec2, rhs: IVec2) -> IVec2;
    /// Returns a vector containing the saturating addition of `self` and `rhs`.
    ///
    /// In other words this computes `[self.x.saturating_add(rhs.x),
    /// self.y.saturating_add(rhs.y), ..]`.
    ///
    /// Mirrors `glam::IVec2::saturating_add`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn saturating_add(self: IVec2, rhs: IVec2) -> IVec2;
    /// Returns a vector containing the saturating subtraction of `self` and `rhs`.
    ///
    /// In other words this computes `[self.x.saturating_sub(rhs.x),
    /// self.y.saturating_sub(rhs.y), ..]`.
    ///
    /// Mirrors `glam::IVec2::saturating_sub`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn saturating_sub(self: IVec2, rhs: IVec2) -> IVec2;
    /// Returns a vector containing the saturating multiplication of `self` and `rhs`.
    ///
    /// In other words this computes `[self.x.saturating_mul(rhs.x),
    /// self.y.saturating_mul(rhs.y), ..]`.
    ///
    /// Mirrors `glam::IVec2::saturating_mul`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn saturating_mul(self: IVec2, rhs: IVec2) -> IVec2;
    /// Returns a vector containing the saturating division of `self` and `rhs`.
    ///
    /// In other words this computes `[self.x.saturating_div(rhs.x),
    /// self.y.saturating_div(rhs.y), ..]`.
    ///
    /// Mirrors `glam::IVec2::saturating_div`.
    /// #### Panics
    /// * `'Division by 0'` if any `rhs` element is 0.
    /// #### Deviations
    /// * None.
    fn saturating_div(self: IVec2, rhs: IVec2) -> IVec2;
    /// Returns a vector containing the checked addition of `self` and unsigned vector
    /// `rhs`.
    ///
    /// In other words this computes `Some([self.x + rhs.x, self.y + rhs.y, ..])` but
    /// returns `None` on any overflow.
    ///
    /// Mirrors `glam::IVec2::checked_add_unsigned`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn checked_add_unsigned(self: IVec2, rhs: UVec2) -> Option<IVec2>;
    /// Returns a vector containing the wrapping addition of `self` and unsigned vector
    /// `rhs`.
    ///
    /// In other words this computes `[self.x.wrapping_add_unsigned(rhs.x),
    /// self.y.wrapping_add_unsigned(rhs.y), ..]`.
    ///
    /// Mirrors `glam::IVec2::wrapping_add_unsigned`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn wrapping_add_unsigned(self: IVec2, rhs: UVec2) -> IVec2;
    /// Returns a vector containing the saturating addition of `self` and unsigned vector
    /// `rhs`.
    ///
    /// In other words this computes `[self.x.saturating_add_unsigned(rhs.x),
    /// self.y.saturating_add_unsigned(rhs.y), ..]`.
    ///
    /// Mirrors `glam::IVec2::saturating_add_unsigned`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn saturating_add_unsigned(self: IVec2, rhs: UVec2) -> IVec2;
    /// Returns a vector containing the checked subtraction of `self` and unsigned vector
    /// `rhs`.
    ///
    /// In other words this computes `Some([self.x - rhs.x, self.y - rhs.y, ..])` but
    /// returns `None` on any overflow.
    ///
    /// Mirrors `glam::IVec2::checked_sub_unsigned`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn checked_sub_unsigned(self: IVec2, rhs: UVec2) -> Option<IVec2>;
    /// Returns a vector containing the wrapping subtraction of `self` and unsigned vector
    /// `rhs`.
    ///
    /// In other words this computes `[self.x.wrapping_sub_unsigned(rhs.x),
    /// self.y.wrapping_sub_unsigned(rhs.y), ..]`.
    ///
    /// Mirrors `glam::IVec2::wrapping_sub_unsigned`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn wrapping_sub_unsigned(self: IVec2, rhs: UVec2) -> IVec2;
    /// Returns a vector containing the saturating subtraction of `self` and unsigned
    /// vector `rhs`.
    ///
    /// In other words this computes `[self.x.saturating_sub_unsigned(rhs.x),
    /// self.y.saturating_sub_unsigned(rhs.y), ..]`.
    ///
    /// Mirrors `glam::IVec2::saturating_sub_unsigned`.
    /// #### Panics
    /// * Never.
    /// #### Deviations
    /// * None.
    fn saturating_sub_unsigned(self: IVec2, rhs: UVec2) -> IVec2;
    /// Returns `[self.x + rhs, self.y + rhs, ..]`.
    ///
    /// Mirrors `impl Add<i32> for glam::IVec2`.
    /// #### Panics
    /// * `'i32_add Overflow'` / `'i32_add Underflow'` on overflow.
    /// #### Deviations
    /// * glam-rs spells this with an operator (`impl Add<i32> for IVec2`); the core
    ///   operator traits of Cairo are homogeneous (docs/DESIGN.md section 3).
    fn add_scalar(self: IVec2, rhs: i32) -> IVec2;
    /// Returns `[self.x - rhs, self.y - rhs, ..]`.
    ///
    /// Mirrors `impl Sub<i32> for glam::IVec2`.
    /// #### Panics
    /// * `'i32_sub Overflow'` / `'i32_sub Underflow'` on overflow.
    /// #### Deviations
    /// * glam-rs spells this with an operator (`impl Sub<i32> for IVec2`); the core
    ///   operator traits of Cairo are homogeneous (docs/DESIGN.md section 3).
    fn sub_scalar(self: IVec2, rhs: i32) -> IVec2;
    /// Returns `[self.x * rhs, self.y * rhs, ..]`.
    ///
    /// Mirrors `impl Mul<i32> for glam::IVec2`.
    /// #### Panics
    /// * `'i32_mul Overflow'` on overflow.
    /// #### Deviations
    /// * glam-rs spells this with an operator (`impl Mul<i32> for IVec2`); the core
    ///   operator traits of Cairo are homogeneous (docs/DESIGN.md section 3).
    fn mul_scalar(self: IVec2, rhs: i32) -> IVec2;
    /// Returns `[self.x / rhs, self.y / rhs, ..]` (truncated division).
    ///
    /// Mirrors `impl Div<i32> for glam::IVec2`.
    /// #### Panics
    /// * `'Division by 0'` if `rhs` is 0.
    /// * `'attempt to divide with overflow'` for `i32::MIN / -1`.
    /// #### Deviations
    /// * glam-rs spells this with an operator (`impl Div<i32> for IVec2`); the core
    ///   operator traits of Cairo are homogeneous (docs/DESIGN.md section 3).
    fn div_scalar(self: IVec2, rhs: i32) -> IVec2;
    /// Returns `[self.x % rhs, self.y % rhs, ..]` (the remainder has the sign of `self`).
    ///
    /// Mirrors `impl Rem<i32> for glam::IVec2`.
    /// #### Panics
    /// * `'Division by 0'` if `rhs` is 0.
    /// * `'attempt to divide with overflow'` for `i32::MIN / -1`.
    /// #### Deviations
    /// * glam-rs spells this with an operator (`impl Rem<i32> for IVec2`); the core
    ///   operator traits of Cairo are homogeneous (docs/DESIGN.md section 3).
    /// * `i32::MIN % -1` panics (the quotient overflows); Rust returns 0 in release
    ///   builds.
    fn rem_scalar(self: IVec2, rhs: i32) -> IVec2;
    /// Shifts every element left by `rhs` bits: the bits shifted out are lost, as with the
    /// Rust `<<` (the result is the two's complement reinterpretation).
    ///
    /// Mirrors `impl Shl<u32> for glam::IVec2`.
    /// #### Panics
    /// * `'IVec2: shift overflow'` if `rhs >= 32` (Rust: overflow panic in debug builds,
    ///   masked shift amount in release builds).
    /// #### Deviations
    /// * glam-rs spells this with an operator; Cairo has no shift operator nor trait. Only
    ///   the `u32` shift amount is ported (not the 8 integer widths nor the per-component
    ///   vector amounts).
    fn shl(self: IVec2, rhs: u32) -> IVec2;
    /// Shifts every element right by `rhs` bits: an arithmetic shift, i.e. a division by
    /// `2^rhs` rounded toward negative infinity, as with the Rust `>>`.
    ///
    /// Mirrors `impl Shr<u32> for glam::IVec2`.
    /// #### Panics
    /// * `'IVec2: shift overflow'` if `rhs >= 32` (Rust: overflow panic in debug builds,
    ///   masked shift amount in release builds).
    /// #### Deviations
    /// * glam-rs spells this with an operator; Cairo has no shift operator nor trait. Only
    ///   the `u32` shift amount is ported (not the 8 integer widths nor the per-component
    ///   vector amounts).
    fn shr(self: IVec2, rhs: u32) -> IVec2;
}

pub impl IVec2Impl of IVec2Trait {
    const ZERO: IVec2 = IVec2 { x: 0, y: 0 };
    const ONE: IVec2 = IVec2 { x: 1, y: 1 };
    const NEG_ONE: IVec2 = IVec2 { x: -1, y: -1 };
    const MIN: IVec2 = IVec2 { x: -0x80000000, y: -0x80000000 };
    const MAX: IVec2 = IVec2 { x: 0x7fffffff, y: 0x7fffffff };
    const X: IVec2 = IVec2 { x: 1, y: 0 };
    const Y: IVec2 = IVec2 { x: 0, y: 1 };
    const NEG_X: IVec2 = IVec2 { x: -1, y: 0 };
    const NEG_Y: IVec2 = IVec2 { x: 0, y: -1 };
    const AXES: [IVec2; 2] = [IVec2 { x: 1, y: 0 }, IVec2 { x: 0, y: 1 }];

    #[inline(always)]
    fn new(x: i32, y: i32) -> IVec2 {
        IVec2 { x, y }
    }

    #[inline(always)]
    fn splat(v: i32) -> IVec2 {
        IVec2 { x: v, y: v }
    }

    #[inline(always)]
    fn select(mask: BVec2, if_true: IVec2, if_false: IVec2) -> IVec2 {
        IVec2 {
            x: if mask.x {
                if_true.x
            } else {
                if_false.x
            },
            y: if mask.y {
                if_true.y
            } else {
                if_false.y
            },
        }
    }

    #[inline(always)]
    fn from_array(a: [i32; 2]) -> IVec2 {
        let [x, y] = a;
        IVec2 { x, y }
    }

    #[inline(always)]
    fn to_array(self: IVec2) -> [i32; 2] {
        [self.x, self.y]
    }

    #[inline(always)]
    fn extend(self: IVec2, z: i32) -> IVec3 {
        IVec3 { x: self.x, y: self.y, z }
    }

    #[inline(always)]
    fn with_x(self: IVec2, x: i32) -> IVec2 {
        IVec2 { x: x, y: self.y }
    }

    #[inline(always)]
    fn with_y(self: IVec2, y: i32) -> IVec2 {
        IVec2 { x: self.x, y: y }
    }

    #[inline(always)]
    fn dot(self: IVec2, rhs: IVec2) -> i32 {
        let d: felt252 = self.x.wide_mul(rhs.x).into() + self.y.wide_mul(rhs.y).into();
        d.try_into().expect('IVec2: overflow')
    }

    #[inline(always)]
    fn dot_into_vec(self: IVec2, rhs: IVec2) -> IVec2 {
        let d = Self::dot(self, rhs);
        IVec2 { x: d, y: d }
    }

    #[inline(always)]
    fn min(self: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 {
            x: if self.x < rhs.x {
                self.x
            } else {
                rhs.x
            },
            y: if self.y < rhs.y {
                self.y
            } else {
                rhs.y
            },
        }
    }

    #[inline(always)]
    fn max(self: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 {
            x: if self.x > rhs.x {
                self.x
            } else {
                rhs.x
            },
            y: if self.y > rhs.y {
                self.y
            } else {
                rhs.y
            },
        }
    }

    #[inline(always)]
    fn clamp(self: IVec2, min: IVec2, max: IVec2) -> IVec2 {
        IVec2 {
            x: if self.x < min.x {
                min.x
            } else if self.x > max.x {
                max.x
            } else {
                self.x
            },
            y: if self.y < min.y {
                min.y
            } else if self.y > max.y {
                max.y
            } else {
                self.y
            },
        }
    }

    #[inline(always)]
    fn min_element(self: IVec2) -> i32 {
        let m = if self.x < self.y {
            self.x
        } else {
            self.y
        };
        m
    }

    #[inline(always)]
    fn max_element(self: IVec2) -> i32 {
        let m = if self.x > self.y {
            self.x
        } else {
            self.y
        };
        m
    }

    #[inline(always)]
    fn min_position(self: IVec2) -> usize {
        let m = self.x;
        let mut index = 0;
        if self.y < m {
            index = 1;
        }
        index
    }

    #[inline(always)]
    fn max_position(self: IVec2) -> usize {
        let m = self.x;
        let mut index = 0;
        if self.y > m {
            index = 1;
        }
        index
    }

    #[inline(always)]
    fn element_sum(self: IVec2) -> i32 {
        self.x + self.y
    }

    #[inline(always)]
    fn element_product(self: IVec2) -> i32 {
        self.x * self.y
    }

    #[inline(always)]
    fn cmpeq(self: IVec2, rhs: IVec2) -> BVec2 {
        BVec2 { x: self.x == rhs.x, y: self.y == rhs.y }
    }

    #[inline(always)]
    fn cmpne(self: IVec2, rhs: IVec2) -> BVec2 {
        BVec2 { x: self.x != rhs.x, y: self.y != rhs.y }
    }

    #[inline(always)]
    fn cmpge(self: IVec2, rhs: IVec2) -> BVec2 {
        BVec2 { x: self.x >= rhs.x, y: self.y >= rhs.y }
    }

    #[inline(always)]
    fn cmpgt(self: IVec2, rhs: IVec2) -> BVec2 {
        BVec2 { x: self.x > rhs.x, y: self.y > rhs.y }
    }

    #[inline(always)]
    fn cmple(self: IVec2, rhs: IVec2) -> BVec2 {
        BVec2 { x: self.x <= rhs.x, y: self.y <= rhs.y }
    }

    #[inline(always)]
    fn cmplt(self: IVec2, rhs: IVec2) -> BVec2 {
        BVec2 { x: self.x < rhs.x, y: self.y < rhs.y }
    }

    #[inline(always)]
    fn abs(self: IVec2) -> IVec2 {
        IVec2 { x: abs_i32(self.x), y: abs_i32(self.y) }
    }

    #[inline(always)]
    fn signum(self: IVec2) -> IVec2 {
        IVec2 { x: signum_i32(self.x), y: signum_i32(self.y) }
    }

    #[inline(always)]
    fn is_negative_bitmask(self: IVec2) -> u32 {
        Self::is_negative_mask(self).bitmask()
    }

    #[inline(always)]
    fn is_negative_mask(self: IVec2) -> BVec2 {
        BVec2 { x: self.x < 0, y: self.y < 0 }
    }

    #[inline(always)]
    fn length_squared(self: IVec2) -> i32 {
        let d: felt252 = self.x.wide_mul(self.x).into() + self.y.wide_mul(self.y).into();
        d.try_into().expect('IVec2: overflow')
    }

    #[inline(always)]
    fn distance_squared(self: IVec2, rhs: IVec2) -> i32 {
        let dx: felt252 = self.x.into() - rhs.x.into();
        let dy: felt252 = self.y.into() - rhs.y.into();
        let d: felt252 = dx * dx + dy * dy;
        d.try_into().expect('IVec2: overflow')
    }

    #[inline(always)]
    fn div_euclid(self: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: div_euclid_i32(self.x, rhs.x), y: div_euclid_i32(self.y, rhs.y) }
    }

    #[inline(always)]
    fn rem_euclid(self: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: rem_euclid_i32(self.x, rhs.x), y: rem_euclid_i32(self.y, rhs.y) }
    }

    #[inline(always)]
    fn manhattan_distance(self: IVec2, rhs: IVec2) -> u32 {
        abs_diff_i32(self.x, rhs.x) + abs_diff_i32(self.y, rhs.y)
    }

    #[inline(always)]
    fn checked_manhattan_distance(self: IVec2, rhs: IVec2) -> Option<u32> {
        let d: felt252 = abs_diff_i32(self.x, rhs.x).into() + abs_diff_i32(self.y, rhs.y).into();
        d.try_into()
    }

    #[inline(always)]
    fn chebyshev_distance(self: IVec2, rhs: IVec2) -> u32 {
        let m = abs_diff_i32(self.x, rhs.x);
        let d = abs_diff_i32(self.y, rhs.y);
        let m = if d > m {
            d
        } else {
            m
        };
        m
    }

    #[inline(always)]
    fn perp(self: IVec2) -> IVec2 {
        IVec2 { x: -self.y, y: self.x }
    }

    #[inline(always)]
    fn perp_dot(self: IVec2, rhs: IVec2) -> i32 {
        let v: felt252 = self.x.wide_mul(rhs.y).into() - self.y.wide_mul(rhs.x).into();
        v.try_into().expect('IVec2: overflow')
    }

    #[inline(always)]
    fn rotate(self: IVec2, rhs: IVec2) -> IVec2 {
        let x = {
            let v: felt252 = self.x.wide_mul(rhs.x).into() - self.y.wide_mul(rhs.y).into();
            v.try_into().expect('IVec2: overflow')
        };
        let y = {
            let v: felt252 = self.y.wide_mul(rhs.x).into() + self.x.wide_mul(rhs.y).into();
            v.try_into().expect('IVec2: overflow')
        };
        IVec2 { x, y }
    }

    #[inline(always)]
    fn as_uvec2(self: IVec2) -> UVec2 {
        UVec2 { x: i32_as_u32(self.x), y: i32_as_u32(self.y) }
    }

    #[inline(always)]
    fn checked_add(self: IVec2, rhs: IVec2) -> Option<IVec2> {
        Some(IVec2 { x: self.x.checked_add(rhs.x)?, y: self.y.checked_add(rhs.y)? })
    }

    #[inline(always)]
    fn checked_sub(self: IVec2, rhs: IVec2) -> Option<IVec2> {
        Some(IVec2 { x: self.x.checked_sub(rhs.x)?, y: self.y.checked_sub(rhs.y)? })
    }

    #[inline(always)]
    fn checked_mul(self: IVec2, rhs: IVec2) -> Option<IVec2> {
        Some(IVec2 { x: checked_mul_i32(self.x, rhs.x)?, y: checked_mul_i32(self.y, rhs.y)? })
    }

    #[inline(always)]
    fn checked_div(self: IVec2, rhs: IVec2) -> Option<IVec2> {
        Some(IVec2 { x: checked_div_i32(self.x, rhs.x)?, y: checked_div_i32(self.y, rhs.y)? })
    }

    #[inline(always)]
    fn wrapping_add(self: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: self.x.wrapping_add(rhs.x), y: self.y.wrapping_add(rhs.y) }
    }

    #[inline(always)]
    fn wrapping_sub(self: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: self.x.wrapping_sub(rhs.x), y: self.y.wrapping_sub(rhs.y) }
    }

    #[inline(always)]
    fn wrapping_mul(self: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: wrapping_mul_i32(self.x, rhs.x), y: wrapping_mul_i32(self.y, rhs.y) }
    }

    #[inline(always)]
    fn wrapping_div(self: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: wrapping_div_i32(self.x, rhs.x), y: wrapping_div_i32(self.y, rhs.y) }
    }

    #[inline(always)]
    fn saturating_add(self: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: self.x.saturating_add(rhs.x), y: self.y.saturating_add(rhs.y) }
    }

    #[inline(always)]
    fn saturating_sub(self: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: self.x.saturating_sub(rhs.x), y: self.y.saturating_sub(rhs.y) }
    }

    #[inline(always)]
    fn saturating_mul(self: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: saturating_mul_i32(self.x, rhs.x), y: saturating_mul_i32(self.y, rhs.y) }
    }

    #[inline(always)]
    fn saturating_div(self: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: saturating_div_i32(self.x, rhs.x), y: saturating_div_i32(self.y, rhs.y) }
    }

    #[inline(always)]
    fn checked_add_unsigned(self: IVec2, rhs: UVec2) -> Option<IVec2> {
        Some(
            IVec2 {
                x: checked_add_unsigned(self.x, rhs.x)?, y: checked_add_unsigned(self.y, rhs.y)?,
            },
        )
    }

    #[inline(always)]
    fn wrapping_add_unsigned(self: IVec2, rhs: UVec2) -> IVec2 {
        IVec2 { x: wrapping_add_unsigned(self.x, rhs.x), y: wrapping_add_unsigned(self.y, rhs.y) }
    }

    #[inline(always)]
    fn saturating_add_unsigned(self: IVec2, rhs: UVec2) -> IVec2 {
        IVec2 {
            x: saturating_add_unsigned(self.x, rhs.x), y: saturating_add_unsigned(self.y, rhs.y),
        }
    }

    #[inline(always)]
    fn checked_sub_unsigned(self: IVec2, rhs: UVec2) -> Option<IVec2> {
        Some(
            IVec2 {
                x: checked_sub_unsigned(self.x, rhs.x)?, y: checked_sub_unsigned(self.y, rhs.y)?,
            },
        )
    }

    #[inline(always)]
    fn wrapping_sub_unsigned(self: IVec2, rhs: UVec2) -> IVec2 {
        IVec2 { x: wrapping_sub_unsigned(self.x, rhs.x), y: wrapping_sub_unsigned(self.y, rhs.y) }
    }

    #[inline(always)]
    fn saturating_sub_unsigned(self: IVec2, rhs: UVec2) -> IVec2 {
        IVec2 {
            x: saturating_sub_unsigned(self.x, rhs.x), y: saturating_sub_unsigned(self.y, rhs.y),
        }
    }

    #[inline(always)]
    fn add_scalar(self: IVec2, rhs: i32) -> IVec2 {
        IVec2 { x: self.x + rhs, y: self.y + rhs }
    }

    #[inline(always)]
    fn sub_scalar(self: IVec2, rhs: i32) -> IVec2 {
        IVec2 { x: self.x - rhs, y: self.y - rhs }
    }

    #[inline(always)]
    fn mul_scalar(self: IVec2, rhs: i32) -> IVec2 {
        IVec2 { x: self.x * rhs, y: self.y * rhs }
    }

    #[inline(always)]
    fn div_scalar(self: IVec2, rhs: i32) -> IVec2 {
        let d: NonZero<i32> = rhs.try_into().expect('Division by 0');
        let (x, _) = DivRem::div_rem(self.x, d);
        let (y, _) = DivRem::div_rem(self.y, d);
        IVec2 { x, y }
    }

    #[inline(always)]
    fn rem_scalar(self: IVec2, rhs: i32) -> IVec2 {
        let d: NonZero<i32> = rhs.try_into().expect('Division by 0');
        let (_, x) = DivRem::div_rem(self.x, d);
        let (_, y) = DivRem::div_rem(self.y, d);
        IVec2 { x, y }
    }

    #[inline(always)]
    fn shl(self: IVec2, rhs: u32) -> IVec2 {
        let p = pow2(rhs);
        IVec2 { x: shl_i32(self.x, p), y: shl_i32(self.y, p) }
    }

    #[inline(always)]
    fn shr(self: IVec2, rhs: u32) -> IVec2 {
        let p: NonZero<u32> = pow2(rhs).try_into().unwrap();
        let (bias, _) = DivRem::div_rem(0x80000000_u32, p);
        let bias: felt252 = bias.into();
        IVec2 { x: shr_i32(self.x, p, bias), y: shr_i32(self.y, p, bias) }
    }
}

/// Component-wise `+`. Panics with the corelib `i32_add` overflow messages.
///
/// Mirrors `impl Add for glam::IVec2`.
pub impl IVec2Add of Add<IVec2> {
    #[inline(always)]
    fn add(lhs: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: lhs.x + rhs.x, y: lhs.y + rhs.y }
    }
}

/// Component-wise `-`. Panics with the corelib `i32_sub` overflow messages.
///
/// Mirrors `impl Sub for glam::IVec2`.
pub impl IVec2Sub of Sub<IVec2> {
    #[inline(always)]
    fn sub(lhs: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: lhs.x - rhs.x, y: lhs.y - rhs.y }
    }
}

/// Component-wise `*`. Panics with `'i32_mul Overflow'`.
///
/// Mirrors `impl Mul for glam::IVec2`.
pub impl IVec2Mul of Mul<IVec2> {
    #[inline(always)]
    fn mul(lhs: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: lhs.x * rhs.x, y: lhs.y * rhs.y }
    }
}

/// Component-wise `/`. Truncated division. Panics with `'Division by 0'` and, for `i32::MIN / -1`,
/// `'attempt to divide with overflow'`.
///
/// Mirrors `impl Div for glam::IVec2`.
pub impl IVec2Div of Div<IVec2> {
    #[inline(always)]
    fn div(lhs: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: lhs.x / rhs.x, y: lhs.y / rhs.y }
    }
}

/// Component-wise `%`. The remainder has the sign of `lhs`. Panics with `'Division by 0'` and, for
/// `i32::MIN % -1`, `'attempt to divide with overflow'`.
///
/// Mirrors `impl Rem for glam::IVec2`.
pub impl IVec2Rem of Rem<IVec2> {
    #[inline(always)]
    fn rem(lhs: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: lhs.x % rhs.x, y: lhs.y % rhs.y }
    }
}

/// Component-wise negation. Panics with `'i32_neg Underflow'` on `i32::MIN`.
///
/// Mirrors `impl Neg for glam::IVec2`.
pub impl IVec2Neg of Neg<IVec2> {
    #[inline(always)]
    fn neg(a: IVec2) -> IVec2 {
        IVec2 { x: -a.x, y: -a.y }
    }
}

/// Mirrors `impl AddAssign for glam::IVec2`.
pub impl IVec2AddAssign of AddAssign<IVec2, IVec2> {
    #[inline(always)]
    fn add_assign(ref self: IVec2, rhs: IVec2) {
        self = self + rhs;
    }
}

/// Mirrors `impl SubAssign for glam::IVec2`.
pub impl IVec2SubAssign of SubAssign<IVec2, IVec2> {
    #[inline(always)]
    fn sub_assign(ref self: IVec2, rhs: IVec2) {
        self = self - rhs;
    }
}

/// Mirrors `impl MulAssign for glam::IVec2`.
pub impl IVec2MulAssign of MulAssign<IVec2, IVec2> {
    #[inline(always)]
    fn mul_assign(ref self: IVec2, rhs: IVec2) {
        self = self * rhs;
    }
}

/// Mirrors `impl DivAssign for glam::IVec2`.
pub impl IVec2DivAssign of DivAssign<IVec2, IVec2> {
    #[inline(always)]
    fn div_assign(ref self: IVec2, rhs: IVec2) {
        self = self / rhs;
    }
}

/// Mirrors `impl RemAssign for glam::IVec2`.
pub impl IVec2RemAssign of RemAssign<IVec2, IVec2> {
    #[inline(always)]
    fn rem_assign(ref self: IVec2, rhs: IVec2) {
        self = self % rhs;
    }
}

/// Mirrors `impl AddAssign<i32> for glam::IVec2` (see `IVec2Trait::add_scalar`).
pub impl IVec2AddAssignScalar of AddAssign<IVec2, i32> {
    #[inline(always)]
    fn add_assign(ref self: IVec2, rhs: i32) {
        self = IVec2Trait::add_scalar(self, rhs);
    }
}

/// Mirrors `impl SubAssign<i32> for glam::IVec2` (see `IVec2Trait::sub_scalar`).
pub impl IVec2SubAssignScalar of SubAssign<IVec2, i32> {
    #[inline(always)]
    fn sub_assign(ref self: IVec2, rhs: i32) {
        self = IVec2Trait::sub_scalar(self, rhs);
    }
}

/// Mirrors `impl MulAssign<i32> for glam::IVec2` (see `IVec2Trait::mul_scalar`).
pub impl IVec2MulAssignScalar of MulAssign<IVec2, i32> {
    #[inline(always)]
    fn mul_assign(ref self: IVec2, rhs: i32) {
        self = IVec2Trait::mul_scalar(self, rhs);
    }
}

/// Mirrors `impl DivAssign<i32> for glam::IVec2` (see `IVec2Trait::div_scalar`).
pub impl IVec2DivAssignScalar of DivAssign<IVec2, i32> {
    #[inline(always)]
    fn div_assign(ref self: IVec2, rhs: i32) {
        self = IVec2Trait::div_scalar(self, rhs);
    }
}

/// Mirrors `impl RemAssign<i32> for glam::IVec2` (see `IVec2Trait::rem_scalar`).
pub impl IVec2RemAssignScalar of RemAssign<IVec2, i32> {
    #[inline(always)]
    fn rem_assign(ref self: IVec2, rhs: i32) {
        self = IVec2Trait::rem_scalar(self, rhs);
    }
}

/// Component-wise bitwise `&` of the two's complement representations. One bitwise builtin cell per
/// component.
///
/// Mirrors `impl BitAnd for glam::IVec2`.
pub impl IVec2BitAnd of BitAnd<IVec2> {
    #[inline(always)]
    fn bitand(lhs: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: bitand_i32(lhs.x, rhs.x), y: bitand_i32(lhs.y, rhs.y) }
    }
}

/// Component-wise bitwise `|` of the two's complement representations. One bitwise builtin cell per
/// component.
///
/// Mirrors `impl BitOr for glam::IVec2`.
pub impl IVec2BitOr of BitOr<IVec2> {
    #[inline(always)]
    fn bitor(lhs: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: bitor_i32(lhs.x, rhs.x), y: bitor_i32(lhs.y, rhs.y) }
    }
}

/// Component-wise bitwise `^` of the two's complement representations. One bitwise builtin cell per
/// component.
///
/// Mirrors `impl BitXor for glam::IVec2`.
pub impl IVec2BitXor of BitXor<IVec2> {
    #[inline(always)]
    fn bitxor(lhs: IVec2, rhs: IVec2) -> IVec2 {
        IVec2 { x: bitxor_i32(lhs.x, rhs.x), y: bitxor_i32(lhs.y, rhs.y) }
    }
}

/// Component-wise bitwise NOT (`~v`): `-x - 1`, which never overflows. No bitwise builtin.
///
/// Mirrors `impl Not for glam::IVec2` (the Rust `!v`; Cairo spells the bitwise
/// complement of an integer `~`).
pub impl IVec2BitNot of BitNot<IVec2> {
    #[inline(always)]
    fn bitnot(a: IVec2) -> IVec2 {
        IVec2 { x: -1 - a.x, y: -1 - a.y }
    }
}

/// `v[i]`. Panics with `'IVec2: index out of bounds'` if `index >= 2`.
///
/// Mirrors `impl Index<usize> for glam::IVec2`.
pub impl IVec2IndexView of IndexView<IVec2, usize> {
    type Target = i32;
    #[inline(always)]
    fn index(self: @IVec2, index: usize) -> i32 {
        match index {
            0 => *self.x,
            1 => *self.y,
            _ => core::panic_with_felt252('IVec2: index out of bounds'),
        }
    }
}

/// Mirrors `impl From<[i32; 2]> for glam::IVec2`.
pub impl I32ArrayIntoIVec2 of Into<[i32; 2], IVec2> {
    #[inline(always)]
    fn into(self: [i32; 2]) -> IVec2 {
        IVec2Trait::from_array(self)
    }
}

/// Mirrors `impl From<glam::IVec2> for [i32; 2]`.
pub impl IVec2IntoI32Array of Into<IVec2, [i32; 2]> {
    #[inline(always)]
    fn into(self: IVec2) -> [i32; 2] {
        [self.x, self.y]
    }
}

/// Mirrors `impl From<(i32, i32)> for glam::IVec2`.
pub impl I32TupleIntoIVec2 of Into<(i32, i32), IVec2> {
    #[inline(always)]
    fn into(self: (i32, i32)) -> IVec2 {
        let (x, y) = self;
        IVec2 { x, y }
    }
}

/// Mirrors `impl From<glam::IVec2> for (i32, i32)`.
pub impl IVec2IntoI32Tuple of Into<IVec2, (i32, i32)> {
    #[inline(always)]
    fn into(self: IVec2) -> (i32, i32) {
        (self.x, self.y)
    }
}

/// `true` maps to 1 and `false` to 0.
///
/// Mirrors `impl From<BVec2> for glam::IVec2`.
pub impl BVec2IntoIVec2 of Into<BVec2, IVec2> {
    #[inline(always)]
    fn into(self: BVec2) -> IVec2 {
        IVec2 { x: if self.x {
            1
        } else {
            0
        }, y: if self.y {
            1
        } else {
            0
        } }
    }
}

/// Checked conversion: `None` if an element does not fit `i32`.
///
/// Mirrors `impl TryFrom<UVec2> for glam::IVec2`.
pub impl UVec2TryIntoIVec2 of TryInto<UVec2, IVec2> {
    #[inline(always)]
    fn try_into(self: UVec2) -> Option<IVec2> {
        Some(IVec2 { x: self.x.try_into()?, y: self.y.try_into()? })
    }
}

/// `2^n`. Shifts are a multiplication / division by a power of two taken from a jump table
/// (a runtime `pow(2, n)` costs ~10 000 gas more, a `const` array lookup ~200 more).
#[inline(always)]
fn pow2(n: u32) -> u32 {
    match n {
        0 => 0x1,
        1 => 0x2,
        2 => 0x4,
        3 => 0x8,
        4 => 0x10,
        5 => 0x20,
        6 => 0x40,
        7 => 0x80,
        8 => 0x100,
        9 => 0x200,
        10 => 0x400,
        11 => 0x800,
        12 => 0x1000,
        13 => 0x2000,
        14 => 0x4000,
        15 => 0x8000,
        16 => 0x10000,
        17 => 0x20000,
        18 => 0x40000,
        19 => 0x80000,
        20 => 0x100000,
        21 => 0x200000,
        22 => 0x400000,
        23 => 0x800000,
        24 => 0x1000000,
        25 => 0x2000000,
        26 => 0x4000000,
        27 => 0x8000000,
        28 => 0x10000000,
        29 => 0x20000000,
        30 => 0x40000000,
        31 => 0x80000000,
        _ => core::panic_with_felt252('IVec2: shift overflow'),
    }
}

/// `(a << n) mod 2^32` with `p = 2^n`: widening multiplication, then the low 32 bits.
#[inline(always)]
fn shl_u32(a: u32, p: u32) -> u32 {
    let (_, lo) = DivRem::div_rem(a.wide_mul(p), 0x100000000);
    lo.try_into().unwrap()
}

/// Two's complement reinterpretation. `i32_diff(a, 0)` is `Ok(a)` when `a >= 0` and
/// `Err(2^32 + a)` otherwise: exactly the wrapped value in both cases, in one libfunc.
#[allow(manual_unwrap_or)]
#[inline(always)]
fn i32_as_u32(a: i32) -> u32 {
    match i32_diff(a, 0) {
        Ok(v) | Err(v) => v,
    }
}

/// Two's complement reinterpretation: `a - 2^32` when `a >= 2^31`.
#[allow(manual_unwrap_or)]
#[inline(always)]
fn u32_as_i32(a: u32) -> i32 {
    match a.try_into() {
        Some(v) => v,
        None => {
            let f: felt252 = a.into();
            (f - 0x100000000).try_into().unwrap()
        },
    }
}

/// `|a - b|` as a `u32` (never overflows). `i32_diff` yields `a - b` directly when `a >= b`.
#[allow(manual_unwrap_or)]
#[inline(always)]
fn abs_diff_i32(a: i32, b: i32) -> u32 {
    match i32_diff(a, b) {
        Ok(d) => d,
        Err(_) => match i32_diff(b, a) {
            Ok(d) | Err(d) => d,
        },
    }
}

/// The `i32 -> u32` downcast is the cheapest sign test (one range check, against a full
/// `i32_diff` for `a < 0`).
#[allow(manual_unwrap_or)]
#[inline(always)]
fn abs_i32(a: i32) -> i32 {
    match TryInto::<i32, u32>::try_into(a) {
        Some(_) => a,
        None => -a,
    }
}

/// Zero first (an equality costs no range check), then the downcast sign test of `abs_i32`.
#[allow(manual_unwrap_or)]
#[inline(always)]
fn signum_i32(a: i32) -> i32 {
    if a == 0 {
        0
    } else {
        match TryInto::<i32, u32>::try_into(a) {
            Some(_) => 1,
            None => -1,
        }
    }
}

/// Euclidean quotient: `a = q * b + r` with `0 <= r < |b|`. Own sign / magnitude split
/// (`i32_diff`), one unsigned `DivRem` on `|a| = Q * |b| + R`, signed rebuild: cheaper than the
/// corelib signed `DivRem` followed by the fix-up of Rust's `i32::div_euclid`.
#[allow(manual_unwrap_or)]
#[inline(always)]
fn div_euclid_i32(a: i32, b: i32) -> i32 {
    let (mb, nb) = match i32_diff(b, 0) {
        Ok(m) => (m, false),
        Err(_) => (abs_diff_i32(0, b), true),
    };
    let d: NonZero<u32> = mb.try_into().expect('Division by 0');
    match i32_diff(a, 0) {
        Ok(ma) => {
            let (q, _) = DivRem::div_rem(ma, d);
            let q: i32 = q.try_into().unwrap();
            if nb {
                -q
            } else {
                q
            }
        },
        Err(_) => {
            let (q, r) = DivRem::div_rem(abs_diff_i32(0, a), d);
            let q: felt252 = if r == 0 {
                q.into()
            } else {
                q.into() + 1
            };
            if nb {
                q.try_into().expect('attempt to divide with overflow')
            } else {
                (-q).try_into().unwrap()
            }
        },
    }
}

/// Euclidean remainder, `0 <= r < |b|`: `R` when `a >= 0`, else `|b| - R` (or 0), with
/// `|a| = Q * |b| + R` from one unsigned `DivRem`.
#[allow(manual_unwrap_or)]
#[inline(always)]
fn rem_euclid_i32(a: i32, b: i32) -> i32 {
    let mb = abs_diff_i32(b, 0);
    let d: NonZero<u32> = mb.try_into().expect('Division by 0');
    match i32_diff(a, 0) {
        Ok(ma) => {
            let (_, r) = DivRem::div_rem(ma, d);
            r.try_into().unwrap()
        },
        Err(_) => {
            let (_, r) = DivRem::div_rem(abs_diff_i32(0, a), d);
            if r == 0 {
                0
            } else {
                (mb - r).try_into().unwrap()
            }
        },
    }
}

#[inline(always)]
fn checked_mul_i32(a: i32, b: i32) -> Option<i32> {
    a.wide_mul(b).try_into()
}

/// `None` for a zero divisor and for `i32::MIN / -1`.
#[allow(manual_unwrap_or)]
#[inline(always)]
fn checked_div_i32(a: i32, b: i32) -> Option<i32> {
    match TryInto::<i32, NonZero<i32>>::try_into(b) {
        Some(d) => {
            if b == -1 && a == -0x80000000 {
                None
            } else {
                let (q, _) = DivRem::div_rem(a, d);
                Some(q)
            }
        },
        None => None,
    }
}

/// In range: one downcast. Otherwise the low 32 bits of the 64-bit product, reinterpreted as an
/// `i32`: adding `2^63` (a multiple of `2^32`) makes the product non-negative without changing
/// its low half.
#[allow(manual_unwrap_or)]
#[inline(always)]
fn wrapping_mul_i32(a: i32, b: i32) -> i32 {
    let p = a.wide_mul(b);
    match p.try_into() {
        Some(v) => v,
        None => {
            let p: felt252 = p.into();
            let p: u64 = (p + 0x8000000000000000).try_into().unwrap();
            let (_, lo) = DivRem::div_rem(p, 0x100000000);
            u32_as_i32(lo.try_into().unwrap())
        },
    }
}

#[allow(manual_unwrap_or)]
#[inline(always)]
fn saturating_mul_i32(a: i32, b: i32) -> i32 {
    let p = a.wide_mul(b);
    match p.try_into() {
        Some(v) => v,
        None => if p < 0 {
            -0x80000000
        } else {
            0x7fffffff
        },
    }
}

/// `i32::MIN / -1` wraps to `i32::MIN`.
#[inline(always)]
fn wrapping_div_i32(a: i32, b: i32) -> i32 {
    if b == -1 && a == -0x80000000 {
        a
    } else {
        a / b
    }
}

/// `i32::MIN / -1` saturates to `i32::MAX`.
#[inline(always)]
fn saturating_div_i32(a: i32, b: i32) -> i32 {
    if b == -1 && a == -0x80000000 {
        0x7fffffff
    } else {
        a / b
    }
}

#[inline(always)]
fn checked_add_unsigned(a: i32, b: u32) -> Option<i32> {
    let s: felt252 = a.into() + b.into();
    s.try_into()
}

#[inline(always)]
fn checked_sub_unsigned(a: i32, b: u32) -> Option<i32> {
    let s: felt252 = a.into() - b.into();
    s.try_into()
}

/// `a + b < 2^31 + 2^32`: at most one wrap, downward.
#[allow(manual_unwrap_or)]
#[inline(always)]
fn wrapping_add_unsigned(a: i32, b: u32) -> i32 {
    let s: felt252 = a.into() + b.into();
    match s.try_into() {
        Some(v) => v,
        None => (s - 0x100000000).try_into().unwrap(),
    }
}

/// `a - b >= -2^31 - 2^32 + 1`: at most one wrap, upward.
#[allow(manual_unwrap_or)]
#[inline(always)]
fn wrapping_sub_unsigned(a: i32, b: u32) -> i32 {
    let s: felt252 = a.into() - b.into();
    match s.try_into() {
        Some(v) => v,
        None => (s + 0x100000000).try_into().unwrap(),
    }
}

#[allow(manual_unwrap_or)]
#[inline(always)]
fn saturating_add_unsigned(a: i32, b: u32) -> i32 {
    let s: felt252 = a.into() + b.into();
    match s.try_into() {
        Some(v) => v,
        None => 0x7fffffff,
    }
}

#[allow(manual_unwrap_or)]
#[inline(always)]
fn saturating_sub_unsigned(a: i32, b: u32) -> i32 {
    let s: felt252 = a.into() - b.into();
    match s.try_into() {
        Some(v) => v,
        None => -0x80000000,
    }
}

/// Two's complement left shift: reinterpret, shift the `u32`, reinterpret back.
#[inline(always)]
fn shl_i32(a: i32, p: u32) -> i32 {
    u32_as_i32(shl_u32(i32_as_u32(a), p))
}

/// Arithmetic right shift, `floor(a / p)`: bias `a` by `2^31` to make it unsigned
/// (`i32_diff(a, MIN)` is always `Ok(a + 2^31)`), divide, remove the bias `2^31 / p`.
#[allow(manual_unwrap_or)]
#[inline(always)]
fn shr_i32(a: i32, p: NonZero<u32>, bias: felt252) -> i32 {
    let u = match i32_diff(a, -0x80000000) {
        Ok(v) | Err(v) => v,
    };
    let (q, _) = DivRem::div_rem(u, p);
    (q.into() - bias).try_into().unwrap()
}

/// Bitwise `&` of the two's complement representations (one bitwise builtin cell).
#[inline(always)]
fn bitand_i32(a: i32, b: i32) -> i32 {
    u32_as_i32(i32_as_u32(a) & i32_as_u32(b))
}

/// Bitwise `|` of the two's complement representations (one bitwise builtin cell).
#[inline(always)]
fn bitor_i32(a: i32, b: i32) -> i32 {
    u32_as_i32(i32_as_u32(a) | i32_as_u32(b))
}

/// Bitwise `^` of the two's complement representations (one bitwise builtin cell).
#[inline(always)]
fn bitxor_i32(a: i32, b: i32) -> i32 {
    u32_as_i32(i32_as_u32(a) ^ i32_as_u32(b))
}
