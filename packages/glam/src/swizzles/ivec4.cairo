// GENERATED by tools/codegen/swizzles.py - do not edit by hand.
//! Port of glam-rs `swizzles/vec_traits.rs` (`Vec4Swizzles`) and
//! `swizzles/ivec4_impl.rs` @ 0.33.8 for `IVec4`: 372 swizzle methods.
//!
//! The nine swizzle modules are generated from one template (`tools/codegen/swizzles.py`), as in
//! glam-rs. Every method is a struct construction from the components of `self`: no
//! arithmetic, no branch, no range check.

use crate::ivec2::IVec2;
use crate::ivec3::IVec3;
use crate::ivec4::IVec4;

/// The 372 swizzles of `IVec4` (scalar: `i32`): every permutation with
/// repetition of its components of length 2, 3 and 4 (`yx`, `xxyz`, ...) and the `with_*`
/// setters.
///
/// Mirrors `glam::Vec4Swizzles`, which glam-rs implements for the `f32`, `f64` and integer
/// vectors alike; Cairo has no associated types here, so there is one trait per vector type.
/// #### Panics
/// * Never: every method is a plain struct construction.
/// #### Deviations
/// * `Vec2` / `Vec3` / `Vec4` of the associated types are the concrete vector types of the same
///   scalar (`Vec*`, `IVec*` or `UVec*`).
/// * The `Panics` / `Deviations` sections of the methods are this one: never / none.
pub trait IVec4Swizzles {
    /// Returns `self`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyzw`.
    fn xyzw(self: IVec4) -> IVec4;
    /// Returns the `IVec2` `(self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xx`.
    fn xx(self: IVec4) -> IVec2;
    /// Returns the `IVec2` `(self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xy`.
    fn xy(self: IVec4) -> IVec2;
    /// Returns `self` with `x` to `rhs.x`, `y` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xy`.
    fn with_xy(self: IVec4, rhs: IVec2) -> IVec4;
    /// Returns the `IVec2` `(self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xz`.
    fn xz(self: IVec4) -> IVec2;
    /// Returns `self` with `x` to `rhs.x`, `z` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xz`.
    fn with_xz(self: IVec4, rhs: IVec2) -> IVec4;
    /// Returns the `IVec2` `(self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xw`.
    fn xw(self: IVec4) -> IVec2;
    /// Returns `self` with `x` to `rhs.x`, `w` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xw`.
    fn with_xw(self: IVec4, rhs: IVec2) -> IVec4;
    /// Returns the `IVec2` `(self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yx`.
    fn yx(self: IVec4) -> IVec2;
    /// Returns `self` with `y` to `rhs.x`, `x` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yx`.
    fn with_yx(self: IVec4, rhs: IVec2) -> IVec4;
    /// Returns the `IVec2` `(self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yy`.
    fn yy(self: IVec4) -> IVec2;
    /// Returns the `IVec2` `(self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yz`.
    fn yz(self: IVec4) -> IVec2;
    /// Returns `self` with `y` to `rhs.x`, `z` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yz`.
    fn with_yz(self: IVec4, rhs: IVec2) -> IVec4;
    /// Returns the `IVec2` `(self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yw`.
    fn yw(self: IVec4) -> IVec2;
    /// Returns `self` with `y` to `rhs.x`, `w` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yw`.
    fn with_yw(self: IVec4, rhs: IVec2) -> IVec4;
    /// Returns the `IVec2` `(self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zx`.
    fn zx(self: IVec4) -> IVec2;
    /// Returns `self` with `z` to `rhs.x`, `x` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zx`.
    fn with_zx(self: IVec4, rhs: IVec2) -> IVec4;
    /// Returns the `IVec2` `(self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zy`.
    fn zy(self: IVec4) -> IVec2;
    /// Returns `self` with `z` to `rhs.x`, `y` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zy`.
    fn with_zy(self: IVec4, rhs: IVec2) -> IVec4;
    /// Returns the `IVec2` `(self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zz`.
    fn zz(self: IVec4) -> IVec2;
    /// Returns the `IVec2` `(self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zw`.
    fn zw(self: IVec4) -> IVec2;
    /// Returns `self` with `z` to `rhs.x`, `w` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zw`.
    fn with_zw(self: IVec4, rhs: IVec2) -> IVec4;
    /// Returns the `IVec2` `(self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wx`.
    fn wx(self: IVec4) -> IVec2;
    /// Returns `self` with `w` to `rhs.x`, `x` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wx`.
    fn with_wx(self: IVec4, rhs: IVec2) -> IVec4;
    /// Returns the `IVec2` `(self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wy`.
    fn wy(self: IVec4) -> IVec2;
    /// Returns `self` with `w` to `rhs.x`, `y` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wy`.
    fn with_wy(self: IVec4, rhs: IVec2) -> IVec4;
    /// Returns the `IVec2` `(self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wz`.
    fn wz(self: IVec4) -> IVec2;
    /// Returns `self` with `w` to `rhs.x`, `z` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wz`.
    fn with_wz(self: IVec4, rhs: IVec2) -> IVec4;
    /// Returns the `IVec2` `(self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ww`.
    fn ww(self: IVec4) -> IVec2;
    /// Returns the `IVec3` `(self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxx`.
    fn xxx(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxy`.
    fn xxy(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.x, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxz`.
    fn xxz(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.x, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxw`.
    fn xxw(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyx`.
    fn xyx(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyy`.
    fn xyy(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.x, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyz`.
    fn xyz(self: IVec4) -> IVec3;
    /// Returns `self` with `x` to `rhs.x`, `y` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xyz`.
    fn with_xyz(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.x, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyw`.
    fn xyw(self: IVec4) -> IVec3;
    /// Returns `self` with `x` to `rhs.x`, `y` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xyw`.
    fn with_xyw(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.x, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzx`.
    fn xzx(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.x, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzy`.
    fn xzy(self: IVec4) -> IVec3;
    /// Returns `self` with `x` to `rhs.x`, `z` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xzy`.
    fn with_xzy(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.x, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzz`.
    fn xzz(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.x, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzw`.
    fn xzw(self: IVec4) -> IVec3;
    /// Returns `self` with `x` to `rhs.x`, `z` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xzw`.
    fn with_xzw(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.x, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwx`.
    fn xwx(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.x, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwy`.
    fn xwy(self: IVec4) -> IVec3;
    /// Returns `self` with `x` to `rhs.x`, `w` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xwy`.
    fn with_xwy(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.x, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwz`.
    fn xwz(self: IVec4) -> IVec3;
    /// Returns `self` with `x` to `rhs.x`, `w` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xwz`.
    fn with_xwz(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.x, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xww`.
    fn xww(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxx`.
    fn yxx(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxy`.
    fn yxy(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.y, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxz`.
    fn yxz(self: IVec4) -> IVec3;
    /// Returns `self` with `y` to `rhs.x`, `x` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yxz`.
    fn with_yxz(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.y, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxw`.
    fn yxw(self: IVec4) -> IVec3;
    /// Returns `self` with `y` to `rhs.x`, `x` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yxw`.
    fn with_yxw(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyx`.
    fn yyx(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyy`.
    fn yyy(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.y, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyz`.
    fn yyz(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.y, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyw`.
    fn yyw(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.y, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzx`.
    fn yzx(self: IVec4) -> IVec3;
    /// Returns `self` with `y` to `rhs.x`, `z` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yzx`.
    fn with_yzx(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.y, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzy`.
    fn yzy(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.y, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzz`.
    fn yzz(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.y, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzw`.
    fn yzw(self: IVec4) -> IVec3;
    /// Returns `self` with `y` to `rhs.x`, `z` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yzw`.
    fn with_yzw(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.y, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywx`.
    fn ywx(self: IVec4) -> IVec3;
    /// Returns `self` with `y` to `rhs.x`, `w` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_ywx`.
    fn with_ywx(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.y, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywy`.
    fn ywy(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.y, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywz`.
    fn ywz(self: IVec4) -> IVec3;
    /// Returns `self` with `y` to `rhs.x`, `w` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_ywz`.
    fn with_ywz(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.y, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yww`.
    fn yww(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.z, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxx`.
    fn zxx(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.z, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxy`.
    fn zxy(self: IVec4) -> IVec3;
    /// Returns `self` with `z` to `rhs.x`, `x` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zxy`.
    fn with_zxy(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.z, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxz`.
    fn zxz(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.z, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxw`.
    fn zxw(self: IVec4) -> IVec3;
    /// Returns `self` with `z` to `rhs.x`, `x` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zxw`.
    fn with_zxw(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.z, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyx`.
    fn zyx(self: IVec4) -> IVec3;
    /// Returns `self` with `z` to `rhs.x`, `y` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zyx`.
    fn with_zyx(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.z, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyy`.
    fn zyy(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.z, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyz`.
    fn zyz(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.z, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyw`.
    fn zyw(self: IVec4) -> IVec3;
    /// Returns `self` with `z` to `rhs.x`, `y` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zyw`.
    fn with_zyw(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.z, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzx`.
    fn zzx(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.z, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzy`.
    fn zzy(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.z, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzz`.
    fn zzz(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.z, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzw`.
    fn zzw(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.z, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwx`.
    fn zwx(self: IVec4) -> IVec3;
    /// Returns `self` with `z` to `rhs.x`, `w` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zwx`.
    fn with_zwx(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.z, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwy`.
    fn zwy(self: IVec4) -> IVec3;
    /// Returns `self` with `z` to `rhs.x`, `w` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zwy`.
    fn with_zwy(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.z, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwz`.
    fn zwz(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.z, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zww`.
    fn zww(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.w, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxx`.
    fn wxx(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.w, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxy`.
    fn wxy(self: IVec4) -> IVec3;
    /// Returns `self` with `w` to `rhs.x`, `x` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wxy`.
    fn with_wxy(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.w, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxz`.
    fn wxz(self: IVec4) -> IVec3;
    /// Returns `self` with `w` to `rhs.x`, `x` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wxz`.
    fn with_wxz(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.w, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxw`.
    fn wxw(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.w, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyx`.
    fn wyx(self: IVec4) -> IVec3;
    /// Returns `self` with `w` to `rhs.x`, `y` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wyx`.
    fn with_wyx(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.w, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyy`.
    fn wyy(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.w, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyz`.
    fn wyz(self: IVec4) -> IVec3;
    /// Returns `self` with `w` to `rhs.x`, `y` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wyz`.
    fn with_wyz(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.w, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyw`.
    fn wyw(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.w, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzx`.
    fn wzx(self: IVec4) -> IVec3;
    /// Returns `self` with `w` to `rhs.x`, `z` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wzx`.
    fn with_wzx(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.w, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzy`.
    fn wzy(self: IVec4) -> IVec3;
    /// Returns `self` with `w` to `rhs.x`, `z` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wzy`.
    fn with_wzy(self: IVec4, rhs: IVec3) -> IVec4;
    /// Returns the `IVec3` `(self.w, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzz`.
    fn wzz(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.w, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzw`.
    fn wzw(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.w, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwx`.
    fn wwx(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.w, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwy`.
    fn wwy(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.w, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwz`.
    fn wwz(self: IVec4) -> IVec3;
    /// Returns the `IVec3` `(self.w, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::www`.
    fn www(self: IVec4) -> IVec3;
    /// Returns the `IVec4` `(self.x, self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxxx`.
    fn xxxx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxxy`.
    fn xxxy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.x, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxxz`.
    fn xxxz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.x, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxxw`.
    fn xxxw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxyx`.
    fn xxyx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxyy`.
    fn xxyy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.x, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxyz`.
    fn xxyz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.x, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxyw`.
    fn xxyw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.x, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxzx`.
    fn xxzx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.x, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxzy`.
    fn xxzy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.x, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxzz`.
    fn xxzz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.x, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxzw`.
    fn xxzw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.x, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxwx`.
    fn xxwx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.x, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxwy`.
    fn xxwy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.x, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxwz`.
    fn xxwz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.x, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxww`.
    fn xxww(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyxx`.
    fn xyxx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyxy`.
    fn xyxy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.y, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyxz`.
    fn xyxz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.y, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyxw`.
    fn xyxw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyyx`.
    fn xyyx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyyy`.
    fn xyyy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.y, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyyz`.
    fn xyyz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.y, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyyw`.
    fn xyyw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.y, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyzx`.
    fn xyzx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.y, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyzy`.
    fn xyzy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.y, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyzz`.
    fn xyzz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.y, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xywx`.
    fn xywx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.y, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xywy`.
    fn xywy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.y, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xywz`.
    fn xywz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.y, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyww`.
    fn xyww(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.z, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzxx`.
    fn xzxx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.z, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzxy`.
    fn xzxy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.z, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzxz`.
    fn xzxz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.z, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzxw`.
    fn xzxw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.z, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzyx`.
    fn xzyx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.z, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzyy`.
    fn xzyy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.z, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzyz`.
    fn xzyz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.z, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzyw`.
    fn xzyw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.z, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzzx`.
    fn xzzx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.z, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzzy`.
    fn xzzy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.z, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzzz`.
    fn xzzz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.z, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzzw`.
    fn xzzw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.z, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzwx`.
    fn xzwx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.z, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzwy`.
    fn xzwy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.z, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzwz`.
    fn xzwz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.z, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzww`.
    fn xzww(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.w, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwxx`.
    fn xwxx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.w, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwxy`.
    fn xwxy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.w, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwxz`.
    fn xwxz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.w, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwxw`.
    fn xwxw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.w, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwyx`.
    fn xwyx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.w, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwyy`.
    fn xwyy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.w, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwyz`.
    fn xwyz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.w, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwyw`.
    fn xwyw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.w, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwzx`.
    fn xwzx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.w, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwzy`.
    fn xwzy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.w, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwzz`.
    fn xwzz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.w, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwzw`.
    fn xwzw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.w, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwwx`.
    fn xwwx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.w, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwwy`.
    fn xwwy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.w, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwwz`.
    fn xwwz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.x, self.w, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwww`.
    fn xwww(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxxx`.
    fn yxxx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxxy`.
    fn yxxy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.x, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxxz`.
    fn yxxz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.x, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxxw`.
    fn yxxw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxyx`.
    fn yxyx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxyy`.
    fn yxyy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.x, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxyz`.
    fn yxyz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.x, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxyw`.
    fn yxyw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.x, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxzx`.
    fn yxzx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.x, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxzy`.
    fn yxzy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.x, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxzz`.
    fn yxzz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.x, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxzw`.
    fn yxzw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.x, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxwx`.
    fn yxwx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.x, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxwy`.
    fn yxwy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.x, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxwz`.
    fn yxwz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.x, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxww`.
    fn yxww(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyxx`.
    fn yyxx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyxy`.
    fn yyxy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.y, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyxz`.
    fn yyxz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.y, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyxw`.
    fn yyxw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyyx`.
    fn yyyx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyyy`.
    fn yyyy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.y, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyyz`.
    fn yyyz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.y, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyyw`.
    fn yyyw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.y, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyzx`.
    fn yyzx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.y, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyzy`.
    fn yyzy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.y, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyzz`.
    fn yyzz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.y, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyzw`.
    fn yyzw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.y, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yywx`.
    fn yywx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.y, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yywy`.
    fn yywy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.y, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yywz`.
    fn yywz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.y, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyww`.
    fn yyww(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.z, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzxx`.
    fn yzxx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.z, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzxy`.
    fn yzxy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.z, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzxz`.
    fn yzxz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.z, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzxw`.
    fn yzxw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.z, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzyx`.
    fn yzyx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.z, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzyy`.
    fn yzyy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.z, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzyz`.
    fn yzyz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.z, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzyw`.
    fn yzyw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.z, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzzx`.
    fn yzzx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.z, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzzy`.
    fn yzzy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.z, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzzz`.
    fn yzzz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.z, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzzw`.
    fn yzzw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.z, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzwx`.
    fn yzwx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.z, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzwy`.
    fn yzwy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.z, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzwz`.
    fn yzwz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.z, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzww`.
    fn yzww(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.w, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywxx`.
    fn ywxx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.w, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywxy`.
    fn ywxy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.w, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywxz`.
    fn ywxz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.w, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywxw`.
    fn ywxw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.w, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywyx`.
    fn ywyx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.w, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywyy`.
    fn ywyy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.w, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywyz`.
    fn ywyz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.w, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywyw`.
    fn ywyw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.w, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywzx`.
    fn ywzx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.w, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywzy`.
    fn ywzy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.w, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywzz`.
    fn ywzz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.w, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywzw`.
    fn ywzw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.w, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywwx`.
    fn ywwx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.w, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywwy`.
    fn ywwy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.w, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywwz`.
    fn ywwz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.y, self.w, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywww`.
    fn ywww(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxxx`.
    fn zxxx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxxy`.
    fn zxxy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.x, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxxz`.
    fn zxxz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.x, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxxw`.
    fn zxxw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxyx`.
    fn zxyx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxyy`.
    fn zxyy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.x, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxyz`.
    fn zxyz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.x, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxyw`.
    fn zxyw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.x, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxzx`.
    fn zxzx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.x, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxzy`.
    fn zxzy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.x, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxzz`.
    fn zxzz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.x, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxzw`.
    fn zxzw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.x, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxwx`.
    fn zxwx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.x, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxwy`.
    fn zxwy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.x, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxwz`.
    fn zxwz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.x, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxww`.
    fn zxww(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyxx`.
    fn zyxx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyxy`.
    fn zyxy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.y, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyxz`.
    fn zyxz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.y, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyxw`.
    fn zyxw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyyx`.
    fn zyyx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyyy`.
    fn zyyy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.y, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyyz`.
    fn zyyz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.y, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyyw`.
    fn zyyw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.y, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyzx`.
    fn zyzx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.y, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyzy`.
    fn zyzy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.y, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyzz`.
    fn zyzz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.y, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyzw`.
    fn zyzw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.y, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zywx`.
    fn zywx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.y, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zywy`.
    fn zywy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.y, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zywz`.
    fn zywz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.y, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyww`.
    fn zyww(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.z, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzxx`.
    fn zzxx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.z, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzxy`.
    fn zzxy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.z, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzxz`.
    fn zzxz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.z, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzxw`.
    fn zzxw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.z, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzyx`.
    fn zzyx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.z, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzyy`.
    fn zzyy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.z, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzyz`.
    fn zzyz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.z, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzyw`.
    fn zzyw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.z, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzzx`.
    fn zzzx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.z, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzzy`.
    fn zzzy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.z, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzzz`.
    fn zzzz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.z, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzzw`.
    fn zzzw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.z, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzwx`.
    fn zzwx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.z, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzwy`.
    fn zzwy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.z, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzwz`.
    fn zzwz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.z, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzww`.
    fn zzww(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.w, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwxx`.
    fn zwxx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.w, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwxy`.
    fn zwxy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.w, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwxz`.
    fn zwxz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.w, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwxw`.
    fn zwxw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.w, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwyx`.
    fn zwyx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.w, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwyy`.
    fn zwyy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.w, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwyz`.
    fn zwyz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.w, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwyw`.
    fn zwyw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.w, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwzx`.
    fn zwzx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.w, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwzy`.
    fn zwzy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.w, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwzz`.
    fn zwzz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.w, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwzw`.
    fn zwzw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.w, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwwx`.
    fn zwwx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.w, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwwy`.
    fn zwwy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.w, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwwz`.
    fn zwwz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.z, self.w, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwww`.
    fn zwww(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxxx`.
    fn wxxx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxxy`.
    fn wxxy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.x, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxxz`.
    fn wxxz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.x, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxxw`.
    fn wxxw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxyx`.
    fn wxyx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxyy`.
    fn wxyy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.x, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxyz`.
    fn wxyz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.x, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxyw`.
    fn wxyw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.x, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxzx`.
    fn wxzx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.x, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxzy`.
    fn wxzy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.x, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxzz`.
    fn wxzz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.x, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxzw`.
    fn wxzw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.x, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxwx`.
    fn wxwx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.x, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxwy`.
    fn wxwy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.x, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxwz`.
    fn wxwz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.x, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxww`.
    fn wxww(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyxx`.
    fn wyxx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyxy`.
    fn wyxy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.y, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyxz`.
    fn wyxz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.y, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyxw`.
    fn wyxw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyyx`.
    fn wyyx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyyy`.
    fn wyyy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.y, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyyz`.
    fn wyyz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.y, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyyw`.
    fn wyyw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.y, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyzx`.
    fn wyzx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.y, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyzy`.
    fn wyzy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.y, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyzz`.
    fn wyzz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.y, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyzw`.
    fn wyzw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.y, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wywx`.
    fn wywx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.y, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wywy`.
    fn wywy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.y, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wywz`.
    fn wywz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.y, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyww`.
    fn wyww(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.z, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzxx`.
    fn wzxx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.z, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzxy`.
    fn wzxy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.z, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzxz`.
    fn wzxz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.z, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzxw`.
    fn wzxw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.z, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzyx`.
    fn wzyx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.z, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzyy`.
    fn wzyy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.z, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzyz`.
    fn wzyz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.z, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzyw`.
    fn wzyw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.z, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzzx`.
    fn wzzx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.z, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzzy`.
    fn wzzy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.z, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzzz`.
    fn wzzz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.z, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzzw`.
    fn wzzw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.z, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzwx`.
    fn wzwx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.z, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzwy`.
    fn wzwy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.z, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzwz`.
    fn wzwz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.z, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzww`.
    fn wzww(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.w, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwxx`.
    fn wwxx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.w, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwxy`.
    fn wwxy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.w, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwxz`.
    fn wwxz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.w, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwxw`.
    fn wwxw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.w, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwyx`.
    fn wwyx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.w, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwyy`.
    fn wwyy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.w, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwyz`.
    fn wwyz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.w, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwyw`.
    fn wwyw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.w, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwzx`.
    fn wwzx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.w, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwzy`.
    fn wwzy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.w, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwzz`.
    fn wwzz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.w, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwzw`.
    fn wwzw(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.w, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwwx`.
    fn wwwx(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.w, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwwy`.
    fn wwwy(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.w, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwwz`.
    fn wwwz(self: IVec4) -> IVec4;
    /// Returns the `IVec4` `(self.w, self.w, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwww`.
    fn wwww(self: IVec4) -> IVec4;
}

pub impl IVec4SwizzlesImpl of IVec4Swizzles {
    #[inline(always)]
    fn xyzw(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn xx(self: IVec4) -> IVec2 {
        IVec2 { x: self.x, y: self.x }
    }

    #[inline(always)]
    fn xy(self: IVec4) -> IVec2 {
        IVec2 { x: self.x, y: self.y }
    }

    #[inline(always)]
    fn with_xy(self: IVec4, rhs: IVec2) -> IVec4 {
        IVec4 { x: rhs.x, y: rhs.y, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn xz(self: IVec4) -> IVec2 {
        IVec2 { x: self.x, y: self.z }
    }

    #[inline(always)]
    fn with_xz(self: IVec4, rhs: IVec2) -> IVec4 {
        IVec4 { x: rhs.x, y: self.y, z: rhs.y, w: self.w }
    }

    #[inline(always)]
    fn xw(self: IVec4) -> IVec2 {
        IVec2 { x: self.x, y: self.w }
    }

    #[inline(always)]
    fn with_xw(self: IVec4, rhs: IVec2) -> IVec4 {
        IVec4 { x: rhs.x, y: self.y, z: self.z, w: rhs.y }
    }

    #[inline(always)]
    fn yx(self: IVec4) -> IVec2 {
        IVec2 { x: self.y, y: self.x }
    }

    #[inline(always)]
    fn with_yx(self: IVec4, rhs: IVec2) -> IVec4 {
        IVec4 { x: rhs.y, y: rhs.x, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn yy(self: IVec4) -> IVec2 {
        IVec2 { x: self.y, y: self.y }
    }

    #[inline(always)]
    fn yz(self: IVec4) -> IVec2 {
        IVec2 { x: self.y, y: self.z }
    }

    #[inline(always)]
    fn with_yz(self: IVec4, rhs: IVec2) -> IVec4 {
        IVec4 { x: self.x, y: rhs.x, z: rhs.y, w: self.w }
    }

    #[inline(always)]
    fn yw(self: IVec4) -> IVec2 {
        IVec2 { x: self.y, y: self.w }
    }

    #[inline(always)]
    fn with_yw(self: IVec4, rhs: IVec2) -> IVec4 {
        IVec4 { x: self.x, y: rhs.x, z: self.z, w: rhs.y }
    }

    #[inline(always)]
    fn zx(self: IVec4) -> IVec2 {
        IVec2 { x: self.z, y: self.x }
    }

    #[inline(always)]
    fn with_zx(self: IVec4, rhs: IVec2) -> IVec4 {
        IVec4 { x: rhs.y, y: self.y, z: rhs.x, w: self.w }
    }

    #[inline(always)]
    fn zy(self: IVec4) -> IVec2 {
        IVec2 { x: self.z, y: self.y }
    }

    #[inline(always)]
    fn with_zy(self: IVec4, rhs: IVec2) -> IVec4 {
        IVec4 { x: self.x, y: rhs.y, z: rhs.x, w: self.w }
    }

    #[inline(always)]
    fn zz(self: IVec4) -> IVec2 {
        IVec2 { x: self.z, y: self.z }
    }

    #[inline(always)]
    fn zw(self: IVec4) -> IVec2 {
        IVec2 { x: self.z, y: self.w }
    }

    #[inline(always)]
    fn with_zw(self: IVec4, rhs: IVec2) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: rhs.x, w: rhs.y }
    }

    #[inline(always)]
    fn wx(self: IVec4) -> IVec2 {
        IVec2 { x: self.w, y: self.x }
    }

    #[inline(always)]
    fn with_wx(self: IVec4, rhs: IVec2) -> IVec4 {
        IVec4 { x: rhs.y, y: self.y, z: self.z, w: rhs.x }
    }

    #[inline(always)]
    fn wy(self: IVec4) -> IVec2 {
        IVec2 { x: self.w, y: self.y }
    }

    #[inline(always)]
    fn with_wy(self: IVec4, rhs: IVec2) -> IVec4 {
        IVec4 { x: self.x, y: rhs.y, z: self.z, w: rhs.x }
    }

    #[inline(always)]
    fn wz(self: IVec4) -> IVec2 {
        IVec2 { x: self.w, y: self.z }
    }

    #[inline(always)]
    fn with_wz(self: IVec4, rhs: IVec2) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: rhs.y, w: rhs.x }
    }

    #[inline(always)]
    fn ww(self: IVec4) -> IVec2 {
        IVec2 { x: self.w, y: self.w }
    }

    #[inline(always)]
    fn xxx(self: IVec4) -> IVec3 {
        IVec3 { x: self.x, y: self.x, z: self.x }
    }

    #[inline(always)]
    fn xxy(self: IVec4) -> IVec3 {
        IVec3 { x: self.x, y: self.x, z: self.y }
    }

    #[inline(always)]
    fn xxz(self: IVec4) -> IVec3 {
        IVec3 { x: self.x, y: self.x, z: self.z }
    }

    #[inline(always)]
    fn xxw(self: IVec4) -> IVec3 {
        IVec3 { x: self.x, y: self.x, z: self.w }
    }

    #[inline(always)]
    fn xyx(self: IVec4) -> IVec3 {
        IVec3 { x: self.x, y: self.y, z: self.x }
    }

    #[inline(always)]
    fn xyy(self: IVec4) -> IVec3 {
        IVec3 { x: self.x, y: self.y, z: self.y }
    }

    #[inline(always)]
    fn xyz(self: IVec4) -> IVec3 {
        IVec3 { x: self.x, y: self.y, z: self.z }
    }

    #[inline(always)]
    fn with_xyz(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.x, y: rhs.y, z: rhs.z, w: self.w }
    }

    #[inline(always)]
    fn xyw(self: IVec4) -> IVec3 {
        IVec3 { x: self.x, y: self.y, z: self.w }
    }

    #[inline(always)]
    fn with_xyw(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.x, y: rhs.y, z: self.z, w: rhs.z }
    }

    #[inline(always)]
    fn xzx(self: IVec4) -> IVec3 {
        IVec3 { x: self.x, y: self.z, z: self.x }
    }

    #[inline(always)]
    fn xzy(self: IVec4) -> IVec3 {
        IVec3 { x: self.x, y: self.z, z: self.y }
    }

    #[inline(always)]
    fn with_xzy(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.x, y: rhs.z, z: rhs.y, w: self.w }
    }

    #[inline(always)]
    fn xzz(self: IVec4) -> IVec3 {
        IVec3 { x: self.x, y: self.z, z: self.z }
    }

    #[inline(always)]
    fn xzw(self: IVec4) -> IVec3 {
        IVec3 { x: self.x, y: self.z, z: self.w }
    }

    #[inline(always)]
    fn with_xzw(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.x, y: self.y, z: rhs.y, w: rhs.z }
    }

    #[inline(always)]
    fn xwx(self: IVec4) -> IVec3 {
        IVec3 { x: self.x, y: self.w, z: self.x }
    }

    #[inline(always)]
    fn xwy(self: IVec4) -> IVec3 {
        IVec3 { x: self.x, y: self.w, z: self.y }
    }

    #[inline(always)]
    fn with_xwy(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.x, y: rhs.z, z: self.z, w: rhs.y }
    }

    #[inline(always)]
    fn xwz(self: IVec4) -> IVec3 {
        IVec3 { x: self.x, y: self.w, z: self.z }
    }

    #[inline(always)]
    fn with_xwz(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.x, y: self.y, z: rhs.z, w: rhs.y }
    }

    #[inline(always)]
    fn xww(self: IVec4) -> IVec3 {
        IVec3 { x: self.x, y: self.w, z: self.w }
    }

    #[inline(always)]
    fn yxx(self: IVec4) -> IVec3 {
        IVec3 { x: self.y, y: self.x, z: self.x }
    }

    #[inline(always)]
    fn yxy(self: IVec4) -> IVec3 {
        IVec3 { x: self.y, y: self.x, z: self.y }
    }

    #[inline(always)]
    fn yxz(self: IVec4) -> IVec3 {
        IVec3 { x: self.y, y: self.x, z: self.z }
    }

    #[inline(always)]
    fn with_yxz(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.y, y: rhs.x, z: rhs.z, w: self.w }
    }

    #[inline(always)]
    fn yxw(self: IVec4) -> IVec3 {
        IVec3 { x: self.y, y: self.x, z: self.w }
    }

    #[inline(always)]
    fn with_yxw(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.y, y: rhs.x, z: self.z, w: rhs.z }
    }

    #[inline(always)]
    fn yyx(self: IVec4) -> IVec3 {
        IVec3 { x: self.y, y: self.y, z: self.x }
    }

    #[inline(always)]
    fn yyy(self: IVec4) -> IVec3 {
        IVec3 { x: self.y, y: self.y, z: self.y }
    }

    #[inline(always)]
    fn yyz(self: IVec4) -> IVec3 {
        IVec3 { x: self.y, y: self.y, z: self.z }
    }

    #[inline(always)]
    fn yyw(self: IVec4) -> IVec3 {
        IVec3 { x: self.y, y: self.y, z: self.w }
    }

    #[inline(always)]
    fn yzx(self: IVec4) -> IVec3 {
        IVec3 { x: self.y, y: self.z, z: self.x }
    }

    #[inline(always)]
    fn with_yzx(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.z, y: rhs.x, z: rhs.y, w: self.w }
    }

    #[inline(always)]
    fn yzy(self: IVec4) -> IVec3 {
        IVec3 { x: self.y, y: self.z, z: self.y }
    }

    #[inline(always)]
    fn yzz(self: IVec4) -> IVec3 {
        IVec3 { x: self.y, y: self.z, z: self.z }
    }

    #[inline(always)]
    fn yzw(self: IVec4) -> IVec3 {
        IVec3 { x: self.y, y: self.z, z: self.w }
    }

    #[inline(always)]
    fn with_yzw(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: self.x, y: rhs.x, z: rhs.y, w: rhs.z }
    }

    #[inline(always)]
    fn ywx(self: IVec4) -> IVec3 {
        IVec3 { x: self.y, y: self.w, z: self.x }
    }

    #[inline(always)]
    fn with_ywx(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.z, y: rhs.x, z: self.z, w: rhs.y }
    }

    #[inline(always)]
    fn ywy(self: IVec4) -> IVec3 {
        IVec3 { x: self.y, y: self.w, z: self.y }
    }

    #[inline(always)]
    fn ywz(self: IVec4) -> IVec3 {
        IVec3 { x: self.y, y: self.w, z: self.z }
    }

    #[inline(always)]
    fn with_ywz(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: self.x, y: rhs.x, z: rhs.z, w: rhs.y }
    }

    #[inline(always)]
    fn yww(self: IVec4) -> IVec3 {
        IVec3 { x: self.y, y: self.w, z: self.w }
    }

    #[inline(always)]
    fn zxx(self: IVec4) -> IVec3 {
        IVec3 { x: self.z, y: self.x, z: self.x }
    }

    #[inline(always)]
    fn zxy(self: IVec4) -> IVec3 {
        IVec3 { x: self.z, y: self.x, z: self.y }
    }

    #[inline(always)]
    fn with_zxy(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.y, y: rhs.z, z: rhs.x, w: self.w }
    }

    #[inline(always)]
    fn zxz(self: IVec4) -> IVec3 {
        IVec3 { x: self.z, y: self.x, z: self.z }
    }

    #[inline(always)]
    fn zxw(self: IVec4) -> IVec3 {
        IVec3 { x: self.z, y: self.x, z: self.w }
    }

    #[inline(always)]
    fn with_zxw(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.y, y: self.y, z: rhs.x, w: rhs.z }
    }

    #[inline(always)]
    fn zyx(self: IVec4) -> IVec3 {
        IVec3 { x: self.z, y: self.y, z: self.x }
    }

    #[inline(always)]
    fn with_zyx(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.z, y: rhs.y, z: rhs.x, w: self.w }
    }

    #[inline(always)]
    fn zyy(self: IVec4) -> IVec3 {
        IVec3 { x: self.z, y: self.y, z: self.y }
    }

    #[inline(always)]
    fn zyz(self: IVec4) -> IVec3 {
        IVec3 { x: self.z, y: self.y, z: self.z }
    }

    #[inline(always)]
    fn zyw(self: IVec4) -> IVec3 {
        IVec3 { x: self.z, y: self.y, z: self.w }
    }

    #[inline(always)]
    fn with_zyw(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: self.x, y: rhs.y, z: rhs.x, w: rhs.z }
    }

    #[inline(always)]
    fn zzx(self: IVec4) -> IVec3 {
        IVec3 { x: self.z, y: self.z, z: self.x }
    }

    #[inline(always)]
    fn zzy(self: IVec4) -> IVec3 {
        IVec3 { x: self.z, y: self.z, z: self.y }
    }

    #[inline(always)]
    fn zzz(self: IVec4) -> IVec3 {
        IVec3 { x: self.z, y: self.z, z: self.z }
    }

    #[inline(always)]
    fn zzw(self: IVec4) -> IVec3 {
        IVec3 { x: self.z, y: self.z, z: self.w }
    }

    #[inline(always)]
    fn zwx(self: IVec4) -> IVec3 {
        IVec3 { x: self.z, y: self.w, z: self.x }
    }

    #[inline(always)]
    fn with_zwx(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.z, y: self.y, z: rhs.x, w: rhs.y }
    }

    #[inline(always)]
    fn zwy(self: IVec4) -> IVec3 {
        IVec3 { x: self.z, y: self.w, z: self.y }
    }

    #[inline(always)]
    fn with_zwy(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: self.x, y: rhs.z, z: rhs.x, w: rhs.y }
    }

    #[inline(always)]
    fn zwz(self: IVec4) -> IVec3 {
        IVec3 { x: self.z, y: self.w, z: self.z }
    }

    #[inline(always)]
    fn zww(self: IVec4) -> IVec3 {
        IVec3 { x: self.z, y: self.w, z: self.w }
    }

    #[inline(always)]
    fn wxx(self: IVec4) -> IVec3 {
        IVec3 { x: self.w, y: self.x, z: self.x }
    }

    #[inline(always)]
    fn wxy(self: IVec4) -> IVec3 {
        IVec3 { x: self.w, y: self.x, z: self.y }
    }

    #[inline(always)]
    fn with_wxy(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.y, y: rhs.z, z: self.z, w: rhs.x }
    }

    #[inline(always)]
    fn wxz(self: IVec4) -> IVec3 {
        IVec3 { x: self.w, y: self.x, z: self.z }
    }

    #[inline(always)]
    fn with_wxz(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.y, y: self.y, z: rhs.z, w: rhs.x }
    }

    #[inline(always)]
    fn wxw(self: IVec4) -> IVec3 {
        IVec3 { x: self.w, y: self.x, z: self.w }
    }

    #[inline(always)]
    fn wyx(self: IVec4) -> IVec3 {
        IVec3 { x: self.w, y: self.y, z: self.x }
    }

    #[inline(always)]
    fn with_wyx(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.z, y: rhs.y, z: self.z, w: rhs.x }
    }

    #[inline(always)]
    fn wyy(self: IVec4) -> IVec3 {
        IVec3 { x: self.w, y: self.y, z: self.y }
    }

    #[inline(always)]
    fn wyz(self: IVec4) -> IVec3 {
        IVec3 { x: self.w, y: self.y, z: self.z }
    }

    #[inline(always)]
    fn with_wyz(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: self.x, y: rhs.y, z: rhs.z, w: rhs.x }
    }

    #[inline(always)]
    fn wyw(self: IVec4) -> IVec3 {
        IVec3 { x: self.w, y: self.y, z: self.w }
    }

    #[inline(always)]
    fn wzx(self: IVec4) -> IVec3 {
        IVec3 { x: self.w, y: self.z, z: self.x }
    }

    #[inline(always)]
    fn with_wzx(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: rhs.z, y: self.y, z: rhs.y, w: rhs.x }
    }

    #[inline(always)]
    fn wzy(self: IVec4) -> IVec3 {
        IVec3 { x: self.w, y: self.z, z: self.y }
    }

    #[inline(always)]
    fn with_wzy(self: IVec4, rhs: IVec3) -> IVec4 {
        IVec4 { x: self.x, y: rhs.z, z: rhs.y, w: rhs.x }
    }

    #[inline(always)]
    fn wzz(self: IVec4) -> IVec3 {
        IVec3 { x: self.w, y: self.z, z: self.z }
    }

    #[inline(always)]
    fn wzw(self: IVec4) -> IVec3 {
        IVec3 { x: self.w, y: self.z, z: self.w }
    }

    #[inline(always)]
    fn wwx(self: IVec4) -> IVec3 {
        IVec3 { x: self.w, y: self.w, z: self.x }
    }

    #[inline(always)]
    fn wwy(self: IVec4) -> IVec3 {
        IVec3 { x: self.w, y: self.w, z: self.y }
    }

    #[inline(always)]
    fn wwz(self: IVec4) -> IVec3 {
        IVec3 { x: self.w, y: self.w, z: self.z }
    }

    #[inline(always)]
    fn www(self: IVec4) -> IVec3 {
        IVec3 { x: self.w, y: self.w, z: self.w }
    }

    #[inline(always)]
    fn xxxx(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.x, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn xxxy(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.x, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn xxxz(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.x, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn xxxw(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.x, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn xxyx(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.x, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn xxyy(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.x, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn xxyz(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.x, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn xxyw(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.x, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn xxzx(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.x, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn xxzy(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.x, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn xxzz(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.x, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn xxzw(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.x, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn xxwx(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.x, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn xxwy(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.x, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn xxwz(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.x, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn xxww(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.x, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn xyxx(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn xyxy(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn xyxz(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn xyxw(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn xyyx(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn xyyy(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn xyyz(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn xyyw(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn xyzx(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn xyzy(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn xyzz(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn xywx(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn xywy(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn xywz(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn xyww(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.y, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn xzxx(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.z, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn xzxy(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.z, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn xzxz(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.z, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn xzxw(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.z, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn xzyx(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.z, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn xzyy(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.z, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn xzyz(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.z, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn xzyw(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.z, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn xzzx(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.z, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn xzzy(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.z, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn xzzz(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.z, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn xzzw(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.z, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn xzwx(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.z, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn xzwy(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.z, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn xzwz(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.z, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn xzww(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.z, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn xwxx(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.w, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn xwxy(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.w, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn xwxz(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.w, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn xwxw(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.w, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn xwyx(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.w, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn xwyy(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.w, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn xwyz(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.w, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn xwyw(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.w, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn xwzx(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.w, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn xwzy(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.w, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn xwzz(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.w, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn xwzw(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.w, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn xwwx(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.w, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn xwwy(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.w, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn xwwz(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.w, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn xwww(self: IVec4) -> IVec4 {
        IVec4 { x: self.x, y: self.w, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn yxxx(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.x, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn yxxy(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.x, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn yxxz(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.x, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn yxxw(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.x, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn yxyx(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.x, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn yxyy(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.x, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn yxyz(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.x, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn yxyw(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.x, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn yxzx(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.x, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn yxzy(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.x, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn yxzz(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.x, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn yxzw(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.x, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn yxwx(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.x, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn yxwy(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.x, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn yxwz(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.x, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn yxww(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.x, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn yyxx(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.y, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn yyxy(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.y, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn yyxz(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.y, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn yyxw(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.y, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn yyyx(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.y, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn yyyy(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.y, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn yyyz(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.y, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn yyyw(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.y, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn yyzx(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.y, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn yyzy(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.y, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn yyzz(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.y, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn yyzw(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.y, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn yywx(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.y, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn yywy(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.y, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn yywz(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.y, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn yyww(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.y, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn yzxx(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.z, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn yzxy(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.z, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn yzxz(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.z, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn yzxw(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.z, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn yzyx(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.z, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn yzyy(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.z, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn yzyz(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.z, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn yzyw(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.z, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn yzzx(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.z, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn yzzy(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.z, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn yzzz(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.z, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn yzzw(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.z, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn yzwx(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.z, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn yzwy(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.z, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn yzwz(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.z, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn yzww(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.z, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn ywxx(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.w, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn ywxy(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.w, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn ywxz(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.w, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn ywxw(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.w, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn ywyx(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.w, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn ywyy(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.w, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn ywyz(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.w, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn ywyw(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.w, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn ywzx(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.w, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn ywzy(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.w, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn ywzz(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.w, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn ywzw(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.w, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn ywwx(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.w, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn ywwy(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.w, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn ywwz(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.w, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn ywww(self: IVec4) -> IVec4 {
        IVec4 { x: self.y, y: self.w, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn zxxx(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.x, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn zxxy(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.x, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn zxxz(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.x, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn zxxw(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.x, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn zxyx(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.x, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn zxyy(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.x, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn zxyz(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.x, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn zxyw(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.x, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn zxzx(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.x, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn zxzy(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.x, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn zxzz(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.x, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn zxzw(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.x, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn zxwx(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.x, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn zxwy(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.x, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn zxwz(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.x, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn zxww(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.x, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn zyxx(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.y, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn zyxy(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.y, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn zyxz(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.y, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn zyxw(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.y, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn zyyx(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.y, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn zyyy(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.y, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn zyyz(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.y, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn zyyw(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.y, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn zyzx(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.y, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn zyzy(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.y, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn zyzz(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.y, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn zyzw(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.y, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn zywx(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.y, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn zywy(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.y, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn zywz(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.y, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn zyww(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.y, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn zzxx(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.z, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn zzxy(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.z, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn zzxz(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.z, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn zzxw(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.z, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn zzyx(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.z, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn zzyy(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.z, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn zzyz(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.z, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn zzyw(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.z, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn zzzx(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.z, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn zzzy(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.z, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn zzzz(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.z, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn zzzw(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.z, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn zzwx(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.z, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn zzwy(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.z, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn zzwz(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.z, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn zzww(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.z, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn zwxx(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.w, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn zwxy(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.w, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn zwxz(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.w, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn zwxw(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.w, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn zwyx(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.w, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn zwyy(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.w, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn zwyz(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.w, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn zwyw(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.w, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn zwzx(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.w, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn zwzy(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.w, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn zwzz(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.w, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn zwzw(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.w, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn zwwx(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.w, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn zwwy(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.w, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn zwwz(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.w, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn zwww(self: IVec4) -> IVec4 {
        IVec4 { x: self.z, y: self.w, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn wxxx(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.x, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn wxxy(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.x, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn wxxz(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.x, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn wxxw(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.x, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn wxyx(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.x, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn wxyy(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.x, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn wxyz(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.x, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn wxyw(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.x, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn wxzx(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.x, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn wxzy(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.x, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn wxzz(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.x, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn wxzw(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.x, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn wxwx(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.x, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn wxwy(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.x, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn wxwz(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.x, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn wxww(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.x, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn wyxx(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.y, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn wyxy(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.y, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn wyxz(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.y, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn wyxw(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.y, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn wyyx(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.y, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn wyyy(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.y, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn wyyz(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.y, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn wyyw(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.y, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn wyzx(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.y, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn wyzy(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.y, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn wyzz(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.y, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn wyzw(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.y, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn wywx(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.y, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn wywy(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.y, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn wywz(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.y, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn wyww(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.y, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn wzxx(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.z, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn wzxy(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.z, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn wzxz(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.z, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn wzxw(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.z, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn wzyx(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.z, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn wzyy(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.z, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn wzyz(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.z, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn wzyw(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.z, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn wzzx(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.z, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn wzzy(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.z, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn wzzz(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.z, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn wzzw(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.z, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn wzwx(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.z, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn wzwy(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.z, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn wzwz(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.z, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn wzww(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.z, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn wwxx(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.w, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn wwxy(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.w, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn wwxz(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.w, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn wwxw(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.w, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn wwyx(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.w, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn wwyy(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.w, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn wwyz(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.w, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn wwyw(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.w, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn wwzx(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.w, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn wwzy(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.w, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn wwzz(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.w, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn wwzw(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.w, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn wwwx(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.w, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn wwwy(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.w, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn wwwz(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.w, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn wwww(self: IVec4) -> IVec4 {
        IVec4 { x: self.w, y: self.w, z: self.w, w: self.w }
    }
}
