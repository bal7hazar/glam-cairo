// GENERATED by tools/codegen/swizzles.py - do not edit by hand.
//! Port of glam-rs `swizzles/vec_traits.rs` (`Vec2Swizzles`) and
//! `swizzles/uvec2_impl.rs` @ 0.33.8 for `UVec2`: 28 swizzle methods.
//!
//! The nine swizzle modules are generated from one template (`tools/codegen/swizzles.py`), as in
//! glam-rs. Every method is a struct construction from the components of `self`: no
//! arithmetic, no branch, no range check.

use crate::uvec2::UVec2;
use crate::uvec3::UVec3;
use crate::uvec4::UVec4;

/// The 28 swizzles of `UVec2` (scalar: `u32`): every permutation with
/// repetition of its components of length 2, 3 and 4 (`yx`, `xxyz`, ...) and the `with_*`
/// setters.
///
/// Mirrors `glam::Vec2Swizzles`, which glam-rs implements for the `f32`, `f64` and integer
/// vectors alike; Cairo has no associated types here, so there is one trait per vector type.
/// #### Panics
/// * Never: every method is a plain struct construction.
/// #### Deviations
/// * `Vec2` / `Vec3` / `Vec4` of the associated types are the concrete vector types of the same
///   scalar (`Vec*`, `IVec*` or `UVec*`).
/// * The `Panics` / `Deviations` sections of the methods are this one: never / none.
pub trait UVec2Swizzles {
    /// Returns `self`.
    ///
    /// Mirrors `glam::Vec2Swizzles::xy`.
    fn xy(self: UVec2) -> UVec2;
    /// Returns the `UVec2` `(self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::xx`.
    fn xx(self: UVec2) -> UVec2;
    /// Returns the `UVec2` `(self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::yx`.
    fn yx(self: UVec2) -> UVec2;
    /// Returns the `UVec2` `(self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::yy`.
    fn yy(self: UVec2) -> UVec2;
    /// Returns the `UVec3` `(self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::xxx`.
    fn xxx(self: UVec2) -> UVec3;
    /// Returns the `UVec3` `(self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::xxy`.
    fn xxy(self: UVec2) -> UVec3;
    /// Returns the `UVec3` `(self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::xyx`.
    fn xyx(self: UVec2) -> UVec3;
    /// Returns the `UVec3` `(self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::xyy`.
    fn xyy(self: UVec2) -> UVec3;
    /// Returns the `UVec3` `(self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::yxx`.
    fn yxx(self: UVec2) -> UVec3;
    /// Returns the `UVec3` `(self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::yxy`.
    fn yxy(self: UVec2) -> UVec3;
    /// Returns the `UVec3` `(self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::yyx`.
    fn yyx(self: UVec2) -> UVec3;
    /// Returns the `UVec3` `(self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::yyy`.
    fn yyy(self: UVec2) -> UVec3;
    /// Returns the `UVec4` `(self.x, self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::xxxx`.
    fn xxxx(self: UVec2) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::xxxy`.
    fn xxxy(self: UVec2) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::xxyx`.
    fn xxyx(self: UVec2) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::xxyy`.
    fn xxyy(self: UVec2) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::xyxx`.
    fn xyxx(self: UVec2) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::xyxy`.
    fn xyxy(self: UVec2) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::xyyx`.
    fn xyyx(self: UVec2) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::xyyy`.
    fn xyyy(self: UVec2) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::yxxx`.
    fn yxxx(self: UVec2) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::yxxy`.
    fn yxxy(self: UVec2) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::yxyx`.
    fn yxyx(self: UVec2) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::yxyy`.
    fn yxyy(self: UVec2) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::yyxx`.
    fn yyxx(self: UVec2) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::yyxy`.
    fn yyxy(self: UVec2) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::yyyx`.
    fn yyyx(self: UVec2) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec2Swizzles::yyyy`.
    fn yyyy(self: UVec2) -> UVec4;
}

pub impl UVec2SwizzlesImpl of UVec2Swizzles {
    #[inline(always)]
    fn xy(self: UVec2) -> UVec2 {
        UVec2 { x: self.x, y: self.y }
    }

    #[inline(always)]
    fn xx(self: UVec2) -> UVec2 {
        UVec2 { x: self.x, y: self.x }
    }

    #[inline(always)]
    fn yx(self: UVec2) -> UVec2 {
        UVec2 { x: self.y, y: self.x }
    }

    #[inline(always)]
    fn yy(self: UVec2) -> UVec2 {
        UVec2 { x: self.y, y: self.y }
    }

    #[inline(always)]
    fn xxx(self: UVec2) -> UVec3 {
        UVec3 { x: self.x, y: self.x, z: self.x }
    }

    #[inline(always)]
    fn xxy(self: UVec2) -> UVec3 {
        UVec3 { x: self.x, y: self.x, z: self.y }
    }

    #[inline(always)]
    fn xyx(self: UVec2) -> UVec3 {
        UVec3 { x: self.x, y: self.y, z: self.x }
    }

    #[inline(always)]
    fn xyy(self: UVec2) -> UVec3 {
        UVec3 { x: self.x, y: self.y, z: self.y }
    }

    #[inline(always)]
    fn yxx(self: UVec2) -> UVec3 {
        UVec3 { x: self.y, y: self.x, z: self.x }
    }

    #[inline(always)]
    fn yxy(self: UVec2) -> UVec3 {
        UVec3 { x: self.y, y: self.x, z: self.y }
    }

    #[inline(always)]
    fn yyx(self: UVec2) -> UVec3 {
        UVec3 { x: self.y, y: self.y, z: self.x }
    }

    #[inline(always)]
    fn yyy(self: UVec2) -> UVec3 {
        UVec3 { x: self.y, y: self.y, z: self.y }
    }

    #[inline(always)]
    fn xxxx(self: UVec2) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn xxxy(self: UVec2) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn xxyx(self: UVec2) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn xxyy(self: UVec2) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn xyxx(self: UVec2) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn xyxy(self: UVec2) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn xyyx(self: UVec2) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn xyyy(self: UVec2) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn yxxx(self: UVec2) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn yxxy(self: UVec2) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn yxyx(self: UVec2) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn yxyy(self: UVec2) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn yyxx(self: UVec2) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn yyxy(self: UVec2) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn yyyx(self: UVec2) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn yyyy(self: UVec2) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.y, w: self.y }
    }
}
