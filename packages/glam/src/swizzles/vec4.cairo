// GENERATED by tools/codegen/swizzles.py - do not edit by hand.
//! Port of glam-rs `swizzles/vec_traits.rs` (`Vec4Swizzles`) and
//! `swizzles/vec4_impl.rs` @ 0.33.8 for `Vec4`: 372 swizzle methods.
//!
//! The nine swizzle modules are generated from one template (`tools/codegen/swizzles.py`), as in
//! glam-rs. Every method is a struct construction from the components of `self`: no
//! arithmetic, no branch, no range check.

use crate::vec2::Vec2;
use crate::vec3::Vec3;
use crate::vec4::Vec4;

/// The 372 swizzles of `Vec4` (scalar: Q32.32 `fixed::Fixed`): every permutation with
/// repetition of its components of length 2, 3 and 4 (`yx`, `xxyz`, ...) and the `with_*`
/// setters.
///
/// Mirrors `glam::Vec4Swizzles`, which glam-rs implements for the `f32`, `f64` and integer
/// vectors alike; Cairo has no associated types here, so there is one trait per vector type.
/// #### Panics
/// * Never: every method is a plain struct construction.
/// #### Deviations
/// * `Vec2` / `Vec3` / `Vec4` of the associated types are the concrete vector types of the same
///   scalar (`Vec*`, `IVec*` or `UVec*`).
/// * The `Panics` / `Deviations` sections of the methods are this one: never / none.
pub trait Vec4Swizzles {
    /// Returns `self`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyzw`.
    fn xyzw(self: Vec4) -> Vec4;
    /// Returns the `Vec2` `(self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xx`.
    fn xx(self: Vec4) -> Vec2;
    /// Returns the `Vec2` `(self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xy`.
    fn xy(self: Vec4) -> Vec2;
    /// Returns `self` with `x` to `rhs.x`, `y` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xy`.
    fn with_xy(self: Vec4, rhs: Vec2) -> Vec4;
    /// Returns the `Vec2` `(self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xz`.
    fn xz(self: Vec4) -> Vec2;
    /// Returns `self` with `x` to `rhs.x`, `z` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xz`.
    fn with_xz(self: Vec4, rhs: Vec2) -> Vec4;
    /// Returns the `Vec2` `(self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xw`.
    fn xw(self: Vec4) -> Vec2;
    /// Returns `self` with `x` to `rhs.x`, `w` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xw`.
    fn with_xw(self: Vec4, rhs: Vec2) -> Vec4;
    /// Returns the `Vec2` `(self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yx`.
    fn yx(self: Vec4) -> Vec2;
    /// Returns `self` with `y` to `rhs.x`, `x` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yx`.
    fn with_yx(self: Vec4, rhs: Vec2) -> Vec4;
    /// Returns the `Vec2` `(self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yy`.
    fn yy(self: Vec4) -> Vec2;
    /// Returns the `Vec2` `(self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yz`.
    fn yz(self: Vec4) -> Vec2;
    /// Returns `self` with `y` to `rhs.x`, `z` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yz`.
    fn with_yz(self: Vec4, rhs: Vec2) -> Vec4;
    /// Returns the `Vec2` `(self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yw`.
    fn yw(self: Vec4) -> Vec2;
    /// Returns `self` with `y` to `rhs.x`, `w` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yw`.
    fn with_yw(self: Vec4, rhs: Vec2) -> Vec4;
    /// Returns the `Vec2` `(self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zx`.
    fn zx(self: Vec4) -> Vec2;
    /// Returns `self` with `z` to `rhs.x`, `x` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zx`.
    fn with_zx(self: Vec4, rhs: Vec2) -> Vec4;
    /// Returns the `Vec2` `(self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zy`.
    fn zy(self: Vec4) -> Vec2;
    /// Returns `self` with `z` to `rhs.x`, `y` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zy`.
    fn with_zy(self: Vec4, rhs: Vec2) -> Vec4;
    /// Returns the `Vec2` `(self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zz`.
    fn zz(self: Vec4) -> Vec2;
    /// Returns the `Vec2` `(self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zw`.
    fn zw(self: Vec4) -> Vec2;
    /// Returns `self` with `z` to `rhs.x`, `w` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zw`.
    fn with_zw(self: Vec4, rhs: Vec2) -> Vec4;
    /// Returns the `Vec2` `(self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wx`.
    fn wx(self: Vec4) -> Vec2;
    /// Returns `self` with `w` to `rhs.x`, `x` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wx`.
    fn with_wx(self: Vec4, rhs: Vec2) -> Vec4;
    /// Returns the `Vec2` `(self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wy`.
    fn wy(self: Vec4) -> Vec2;
    /// Returns `self` with `w` to `rhs.x`, `y` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wy`.
    fn with_wy(self: Vec4, rhs: Vec2) -> Vec4;
    /// Returns the `Vec2` `(self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wz`.
    fn wz(self: Vec4) -> Vec2;
    /// Returns `self` with `w` to `rhs.x`, `z` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wz`.
    fn with_wz(self: Vec4, rhs: Vec2) -> Vec4;
    /// Returns the `Vec2` `(self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ww`.
    fn ww(self: Vec4) -> Vec2;
    /// Returns the `Vec3` `(self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxx`.
    fn xxx(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxy`.
    fn xxy(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.x, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxz`.
    fn xxz(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.x, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxw`.
    fn xxw(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyx`.
    fn xyx(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyy`.
    fn xyy(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.x, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyz`.
    fn xyz(self: Vec4) -> Vec3;
    /// Returns `self` with `x` to `rhs.x`, `y` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xyz`.
    fn with_xyz(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.x, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyw`.
    fn xyw(self: Vec4) -> Vec3;
    /// Returns `self` with `x` to `rhs.x`, `y` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xyw`.
    fn with_xyw(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.x, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzx`.
    fn xzx(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.x, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzy`.
    fn xzy(self: Vec4) -> Vec3;
    /// Returns `self` with `x` to `rhs.x`, `z` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xzy`.
    fn with_xzy(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.x, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzz`.
    fn xzz(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.x, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzw`.
    fn xzw(self: Vec4) -> Vec3;
    /// Returns `self` with `x` to `rhs.x`, `z` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xzw`.
    fn with_xzw(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.x, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwx`.
    fn xwx(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.x, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwy`.
    fn xwy(self: Vec4) -> Vec3;
    /// Returns `self` with `x` to `rhs.x`, `w` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xwy`.
    fn with_xwy(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.x, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwz`.
    fn xwz(self: Vec4) -> Vec3;
    /// Returns `self` with `x` to `rhs.x`, `w` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xwz`.
    fn with_xwz(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.x, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xww`.
    fn xww(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxx`.
    fn yxx(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxy`.
    fn yxy(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.y, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxz`.
    fn yxz(self: Vec4) -> Vec3;
    /// Returns `self` with `y` to `rhs.x`, `x` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yxz`.
    fn with_yxz(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.y, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxw`.
    fn yxw(self: Vec4) -> Vec3;
    /// Returns `self` with `y` to `rhs.x`, `x` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yxw`.
    fn with_yxw(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyx`.
    fn yyx(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyy`.
    fn yyy(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.y, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyz`.
    fn yyz(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.y, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyw`.
    fn yyw(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.y, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzx`.
    fn yzx(self: Vec4) -> Vec3;
    /// Returns `self` with `y` to `rhs.x`, `z` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yzx`.
    fn with_yzx(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.y, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzy`.
    fn yzy(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.y, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzz`.
    fn yzz(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.y, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzw`.
    fn yzw(self: Vec4) -> Vec3;
    /// Returns `self` with `y` to `rhs.x`, `z` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yzw`.
    fn with_yzw(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.y, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywx`.
    fn ywx(self: Vec4) -> Vec3;
    /// Returns `self` with `y` to `rhs.x`, `w` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_ywx`.
    fn with_ywx(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.y, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywy`.
    fn ywy(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.y, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywz`.
    fn ywz(self: Vec4) -> Vec3;
    /// Returns `self` with `y` to `rhs.x`, `w` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_ywz`.
    fn with_ywz(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.y, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yww`.
    fn yww(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.z, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxx`.
    fn zxx(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.z, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxy`.
    fn zxy(self: Vec4) -> Vec3;
    /// Returns `self` with `z` to `rhs.x`, `x` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zxy`.
    fn with_zxy(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.z, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxz`.
    fn zxz(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.z, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxw`.
    fn zxw(self: Vec4) -> Vec3;
    /// Returns `self` with `z` to `rhs.x`, `x` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zxw`.
    fn with_zxw(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.z, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyx`.
    fn zyx(self: Vec4) -> Vec3;
    /// Returns `self` with `z` to `rhs.x`, `y` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zyx`.
    fn with_zyx(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.z, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyy`.
    fn zyy(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.z, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyz`.
    fn zyz(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.z, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyw`.
    fn zyw(self: Vec4) -> Vec3;
    /// Returns `self` with `z` to `rhs.x`, `y` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zyw`.
    fn with_zyw(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.z, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzx`.
    fn zzx(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.z, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzy`.
    fn zzy(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.z, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzz`.
    fn zzz(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.z, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzw`.
    fn zzw(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.z, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwx`.
    fn zwx(self: Vec4) -> Vec3;
    /// Returns `self` with `z` to `rhs.x`, `w` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zwx`.
    fn with_zwx(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.z, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwy`.
    fn zwy(self: Vec4) -> Vec3;
    /// Returns `self` with `z` to `rhs.x`, `w` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zwy`.
    fn with_zwy(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.z, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwz`.
    fn zwz(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.z, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zww`.
    fn zww(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.w, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxx`.
    fn wxx(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.w, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxy`.
    fn wxy(self: Vec4) -> Vec3;
    /// Returns `self` with `w` to `rhs.x`, `x` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wxy`.
    fn with_wxy(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.w, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxz`.
    fn wxz(self: Vec4) -> Vec3;
    /// Returns `self` with `w` to `rhs.x`, `x` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wxz`.
    fn with_wxz(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.w, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxw`.
    fn wxw(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.w, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyx`.
    fn wyx(self: Vec4) -> Vec3;
    /// Returns `self` with `w` to `rhs.x`, `y` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wyx`.
    fn with_wyx(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.w, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyy`.
    fn wyy(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.w, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyz`.
    fn wyz(self: Vec4) -> Vec3;
    /// Returns `self` with `w` to `rhs.x`, `y` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wyz`.
    fn with_wyz(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.w, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyw`.
    fn wyw(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.w, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzx`.
    fn wzx(self: Vec4) -> Vec3;
    /// Returns `self` with `w` to `rhs.x`, `z` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wzx`.
    fn with_wzx(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.w, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzy`.
    fn wzy(self: Vec4) -> Vec3;
    /// Returns `self` with `w` to `rhs.x`, `z` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wzy`.
    fn with_wzy(self: Vec4, rhs: Vec3) -> Vec4;
    /// Returns the `Vec3` `(self.w, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzz`.
    fn wzz(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.w, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzw`.
    fn wzw(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.w, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwx`.
    fn wwx(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.w, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwy`.
    fn wwy(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.w, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwz`.
    fn wwz(self: Vec4) -> Vec3;
    /// Returns the `Vec3` `(self.w, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::www`.
    fn www(self: Vec4) -> Vec3;
    /// Returns the `Vec4` `(self.x, self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxxx`.
    fn xxxx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxxy`.
    fn xxxy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.x, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxxz`.
    fn xxxz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.x, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxxw`.
    fn xxxw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxyx`.
    fn xxyx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxyy`.
    fn xxyy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.x, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxyz`.
    fn xxyz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.x, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxyw`.
    fn xxyw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.x, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxzx`.
    fn xxzx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.x, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxzy`.
    fn xxzy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.x, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxzz`.
    fn xxzz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.x, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxzw`.
    fn xxzw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.x, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxwx`.
    fn xxwx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.x, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxwy`.
    fn xxwy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.x, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxwz`.
    fn xxwz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.x, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxww`.
    fn xxww(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyxx`.
    fn xyxx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyxy`.
    fn xyxy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.y, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyxz`.
    fn xyxz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.y, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyxw`.
    fn xyxw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyyx`.
    fn xyyx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyyy`.
    fn xyyy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.y, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyyz`.
    fn xyyz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.y, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyyw`.
    fn xyyw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.y, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyzx`.
    fn xyzx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.y, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyzy`.
    fn xyzy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.y, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyzz`.
    fn xyzz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.y, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xywx`.
    fn xywx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.y, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xywy`.
    fn xywy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.y, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xywz`.
    fn xywz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.y, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyww`.
    fn xyww(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.z, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzxx`.
    fn xzxx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.z, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzxy`.
    fn xzxy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.z, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzxz`.
    fn xzxz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.z, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzxw`.
    fn xzxw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.z, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzyx`.
    fn xzyx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.z, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzyy`.
    fn xzyy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.z, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzyz`.
    fn xzyz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.z, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzyw`.
    fn xzyw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.z, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzzx`.
    fn xzzx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.z, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzzy`.
    fn xzzy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.z, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzzz`.
    fn xzzz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.z, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzzw`.
    fn xzzw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.z, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzwx`.
    fn xzwx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.z, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzwy`.
    fn xzwy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.z, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzwz`.
    fn xzwz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.z, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzww`.
    fn xzww(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.w, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwxx`.
    fn xwxx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.w, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwxy`.
    fn xwxy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.w, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwxz`.
    fn xwxz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.w, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwxw`.
    fn xwxw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.w, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwyx`.
    fn xwyx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.w, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwyy`.
    fn xwyy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.w, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwyz`.
    fn xwyz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.w, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwyw`.
    fn xwyw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.w, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwzx`.
    fn xwzx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.w, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwzy`.
    fn xwzy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.w, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwzz`.
    fn xwzz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.w, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwzw`.
    fn xwzw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.w, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwwx`.
    fn xwwx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.w, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwwy`.
    fn xwwy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.w, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwwz`.
    fn xwwz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.x, self.w, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwww`.
    fn xwww(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxxx`.
    fn yxxx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxxy`.
    fn yxxy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.x, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxxz`.
    fn yxxz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.x, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxxw`.
    fn yxxw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxyx`.
    fn yxyx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxyy`.
    fn yxyy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.x, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxyz`.
    fn yxyz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.x, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxyw`.
    fn yxyw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.x, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxzx`.
    fn yxzx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.x, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxzy`.
    fn yxzy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.x, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxzz`.
    fn yxzz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.x, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxzw`.
    fn yxzw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.x, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxwx`.
    fn yxwx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.x, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxwy`.
    fn yxwy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.x, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxwz`.
    fn yxwz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.x, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxww`.
    fn yxww(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyxx`.
    fn yyxx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyxy`.
    fn yyxy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.y, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyxz`.
    fn yyxz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.y, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyxw`.
    fn yyxw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyyx`.
    fn yyyx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyyy`.
    fn yyyy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.y, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyyz`.
    fn yyyz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.y, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyyw`.
    fn yyyw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.y, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyzx`.
    fn yyzx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.y, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyzy`.
    fn yyzy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.y, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyzz`.
    fn yyzz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.y, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyzw`.
    fn yyzw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.y, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yywx`.
    fn yywx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.y, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yywy`.
    fn yywy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.y, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yywz`.
    fn yywz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.y, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyww`.
    fn yyww(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.z, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzxx`.
    fn yzxx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.z, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzxy`.
    fn yzxy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.z, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzxz`.
    fn yzxz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.z, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzxw`.
    fn yzxw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.z, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzyx`.
    fn yzyx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.z, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzyy`.
    fn yzyy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.z, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzyz`.
    fn yzyz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.z, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzyw`.
    fn yzyw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.z, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzzx`.
    fn yzzx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.z, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzzy`.
    fn yzzy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.z, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzzz`.
    fn yzzz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.z, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzzw`.
    fn yzzw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.z, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzwx`.
    fn yzwx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.z, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzwy`.
    fn yzwy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.z, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzwz`.
    fn yzwz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.z, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzww`.
    fn yzww(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.w, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywxx`.
    fn ywxx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.w, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywxy`.
    fn ywxy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.w, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywxz`.
    fn ywxz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.w, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywxw`.
    fn ywxw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.w, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywyx`.
    fn ywyx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.w, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywyy`.
    fn ywyy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.w, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywyz`.
    fn ywyz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.w, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywyw`.
    fn ywyw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.w, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywzx`.
    fn ywzx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.w, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywzy`.
    fn ywzy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.w, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywzz`.
    fn ywzz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.w, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywzw`.
    fn ywzw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.w, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywwx`.
    fn ywwx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.w, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywwy`.
    fn ywwy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.w, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywwz`.
    fn ywwz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.y, self.w, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywww`.
    fn ywww(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxxx`.
    fn zxxx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxxy`.
    fn zxxy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.x, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxxz`.
    fn zxxz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.x, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxxw`.
    fn zxxw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxyx`.
    fn zxyx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxyy`.
    fn zxyy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.x, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxyz`.
    fn zxyz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.x, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxyw`.
    fn zxyw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.x, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxzx`.
    fn zxzx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.x, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxzy`.
    fn zxzy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.x, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxzz`.
    fn zxzz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.x, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxzw`.
    fn zxzw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.x, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxwx`.
    fn zxwx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.x, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxwy`.
    fn zxwy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.x, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxwz`.
    fn zxwz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.x, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxww`.
    fn zxww(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyxx`.
    fn zyxx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyxy`.
    fn zyxy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.y, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyxz`.
    fn zyxz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.y, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyxw`.
    fn zyxw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyyx`.
    fn zyyx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyyy`.
    fn zyyy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.y, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyyz`.
    fn zyyz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.y, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyyw`.
    fn zyyw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.y, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyzx`.
    fn zyzx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.y, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyzy`.
    fn zyzy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.y, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyzz`.
    fn zyzz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.y, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyzw`.
    fn zyzw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.y, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zywx`.
    fn zywx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.y, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zywy`.
    fn zywy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.y, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zywz`.
    fn zywz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.y, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyww`.
    fn zyww(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.z, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzxx`.
    fn zzxx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.z, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzxy`.
    fn zzxy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.z, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzxz`.
    fn zzxz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.z, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzxw`.
    fn zzxw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.z, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzyx`.
    fn zzyx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.z, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzyy`.
    fn zzyy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.z, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzyz`.
    fn zzyz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.z, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzyw`.
    fn zzyw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.z, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzzx`.
    fn zzzx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.z, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzzy`.
    fn zzzy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.z, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzzz`.
    fn zzzz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.z, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzzw`.
    fn zzzw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.z, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzwx`.
    fn zzwx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.z, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzwy`.
    fn zzwy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.z, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzwz`.
    fn zzwz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.z, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzww`.
    fn zzww(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.w, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwxx`.
    fn zwxx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.w, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwxy`.
    fn zwxy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.w, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwxz`.
    fn zwxz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.w, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwxw`.
    fn zwxw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.w, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwyx`.
    fn zwyx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.w, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwyy`.
    fn zwyy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.w, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwyz`.
    fn zwyz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.w, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwyw`.
    fn zwyw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.w, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwzx`.
    fn zwzx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.w, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwzy`.
    fn zwzy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.w, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwzz`.
    fn zwzz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.w, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwzw`.
    fn zwzw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.w, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwwx`.
    fn zwwx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.w, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwwy`.
    fn zwwy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.w, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwwz`.
    fn zwwz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.z, self.w, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwww`.
    fn zwww(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxxx`.
    fn wxxx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxxy`.
    fn wxxy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.x, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxxz`.
    fn wxxz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.x, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxxw`.
    fn wxxw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxyx`.
    fn wxyx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxyy`.
    fn wxyy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.x, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxyz`.
    fn wxyz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.x, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxyw`.
    fn wxyw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.x, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxzx`.
    fn wxzx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.x, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxzy`.
    fn wxzy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.x, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxzz`.
    fn wxzz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.x, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxzw`.
    fn wxzw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.x, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxwx`.
    fn wxwx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.x, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxwy`.
    fn wxwy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.x, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxwz`.
    fn wxwz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.x, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxww`.
    fn wxww(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyxx`.
    fn wyxx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyxy`.
    fn wyxy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.y, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyxz`.
    fn wyxz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.y, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyxw`.
    fn wyxw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyyx`.
    fn wyyx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyyy`.
    fn wyyy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.y, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyyz`.
    fn wyyz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.y, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyyw`.
    fn wyyw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.y, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyzx`.
    fn wyzx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.y, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyzy`.
    fn wyzy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.y, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyzz`.
    fn wyzz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.y, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyzw`.
    fn wyzw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.y, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wywx`.
    fn wywx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.y, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wywy`.
    fn wywy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.y, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wywz`.
    fn wywz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.y, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyww`.
    fn wyww(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.z, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzxx`.
    fn wzxx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.z, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzxy`.
    fn wzxy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.z, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzxz`.
    fn wzxz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.z, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzxw`.
    fn wzxw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.z, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzyx`.
    fn wzyx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.z, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzyy`.
    fn wzyy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.z, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzyz`.
    fn wzyz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.z, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzyw`.
    fn wzyw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.z, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzzx`.
    fn wzzx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.z, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzzy`.
    fn wzzy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.z, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzzz`.
    fn wzzz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.z, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzzw`.
    fn wzzw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.z, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzwx`.
    fn wzwx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.z, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzwy`.
    fn wzwy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.z, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzwz`.
    fn wzwz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.z, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzww`.
    fn wzww(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.w, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwxx`.
    fn wwxx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.w, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwxy`.
    fn wwxy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.w, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwxz`.
    fn wwxz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.w, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwxw`.
    fn wwxw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.w, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwyx`.
    fn wwyx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.w, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwyy`.
    fn wwyy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.w, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwyz`.
    fn wwyz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.w, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwyw`.
    fn wwyw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.w, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwzx`.
    fn wwzx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.w, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwzy`.
    fn wwzy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.w, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwzz`.
    fn wwzz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.w, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwzw`.
    fn wwzw(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.w, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwwx`.
    fn wwwx(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.w, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwwy`.
    fn wwwy(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.w, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwwz`.
    fn wwwz(self: Vec4) -> Vec4;
    /// Returns the `Vec4` `(self.w, self.w, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwww`.
    fn wwww(self: Vec4) -> Vec4;
}

pub impl Vec4SwizzlesImpl of Vec4Swizzles {
    #[inline(always)]
    fn xyzw(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn xx(self: Vec4) -> Vec2 {
        Vec2 { x: self.x, y: self.x }
    }

    #[inline(always)]
    fn xy(self: Vec4) -> Vec2 {
        Vec2 { x: self.x, y: self.y }
    }

    #[inline(always)]
    fn with_xy(self: Vec4, rhs: Vec2) -> Vec4 {
        Vec4 { x: rhs.x, y: rhs.y, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn xz(self: Vec4) -> Vec2 {
        Vec2 { x: self.x, y: self.z }
    }

    #[inline(always)]
    fn with_xz(self: Vec4, rhs: Vec2) -> Vec4 {
        Vec4 { x: rhs.x, y: self.y, z: rhs.y, w: self.w }
    }

    #[inline(always)]
    fn xw(self: Vec4) -> Vec2 {
        Vec2 { x: self.x, y: self.w }
    }

    #[inline(always)]
    fn with_xw(self: Vec4, rhs: Vec2) -> Vec4 {
        Vec4 { x: rhs.x, y: self.y, z: self.z, w: rhs.y }
    }

    #[inline(always)]
    fn yx(self: Vec4) -> Vec2 {
        Vec2 { x: self.y, y: self.x }
    }

    #[inline(always)]
    fn with_yx(self: Vec4, rhs: Vec2) -> Vec4 {
        Vec4 { x: rhs.y, y: rhs.x, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn yy(self: Vec4) -> Vec2 {
        Vec2 { x: self.y, y: self.y }
    }

    #[inline(always)]
    fn yz(self: Vec4) -> Vec2 {
        Vec2 { x: self.y, y: self.z }
    }

    #[inline(always)]
    fn with_yz(self: Vec4, rhs: Vec2) -> Vec4 {
        Vec4 { x: self.x, y: rhs.x, z: rhs.y, w: self.w }
    }

    #[inline(always)]
    fn yw(self: Vec4) -> Vec2 {
        Vec2 { x: self.y, y: self.w }
    }

    #[inline(always)]
    fn with_yw(self: Vec4, rhs: Vec2) -> Vec4 {
        Vec4 { x: self.x, y: rhs.x, z: self.z, w: rhs.y }
    }

    #[inline(always)]
    fn zx(self: Vec4) -> Vec2 {
        Vec2 { x: self.z, y: self.x }
    }

    #[inline(always)]
    fn with_zx(self: Vec4, rhs: Vec2) -> Vec4 {
        Vec4 { x: rhs.y, y: self.y, z: rhs.x, w: self.w }
    }

    #[inline(always)]
    fn zy(self: Vec4) -> Vec2 {
        Vec2 { x: self.z, y: self.y }
    }

    #[inline(always)]
    fn with_zy(self: Vec4, rhs: Vec2) -> Vec4 {
        Vec4 { x: self.x, y: rhs.y, z: rhs.x, w: self.w }
    }

    #[inline(always)]
    fn zz(self: Vec4) -> Vec2 {
        Vec2 { x: self.z, y: self.z }
    }

    #[inline(always)]
    fn zw(self: Vec4) -> Vec2 {
        Vec2 { x: self.z, y: self.w }
    }

    #[inline(always)]
    fn with_zw(self: Vec4, rhs: Vec2) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: rhs.x, w: rhs.y }
    }

    #[inline(always)]
    fn wx(self: Vec4) -> Vec2 {
        Vec2 { x: self.w, y: self.x }
    }

    #[inline(always)]
    fn with_wx(self: Vec4, rhs: Vec2) -> Vec4 {
        Vec4 { x: rhs.y, y: self.y, z: self.z, w: rhs.x }
    }

    #[inline(always)]
    fn wy(self: Vec4) -> Vec2 {
        Vec2 { x: self.w, y: self.y }
    }

    #[inline(always)]
    fn with_wy(self: Vec4, rhs: Vec2) -> Vec4 {
        Vec4 { x: self.x, y: rhs.y, z: self.z, w: rhs.x }
    }

    #[inline(always)]
    fn wz(self: Vec4) -> Vec2 {
        Vec2 { x: self.w, y: self.z }
    }

    #[inline(always)]
    fn with_wz(self: Vec4, rhs: Vec2) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: rhs.y, w: rhs.x }
    }

    #[inline(always)]
    fn ww(self: Vec4) -> Vec2 {
        Vec2 { x: self.w, y: self.w }
    }

    #[inline(always)]
    fn xxx(self: Vec4) -> Vec3 {
        Vec3 { x: self.x, y: self.x, z: self.x }
    }

    #[inline(always)]
    fn xxy(self: Vec4) -> Vec3 {
        Vec3 { x: self.x, y: self.x, z: self.y }
    }

    #[inline(always)]
    fn xxz(self: Vec4) -> Vec3 {
        Vec3 { x: self.x, y: self.x, z: self.z }
    }

    #[inline(always)]
    fn xxw(self: Vec4) -> Vec3 {
        Vec3 { x: self.x, y: self.x, z: self.w }
    }

    #[inline(always)]
    fn xyx(self: Vec4) -> Vec3 {
        Vec3 { x: self.x, y: self.y, z: self.x }
    }

    #[inline(always)]
    fn xyy(self: Vec4) -> Vec3 {
        Vec3 { x: self.x, y: self.y, z: self.y }
    }

    #[inline(always)]
    fn xyz(self: Vec4) -> Vec3 {
        Vec3 { x: self.x, y: self.y, z: self.z }
    }

    #[inline(always)]
    fn with_xyz(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.x, y: rhs.y, z: rhs.z, w: self.w }
    }

    #[inline(always)]
    fn xyw(self: Vec4) -> Vec3 {
        Vec3 { x: self.x, y: self.y, z: self.w }
    }

    #[inline(always)]
    fn with_xyw(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.x, y: rhs.y, z: self.z, w: rhs.z }
    }

    #[inline(always)]
    fn xzx(self: Vec4) -> Vec3 {
        Vec3 { x: self.x, y: self.z, z: self.x }
    }

    #[inline(always)]
    fn xzy(self: Vec4) -> Vec3 {
        Vec3 { x: self.x, y: self.z, z: self.y }
    }

    #[inline(always)]
    fn with_xzy(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.x, y: rhs.z, z: rhs.y, w: self.w }
    }

    #[inline(always)]
    fn xzz(self: Vec4) -> Vec3 {
        Vec3 { x: self.x, y: self.z, z: self.z }
    }

    #[inline(always)]
    fn xzw(self: Vec4) -> Vec3 {
        Vec3 { x: self.x, y: self.z, z: self.w }
    }

    #[inline(always)]
    fn with_xzw(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.x, y: self.y, z: rhs.y, w: rhs.z }
    }

    #[inline(always)]
    fn xwx(self: Vec4) -> Vec3 {
        Vec3 { x: self.x, y: self.w, z: self.x }
    }

    #[inline(always)]
    fn xwy(self: Vec4) -> Vec3 {
        Vec3 { x: self.x, y: self.w, z: self.y }
    }

    #[inline(always)]
    fn with_xwy(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.x, y: rhs.z, z: self.z, w: rhs.y }
    }

    #[inline(always)]
    fn xwz(self: Vec4) -> Vec3 {
        Vec3 { x: self.x, y: self.w, z: self.z }
    }

    #[inline(always)]
    fn with_xwz(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.x, y: self.y, z: rhs.z, w: rhs.y }
    }

    #[inline(always)]
    fn xww(self: Vec4) -> Vec3 {
        Vec3 { x: self.x, y: self.w, z: self.w }
    }

    #[inline(always)]
    fn yxx(self: Vec4) -> Vec3 {
        Vec3 { x: self.y, y: self.x, z: self.x }
    }

    #[inline(always)]
    fn yxy(self: Vec4) -> Vec3 {
        Vec3 { x: self.y, y: self.x, z: self.y }
    }

    #[inline(always)]
    fn yxz(self: Vec4) -> Vec3 {
        Vec3 { x: self.y, y: self.x, z: self.z }
    }

    #[inline(always)]
    fn with_yxz(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.y, y: rhs.x, z: rhs.z, w: self.w }
    }

    #[inline(always)]
    fn yxw(self: Vec4) -> Vec3 {
        Vec3 { x: self.y, y: self.x, z: self.w }
    }

    #[inline(always)]
    fn with_yxw(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.y, y: rhs.x, z: self.z, w: rhs.z }
    }

    #[inline(always)]
    fn yyx(self: Vec4) -> Vec3 {
        Vec3 { x: self.y, y: self.y, z: self.x }
    }

    #[inline(always)]
    fn yyy(self: Vec4) -> Vec3 {
        Vec3 { x: self.y, y: self.y, z: self.y }
    }

    #[inline(always)]
    fn yyz(self: Vec4) -> Vec3 {
        Vec3 { x: self.y, y: self.y, z: self.z }
    }

    #[inline(always)]
    fn yyw(self: Vec4) -> Vec3 {
        Vec3 { x: self.y, y: self.y, z: self.w }
    }

    #[inline(always)]
    fn yzx(self: Vec4) -> Vec3 {
        Vec3 { x: self.y, y: self.z, z: self.x }
    }

    #[inline(always)]
    fn with_yzx(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.z, y: rhs.x, z: rhs.y, w: self.w }
    }

    #[inline(always)]
    fn yzy(self: Vec4) -> Vec3 {
        Vec3 { x: self.y, y: self.z, z: self.y }
    }

    #[inline(always)]
    fn yzz(self: Vec4) -> Vec3 {
        Vec3 { x: self.y, y: self.z, z: self.z }
    }

    #[inline(always)]
    fn yzw(self: Vec4) -> Vec3 {
        Vec3 { x: self.y, y: self.z, z: self.w }
    }

    #[inline(always)]
    fn with_yzw(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: self.x, y: rhs.x, z: rhs.y, w: rhs.z }
    }

    #[inline(always)]
    fn ywx(self: Vec4) -> Vec3 {
        Vec3 { x: self.y, y: self.w, z: self.x }
    }

    #[inline(always)]
    fn with_ywx(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.z, y: rhs.x, z: self.z, w: rhs.y }
    }

    #[inline(always)]
    fn ywy(self: Vec4) -> Vec3 {
        Vec3 { x: self.y, y: self.w, z: self.y }
    }

    #[inline(always)]
    fn ywz(self: Vec4) -> Vec3 {
        Vec3 { x: self.y, y: self.w, z: self.z }
    }

    #[inline(always)]
    fn with_ywz(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: self.x, y: rhs.x, z: rhs.z, w: rhs.y }
    }

    #[inline(always)]
    fn yww(self: Vec4) -> Vec3 {
        Vec3 { x: self.y, y: self.w, z: self.w }
    }

    #[inline(always)]
    fn zxx(self: Vec4) -> Vec3 {
        Vec3 { x: self.z, y: self.x, z: self.x }
    }

    #[inline(always)]
    fn zxy(self: Vec4) -> Vec3 {
        Vec3 { x: self.z, y: self.x, z: self.y }
    }

    #[inline(always)]
    fn with_zxy(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.y, y: rhs.z, z: rhs.x, w: self.w }
    }

    #[inline(always)]
    fn zxz(self: Vec4) -> Vec3 {
        Vec3 { x: self.z, y: self.x, z: self.z }
    }

    #[inline(always)]
    fn zxw(self: Vec4) -> Vec3 {
        Vec3 { x: self.z, y: self.x, z: self.w }
    }

    #[inline(always)]
    fn with_zxw(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.y, y: self.y, z: rhs.x, w: rhs.z }
    }

    #[inline(always)]
    fn zyx(self: Vec4) -> Vec3 {
        Vec3 { x: self.z, y: self.y, z: self.x }
    }

    #[inline(always)]
    fn with_zyx(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.z, y: rhs.y, z: rhs.x, w: self.w }
    }

    #[inline(always)]
    fn zyy(self: Vec4) -> Vec3 {
        Vec3 { x: self.z, y: self.y, z: self.y }
    }

    #[inline(always)]
    fn zyz(self: Vec4) -> Vec3 {
        Vec3 { x: self.z, y: self.y, z: self.z }
    }

    #[inline(always)]
    fn zyw(self: Vec4) -> Vec3 {
        Vec3 { x: self.z, y: self.y, z: self.w }
    }

    #[inline(always)]
    fn with_zyw(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: self.x, y: rhs.y, z: rhs.x, w: rhs.z }
    }

    #[inline(always)]
    fn zzx(self: Vec4) -> Vec3 {
        Vec3 { x: self.z, y: self.z, z: self.x }
    }

    #[inline(always)]
    fn zzy(self: Vec4) -> Vec3 {
        Vec3 { x: self.z, y: self.z, z: self.y }
    }

    #[inline(always)]
    fn zzz(self: Vec4) -> Vec3 {
        Vec3 { x: self.z, y: self.z, z: self.z }
    }

    #[inline(always)]
    fn zzw(self: Vec4) -> Vec3 {
        Vec3 { x: self.z, y: self.z, z: self.w }
    }

    #[inline(always)]
    fn zwx(self: Vec4) -> Vec3 {
        Vec3 { x: self.z, y: self.w, z: self.x }
    }

    #[inline(always)]
    fn with_zwx(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.z, y: self.y, z: rhs.x, w: rhs.y }
    }

    #[inline(always)]
    fn zwy(self: Vec4) -> Vec3 {
        Vec3 { x: self.z, y: self.w, z: self.y }
    }

    #[inline(always)]
    fn with_zwy(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: self.x, y: rhs.z, z: rhs.x, w: rhs.y }
    }

    #[inline(always)]
    fn zwz(self: Vec4) -> Vec3 {
        Vec3 { x: self.z, y: self.w, z: self.z }
    }

    #[inline(always)]
    fn zww(self: Vec4) -> Vec3 {
        Vec3 { x: self.z, y: self.w, z: self.w }
    }

    #[inline(always)]
    fn wxx(self: Vec4) -> Vec3 {
        Vec3 { x: self.w, y: self.x, z: self.x }
    }

    #[inline(always)]
    fn wxy(self: Vec4) -> Vec3 {
        Vec3 { x: self.w, y: self.x, z: self.y }
    }

    #[inline(always)]
    fn with_wxy(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.y, y: rhs.z, z: self.z, w: rhs.x }
    }

    #[inline(always)]
    fn wxz(self: Vec4) -> Vec3 {
        Vec3 { x: self.w, y: self.x, z: self.z }
    }

    #[inline(always)]
    fn with_wxz(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.y, y: self.y, z: rhs.z, w: rhs.x }
    }

    #[inline(always)]
    fn wxw(self: Vec4) -> Vec3 {
        Vec3 { x: self.w, y: self.x, z: self.w }
    }

    #[inline(always)]
    fn wyx(self: Vec4) -> Vec3 {
        Vec3 { x: self.w, y: self.y, z: self.x }
    }

    #[inline(always)]
    fn with_wyx(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.z, y: rhs.y, z: self.z, w: rhs.x }
    }

    #[inline(always)]
    fn wyy(self: Vec4) -> Vec3 {
        Vec3 { x: self.w, y: self.y, z: self.y }
    }

    #[inline(always)]
    fn wyz(self: Vec4) -> Vec3 {
        Vec3 { x: self.w, y: self.y, z: self.z }
    }

    #[inline(always)]
    fn with_wyz(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: self.x, y: rhs.y, z: rhs.z, w: rhs.x }
    }

    #[inline(always)]
    fn wyw(self: Vec4) -> Vec3 {
        Vec3 { x: self.w, y: self.y, z: self.w }
    }

    #[inline(always)]
    fn wzx(self: Vec4) -> Vec3 {
        Vec3 { x: self.w, y: self.z, z: self.x }
    }

    #[inline(always)]
    fn with_wzx(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: rhs.z, y: self.y, z: rhs.y, w: rhs.x }
    }

    #[inline(always)]
    fn wzy(self: Vec4) -> Vec3 {
        Vec3 { x: self.w, y: self.z, z: self.y }
    }

    #[inline(always)]
    fn with_wzy(self: Vec4, rhs: Vec3) -> Vec4 {
        Vec4 { x: self.x, y: rhs.z, z: rhs.y, w: rhs.x }
    }

    #[inline(always)]
    fn wzz(self: Vec4) -> Vec3 {
        Vec3 { x: self.w, y: self.z, z: self.z }
    }

    #[inline(always)]
    fn wzw(self: Vec4) -> Vec3 {
        Vec3 { x: self.w, y: self.z, z: self.w }
    }

    #[inline(always)]
    fn wwx(self: Vec4) -> Vec3 {
        Vec3 { x: self.w, y: self.w, z: self.x }
    }

    #[inline(always)]
    fn wwy(self: Vec4) -> Vec3 {
        Vec3 { x: self.w, y: self.w, z: self.y }
    }

    #[inline(always)]
    fn wwz(self: Vec4) -> Vec3 {
        Vec3 { x: self.w, y: self.w, z: self.z }
    }

    #[inline(always)]
    fn www(self: Vec4) -> Vec3 {
        Vec3 { x: self.w, y: self.w, z: self.w }
    }

    #[inline(always)]
    fn xxxx(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.x, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn xxxy(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.x, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn xxxz(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.x, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn xxxw(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.x, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn xxyx(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.x, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn xxyy(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.x, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn xxyz(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.x, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn xxyw(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.x, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn xxzx(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.x, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn xxzy(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.x, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn xxzz(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.x, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn xxzw(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.x, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn xxwx(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.x, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn xxwy(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.x, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn xxwz(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.x, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn xxww(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.x, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn xyxx(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn xyxy(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn xyxz(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn xyxw(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn xyyx(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn xyyy(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn xyyz(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn xyyw(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn xyzx(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn xyzy(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn xyzz(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn xywx(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn xywy(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn xywz(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn xyww(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.y, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn xzxx(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.z, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn xzxy(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.z, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn xzxz(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.z, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn xzxw(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.z, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn xzyx(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.z, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn xzyy(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.z, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn xzyz(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.z, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn xzyw(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.z, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn xzzx(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.z, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn xzzy(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.z, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn xzzz(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.z, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn xzzw(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.z, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn xzwx(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.z, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn xzwy(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.z, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn xzwz(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.z, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn xzww(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.z, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn xwxx(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.w, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn xwxy(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.w, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn xwxz(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.w, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn xwxw(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.w, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn xwyx(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.w, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn xwyy(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.w, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn xwyz(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.w, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn xwyw(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.w, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn xwzx(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.w, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn xwzy(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.w, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn xwzz(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.w, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn xwzw(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.w, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn xwwx(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.w, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn xwwy(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.w, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn xwwz(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.w, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn xwww(self: Vec4) -> Vec4 {
        Vec4 { x: self.x, y: self.w, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn yxxx(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.x, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn yxxy(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.x, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn yxxz(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.x, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn yxxw(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.x, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn yxyx(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.x, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn yxyy(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.x, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn yxyz(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.x, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn yxyw(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.x, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn yxzx(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.x, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn yxzy(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.x, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn yxzz(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.x, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn yxzw(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.x, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn yxwx(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.x, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn yxwy(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.x, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn yxwz(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.x, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn yxww(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.x, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn yyxx(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.y, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn yyxy(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.y, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn yyxz(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.y, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn yyxw(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.y, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn yyyx(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.y, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn yyyy(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.y, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn yyyz(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.y, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn yyyw(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.y, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn yyzx(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.y, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn yyzy(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.y, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn yyzz(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.y, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn yyzw(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.y, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn yywx(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.y, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn yywy(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.y, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn yywz(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.y, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn yyww(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.y, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn yzxx(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.z, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn yzxy(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.z, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn yzxz(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.z, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn yzxw(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.z, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn yzyx(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.z, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn yzyy(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.z, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn yzyz(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.z, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn yzyw(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.z, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn yzzx(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.z, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn yzzy(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.z, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn yzzz(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.z, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn yzzw(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.z, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn yzwx(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.z, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn yzwy(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.z, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn yzwz(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.z, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn yzww(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.z, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn ywxx(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.w, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn ywxy(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.w, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn ywxz(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.w, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn ywxw(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.w, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn ywyx(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.w, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn ywyy(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.w, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn ywyz(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.w, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn ywyw(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.w, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn ywzx(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.w, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn ywzy(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.w, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn ywzz(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.w, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn ywzw(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.w, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn ywwx(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.w, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn ywwy(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.w, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn ywwz(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.w, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn ywww(self: Vec4) -> Vec4 {
        Vec4 { x: self.y, y: self.w, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn zxxx(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.x, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn zxxy(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.x, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn zxxz(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.x, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn zxxw(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.x, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn zxyx(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.x, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn zxyy(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.x, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn zxyz(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.x, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn zxyw(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.x, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn zxzx(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.x, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn zxzy(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.x, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn zxzz(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.x, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn zxzw(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.x, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn zxwx(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.x, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn zxwy(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.x, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn zxwz(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.x, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn zxww(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.x, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn zyxx(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.y, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn zyxy(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.y, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn zyxz(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.y, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn zyxw(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.y, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn zyyx(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.y, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn zyyy(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.y, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn zyyz(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.y, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn zyyw(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.y, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn zyzx(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.y, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn zyzy(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.y, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn zyzz(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.y, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn zyzw(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.y, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn zywx(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.y, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn zywy(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.y, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn zywz(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.y, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn zyww(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.y, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn zzxx(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.z, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn zzxy(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.z, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn zzxz(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.z, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn zzxw(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.z, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn zzyx(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.z, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn zzyy(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.z, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn zzyz(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.z, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn zzyw(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.z, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn zzzx(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.z, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn zzzy(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.z, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn zzzz(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.z, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn zzzw(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.z, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn zzwx(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.z, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn zzwy(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.z, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn zzwz(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.z, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn zzww(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.z, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn zwxx(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.w, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn zwxy(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.w, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn zwxz(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.w, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn zwxw(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.w, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn zwyx(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.w, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn zwyy(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.w, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn zwyz(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.w, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn zwyw(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.w, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn zwzx(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.w, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn zwzy(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.w, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn zwzz(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.w, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn zwzw(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.w, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn zwwx(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.w, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn zwwy(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.w, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn zwwz(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.w, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn zwww(self: Vec4) -> Vec4 {
        Vec4 { x: self.z, y: self.w, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn wxxx(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.x, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn wxxy(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.x, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn wxxz(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.x, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn wxxw(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.x, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn wxyx(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.x, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn wxyy(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.x, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn wxyz(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.x, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn wxyw(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.x, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn wxzx(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.x, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn wxzy(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.x, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn wxzz(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.x, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn wxzw(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.x, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn wxwx(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.x, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn wxwy(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.x, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn wxwz(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.x, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn wxww(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.x, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn wyxx(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.y, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn wyxy(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.y, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn wyxz(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.y, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn wyxw(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.y, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn wyyx(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.y, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn wyyy(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.y, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn wyyz(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.y, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn wyyw(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.y, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn wyzx(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.y, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn wyzy(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.y, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn wyzz(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.y, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn wyzw(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.y, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn wywx(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.y, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn wywy(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.y, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn wywz(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.y, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn wyww(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.y, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn wzxx(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.z, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn wzxy(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.z, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn wzxz(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.z, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn wzxw(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.z, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn wzyx(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.z, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn wzyy(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.z, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn wzyz(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.z, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn wzyw(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.z, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn wzzx(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.z, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn wzzy(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.z, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn wzzz(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.z, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn wzzw(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.z, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn wzwx(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.z, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn wzwy(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.z, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn wzwz(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.z, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn wzww(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.z, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn wwxx(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.w, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn wwxy(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.w, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn wwxz(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.w, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn wwxw(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.w, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn wwyx(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.w, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn wwyy(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.w, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn wwyz(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.w, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn wwyw(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.w, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn wwzx(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.w, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn wwzy(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.w, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn wwzz(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.w, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn wwzw(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.w, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn wwwx(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.w, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn wwwy(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.w, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn wwwz(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.w, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn wwww(self: Vec4) -> Vec4 {
        Vec4 { x: self.w, y: self.w, z: self.w, w: self.w }
    }
}
