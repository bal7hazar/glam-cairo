// GENERATED by tools/codegen/swizzles.py - do not edit by hand.
//! Port of glam-rs `swizzles/vec_traits.rs` (`Vec4Swizzles`) and
//! `swizzles/uvec4_impl.rs` @ 0.33.8 for `UVec4`: 372 swizzle methods.
//!
//! The nine swizzle modules are generated from one template (`tools/codegen/swizzles.py`), as in
//! glam-rs. Every method is a struct construction from the components of `self`: no
//! arithmetic, no branch, no range check.

use crate::uvec2::UVec2;
use crate::uvec3::UVec3;
use crate::uvec4::UVec4;

/// The 372 swizzles of `UVec4` (scalar: `u32`): every permutation with
/// repetition of its components of length 2, 3 and 4 (`yx`, `xxyz`, ...) and the `with_*`
/// setters.
///
/// Mirrors `glam::Vec4Swizzles`, which glam-rs implements for the `f32`, `f64` and integer
/// vectors alike; Cairo has no associated types here, so there is one trait per vector type.
/// #### Panics
/// * Never: every method is a plain struct construction.
/// #### Deviations
/// * `Vec2` / `Vec3` / `Vec4` of the associated types are the concrete vector types of the same
///   scalar (`Vec*`, `IVec*` or `UVec*`).
/// * The `Panics` / `Deviations` sections of the methods are this one: never / none.
pub trait UVec4Swizzles {
    /// Returns `self`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyzw`.
    fn xyzw(self: UVec4) -> UVec4;
    /// Returns the `UVec2` `(self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xx`.
    fn xx(self: UVec4) -> UVec2;
    /// Returns the `UVec2` `(self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xy`.
    fn xy(self: UVec4) -> UVec2;
    /// Returns `self` with `x` to `rhs.x`, `y` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xy`.
    fn with_xy(self: UVec4, rhs: UVec2) -> UVec4;
    /// Returns the `UVec2` `(self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xz`.
    fn xz(self: UVec4) -> UVec2;
    /// Returns `self` with `x` to `rhs.x`, `z` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xz`.
    fn with_xz(self: UVec4, rhs: UVec2) -> UVec4;
    /// Returns the `UVec2` `(self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xw`.
    fn xw(self: UVec4) -> UVec2;
    /// Returns `self` with `x` to `rhs.x`, `w` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xw`.
    fn with_xw(self: UVec4, rhs: UVec2) -> UVec4;
    /// Returns the `UVec2` `(self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yx`.
    fn yx(self: UVec4) -> UVec2;
    /// Returns `self` with `y` to `rhs.x`, `x` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yx`.
    fn with_yx(self: UVec4, rhs: UVec2) -> UVec4;
    /// Returns the `UVec2` `(self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yy`.
    fn yy(self: UVec4) -> UVec2;
    /// Returns the `UVec2` `(self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yz`.
    fn yz(self: UVec4) -> UVec2;
    /// Returns `self` with `y` to `rhs.x`, `z` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yz`.
    fn with_yz(self: UVec4, rhs: UVec2) -> UVec4;
    /// Returns the `UVec2` `(self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yw`.
    fn yw(self: UVec4) -> UVec2;
    /// Returns `self` with `y` to `rhs.x`, `w` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yw`.
    fn with_yw(self: UVec4, rhs: UVec2) -> UVec4;
    /// Returns the `UVec2` `(self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zx`.
    fn zx(self: UVec4) -> UVec2;
    /// Returns `self` with `z` to `rhs.x`, `x` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zx`.
    fn with_zx(self: UVec4, rhs: UVec2) -> UVec4;
    /// Returns the `UVec2` `(self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zy`.
    fn zy(self: UVec4) -> UVec2;
    /// Returns `self` with `z` to `rhs.x`, `y` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zy`.
    fn with_zy(self: UVec4, rhs: UVec2) -> UVec4;
    /// Returns the `UVec2` `(self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zz`.
    fn zz(self: UVec4) -> UVec2;
    /// Returns the `UVec2` `(self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zw`.
    fn zw(self: UVec4) -> UVec2;
    /// Returns `self` with `z` to `rhs.x`, `w` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zw`.
    fn with_zw(self: UVec4, rhs: UVec2) -> UVec4;
    /// Returns the `UVec2` `(self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wx`.
    fn wx(self: UVec4) -> UVec2;
    /// Returns `self` with `w` to `rhs.x`, `x` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wx`.
    fn with_wx(self: UVec4, rhs: UVec2) -> UVec4;
    /// Returns the `UVec2` `(self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wy`.
    fn wy(self: UVec4) -> UVec2;
    /// Returns `self` with `w` to `rhs.x`, `y` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wy`.
    fn with_wy(self: UVec4, rhs: UVec2) -> UVec4;
    /// Returns the `UVec2` `(self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wz`.
    fn wz(self: UVec4) -> UVec2;
    /// Returns `self` with `w` to `rhs.x`, `z` to `rhs.y`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wz`.
    fn with_wz(self: UVec4, rhs: UVec2) -> UVec4;
    /// Returns the `UVec2` `(self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ww`.
    fn ww(self: UVec4) -> UVec2;
    /// Returns the `UVec3` `(self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxx`.
    fn xxx(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxy`.
    fn xxy(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.x, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxz`.
    fn xxz(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.x, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxw`.
    fn xxw(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyx`.
    fn xyx(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyy`.
    fn xyy(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.x, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyz`.
    fn xyz(self: UVec4) -> UVec3;
    /// Returns `self` with `x` to `rhs.x`, `y` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xyz`.
    fn with_xyz(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.x, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyw`.
    fn xyw(self: UVec4) -> UVec3;
    /// Returns `self` with `x` to `rhs.x`, `y` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xyw`.
    fn with_xyw(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.x, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzx`.
    fn xzx(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.x, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzy`.
    fn xzy(self: UVec4) -> UVec3;
    /// Returns `self` with `x` to `rhs.x`, `z` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xzy`.
    fn with_xzy(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.x, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzz`.
    fn xzz(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.x, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzw`.
    fn xzw(self: UVec4) -> UVec3;
    /// Returns `self` with `x` to `rhs.x`, `z` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xzw`.
    fn with_xzw(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.x, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwx`.
    fn xwx(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.x, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwy`.
    fn xwy(self: UVec4) -> UVec3;
    /// Returns `self` with `x` to `rhs.x`, `w` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xwy`.
    fn with_xwy(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.x, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwz`.
    fn xwz(self: UVec4) -> UVec3;
    /// Returns `self` with `x` to `rhs.x`, `w` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_xwz`.
    fn with_xwz(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.x, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xww`.
    fn xww(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxx`.
    fn yxx(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxy`.
    fn yxy(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.y, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxz`.
    fn yxz(self: UVec4) -> UVec3;
    /// Returns `self` with `y` to `rhs.x`, `x` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yxz`.
    fn with_yxz(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.y, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxw`.
    fn yxw(self: UVec4) -> UVec3;
    /// Returns `self` with `y` to `rhs.x`, `x` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yxw`.
    fn with_yxw(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyx`.
    fn yyx(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyy`.
    fn yyy(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.y, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyz`.
    fn yyz(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.y, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyw`.
    fn yyw(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.y, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzx`.
    fn yzx(self: UVec4) -> UVec3;
    /// Returns `self` with `y` to `rhs.x`, `z` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yzx`.
    fn with_yzx(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.y, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzy`.
    fn yzy(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.y, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzz`.
    fn yzz(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.y, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzw`.
    fn yzw(self: UVec4) -> UVec3;
    /// Returns `self` with `y` to `rhs.x`, `z` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_yzw`.
    fn with_yzw(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.y, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywx`.
    fn ywx(self: UVec4) -> UVec3;
    /// Returns `self` with `y` to `rhs.x`, `w` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_ywx`.
    fn with_ywx(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.y, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywy`.
    fn ywy(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.y, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywz`.
    fn ywz(self: UVec4) -> UVec3;
    /// Returns `self` with `y` to `rhs.x`, `w` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_ywz`.
    fn with_ywz(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.y, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yww`.
    fn yww(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.z, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxx`.
    fn zxx(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.z, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxy`.
    fn zxy(self: UVec4) -> UVec3;
    /// Returns `self` with `z` to `rhs.x`, `x` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zxy`.
    fn with_zxy(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.z, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxz`.
    fn zxz(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.z, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxw`.
    fn zxw(self: UVec4) -> UVec3;
    /// Returns `self` with `z` to `rhs.x`, `x` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zxw`.
    fn with_zxw(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.z, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyx`.
    fn zyx(self: UVec4) -> UVec3;
    /// Returns `self` with `z` to `rhs.x`, `y` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zyx`.
    fn with_zyx(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.z, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyy`.
    fn zyy(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.z, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyz`.
    fn zyz(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.z, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyw`.
    fn zyw(self: UVec4) -> UVec3;
    /// Returns `self` with `z` to `rhs.x`, `y` to `rhs.y`, `w` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zyw`.
    fn with_zyw(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.z, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzx`.
    fn zzx(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.z, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzy`.
    fn zzy(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.z, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzz`.
    fn zzz(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.z, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzw`.
    fn zzw(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.z, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwx`.
    fn zwx(self: UVec4) -> UVec3;
    /// Returns `self` with `z` to `rhs.x`, `w` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zwx`.
    fn with_zwx(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.z, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwy`.
    fn zwy(self: UVec4) -> UVec3;
    /// Returns `self` with `z` to `rhs.x`, `w` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_zwy`.
    fn with_zwy(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.z, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwz`.
    fn zwz(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.z, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zww`.
    fn zww(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.w, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxx`.
    fn wxx(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.w, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxy`.
    fn wxy(self: UVec4) -> UVec3;
    /// Returns `self` with `w` to `rhs.x`, `x` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wxy`.
    fn with_wxy(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.w, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxz`.
    fn wxz(self: UVec4) -> UVec3;
    /// Returns `self` with `w` to `rhs.x`, `x` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wxz`.
    fn with_wxz(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.w, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxw`.
    fn wxw(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.w, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyx`.
    fn wyx(self: UVec4) -> UVec3;
    /// Returns `self` with `w` to `rhs.x`, `y` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wyx`.
    fn with_wyx(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.w, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyy`.
    fn wyy(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.w, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyz`.
    fn wyz(self: UVec4) -> UVec3;
    /// Returns `self` with `w` to `rhs.x`, `y` to `rhs.y`, `z` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wyz`.
    fn with_wyz(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.w, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyw`.
    fn wyw(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.w, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzx`.
    fn wzx(self: UVec4) -> UVec3;
    /// Returns `self` with `w` to `rhs.x`, `z` to `rhs.y`, `x` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wzx`.
    fn with_wzx(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.w, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzy`.
    fn wzy(self: UVec4) -> UVec3;
    /// Returns `self` with `w` to `rhs.x`, `z` to `rhs.y`, `y` to `rhs.z`.
    ///
    /// Mirrors `glam::Vec4Swizzles::with_wzy`.
    fn with_wzy(self: UVec4, rhs: UVec3) -> UVec4;
    /// Returns the `UVec3` `(self.w, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzz`.
    fn wzz(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.w, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzw`.
    fn wzw(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.w, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwx`.
    fn wwx(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.w, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwy`.
    fn wwy(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.w, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwz`.
    fn wwz(self: UVec4) -> UVec3;
    /// Returns the `UVec3` `(self.w, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::www`.
    fn www(self: UVec4) -> UVec3;
    /// Returns the `UVec4` `(self.x, self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxxx`.
    fn xxxx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxxy`.
    fn xxxy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxxz`.
    fn xxxz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxxw`.
    fn xxxw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxyx`.
    fn xxyx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxyy`.
    fn xxyy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxyz`.
    fn xxyz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxyw`.
    fn xxyw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxzx`.
    fn xxzx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxzy`.
    fn xxzy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxzz`.
    fn xxzz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxzw`.
    fn xxzw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxwx`.
    fn xxwx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxwy`.
    fn xxwy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxwz`.
    fn xxwz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.x, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xxww`.
    fn xxww(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyxx`.
    fn xyxx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyxy`.
    fn xyxy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyxz`.
    fn xyxz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyxw`.
    fn xyxw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyyx`.
    fn xyyx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyyy`.
    fn xyyy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyyz`.
    fn xyyz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyyw`.
    fn xyyw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyzx`.
    fn xyzx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyzy`.
    fn xyzy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyzz`.
    fn xyzz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xywx`.
    fn xywx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xywy`.
    fn xywy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xywz`.
    fn xywz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.y, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xyww`.
    fn xyww(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.z, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzxx`.
    fn xzxx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.z, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzxy`.
    fn xzxy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.z, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzxz`.
    fn xzxz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.z, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzxw`.
    fn xzxw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.z, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzyx`.
    fn xzyx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.z, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzyy`.
    fn xzyy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.z, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzyz`.
    fn xzyz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.z, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzyw`.
    fn xzyw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.z, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzzx`.
    fn xzzx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.z, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzzy`.
    fn xzzy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.z, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzzz`.
    fn xzzz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.z, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzzw`.
    fn xzzw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.z, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzwx`.
    fn xzwx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.z, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzwy`.
    fn xzwy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.z, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzwz`.
    fn xzwz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.z, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xzww`.
    fn xzww(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.w, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwxx`.
    fn xwxx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.w, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwxy`.
    fn xwxy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.w, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwxz`.
    fn xwxz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.w, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwxw`.
    fn xwxw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.w, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwyx`.
    fn xwyx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.w, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwyy`.
    fn xwyy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.w, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwyz`.
    fn xwyz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.w, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwyw`.
    fn xwyw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.w, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwzx`.
    fn xwzx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.w, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwzy`.
    fn xwzy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.w, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwzz`.
    fn xwzz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.w, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwzw`.
    fn xwzw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.w, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwwx`.
    fn xwwx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.w, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwwy`.
    fn xwwy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.w, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwwz`.
    fn xwwz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.x, self.w, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::xwww`.
    fn xwww(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxxx`.
    fn yxxx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxxy`.
    fn yxxy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxxz`.
    fn yxxz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxxw`.
    fn yxxw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxyx`.
    fn yxyx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxyy`.
    fn yxyy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxyz`.
    fn yxyz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxyw`.
    fn yxyw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxzx`.
    fn yxzx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxzy`.
    fn yxzy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxzz`.
    fn yxzz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxzw`.
    fn yxzw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxwx`.
    fn yxwx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxwy`.
    fn yxwy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxwz`.
    fn yxwz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.x, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yxww`.
    fn yxww(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyxx`.
    fn yyxx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyxy`.
    fn yyxy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyxz`.
    fn yyxz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyxw`.
    fn yyxw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyyx`.
    fn yyyx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyyy`.
    fn yyyy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyyz`.
    fn yyyz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyyw`.
    fn yyyw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyzx`.
    fn yyzx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyzy`.
    fn yyzy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyzz`.
    fn yyzz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyzw`.
    fn yyzw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yywx`.
    fn yywx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yywy`.
    fn yywy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yywz`.
    fn yywz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.y, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yyww`.
    fn yyww(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.z, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzxx`.
    fn yzxx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.z, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzxy`.
    fn yzxy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.z, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzxz`.
    fn yzxz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.z, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzxw`.
    fn yzxw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.z, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzyx`.
    fn yzyx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.z, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzyy`.
    fn yzyy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.z, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzyz`.
    fn yzyz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.z, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzyw`.
    fn yzyw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.z, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzzx`.
    fn yzzx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.z, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzzy`.
    fn yzzy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.z, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzzz`.
    fn yzzz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.z, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzzw`.
    fn yzzw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.z, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzwx`.
    fn yzwx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.z, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzwy`.
    fn yzwy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.z, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzwz`.
    fn yzwz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.z, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::yzww`.
    fn yzww(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.w, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywxx`.
    fn ywxx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.w, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywxy`.
    fn ywxy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.w, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywxz`.
    fn ywxz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.w, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywxw`.
    fn ywxw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.w, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywyx`.
    fn ywyx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.w, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywyy`.
    fn ywyy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.w, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywyz`.
    fn ywyz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.w, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywyw`.
    fn ywyw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.w, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywzx`.
    fn ywzx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.w, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywzy`.
    fn ywzy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.w, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywzz`.
    fn ywzz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.w, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywzw`.
    fn ywzw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.w, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywwx`.
    fn ywwx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.w, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywwy`.
    fn ywwy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.w, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywwz`.
    fn ywwz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.y, self.w, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::ywww`.
    fn ywww(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxxx`.
    fn zxxx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxxy`.
    fn zxxy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.x, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxxz`.
    fn zxxz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.x, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxxw`.
    fn zxxw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxyx`.
    fn zxyx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxyy`.
    fn zxyy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.x, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxyz`.
    fn zxyz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.x, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxyw`.
    fn zxyw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.x, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxzx`.
    fn zxzx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.x, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxzy`.
    fn zxzy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.x, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxzz`.
    fn zxzz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.x, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxzw`.
    fn zxzw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.x, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxwx`.
    fn zxwx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.x, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxwy`.
    fn zxwy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.x, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxwz`.
    fn zxwz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.x, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zxww`.
    fn zxww(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyxx`.
    fn zyxx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyxy`.
    fn zyxy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.y, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyxz`.
    fn zyxz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.y, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyxw`.
    fn zyxw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyyx`.
    fn zyyx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyyy`.
    fn zyyy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.y, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyyz`.
    fn zyyz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.y, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyyw`.
    fn zyyw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.y, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyzx`.
    fn zyzx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.y, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyzy`.
    fn zyzy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.y, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyzz`.
    fn zyzz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.y, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyzw`.
    fn zyzw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.y, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zywx`.
    fn zywx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.y, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zywy`.
    fn zywy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.y, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zywz`.
    fn zywz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.y, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zyww`.
    fn zyww(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.z, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzxx`.
    fn zzxx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.z, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzxy`.
    fn zzxy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.z, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzxz`.
    fn zzxz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.z, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzxw`.
    fn zzxw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.z, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzyx`.
    fn zzyx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.z, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzyy`.
    fn zzyy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.z, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzyz`.
    fn zzyz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.z, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzyw`.
    fn zzyw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.z, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzzx`.
    fn zzzx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.z, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzzy`.
    fn zzzy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.z, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzzz`.
    fn zzzz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.z, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzzw`.
    fn zzzw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.z, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzwx`.
    fn zzwx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.z, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzwy`.
    fn zzwy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.z, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzwz`.
    fn zzwz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.z, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zzww`.
    fn zzww(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.w, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwxx`.
    fn zwxx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.w, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwxy`.
    fn zwxy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.w, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwxz`.
    fn zwxz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.w, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwxw`.
    fn zwxw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.w, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwyx`.
    fn zwyx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.w, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwyy`.
    fn zwyy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.w, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwyz`.
    fn zwyz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.w, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwyw`.
    fn zwyw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.w, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwzx`.
    fn zwzx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.w, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwzy`.
    fn zwzy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.w, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwzz`.
    fn zwzz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.w, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwzw`.
    fn zwzw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.w, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwwx`.
    fn zwwx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.w, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwwy`.
    fn zwwy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.w, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwwz`.
    fn zwwz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.z, self.w, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::zwww`.
    fn zwww(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.x, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxxx`.
    fn wxxx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.x, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxxy`.
    fn wxxy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.x, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxxz`.
    fn wxxz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.x, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxxw`.
    fn wxxw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.x, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxyx`.
    fn wxyx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.x, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxyy`.
    fn wxyy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.x, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxyz`.
    fn wxyz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.x, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxyw`.
    fn wxyw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.x, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxzx`.
    fn wxzx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.x, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxzy`.
    fn wxzy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.x, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxzz`.
    fn wxzz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.x, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxzw`.
    fn wxzw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.x, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxwx`.
    fn wxwx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.x, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxwy`.
    fn wxwy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.x, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxwz`.
    fn wxwz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.x, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wxww`.
    fn wxww(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.y, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyxx`.
    fn wyxx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.y, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyxy`.
    fn wyxy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.y, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyxz`.
    fn wyxz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.y, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyxw`.
    fn wyxw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.y, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyyx`.
    fn wyyx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.y, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyyy`.
    fn wyyy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.y, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyyz`.
    fn wyyz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.y, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyyw`.
    fn wyyw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.y, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyzx`.
    fn wyzx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.y, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyzy`.
    fn wyzy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.y, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyzz`.
    fn wyzz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.y, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyzw`.
    fn wyzw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.y, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wywx`.
    fn wywx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.y, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wywy`.
    fn wywy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.y, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wywz`.
    fn wywz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.y, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wyww`.
    fn wyww(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.z, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzxx`.
    fn wzxx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.z, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzxy`.
    fn wzxy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.z, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzxz`.
    fn wzxz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.z, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzxw`.
    fn wzxw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.z, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzyx`.
    fn wzyx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.z, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzyy`.
    fn wzyy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.z, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzyz`.
    fn wzyz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.z, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzyw`.
    fn wzyw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.z, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzzx`.
    fn wzzx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.z, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzzy`.
    fn wzzy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.z, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzzz`.
    fn wzzz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.z, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzzw`.
    fn wzzw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.z, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzwx`.
    fn wzwx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.z, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzwy`.
    fn wzwy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.z, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzwz`.
    fn wzwz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.z, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wzww`.
    fn wzww(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.w, self.x, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwxx`.
    fn wwxx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.w, self.x, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwxy`.
    fn wwxy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.w, self.x, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwxz`.
    fn wwxz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.w, self.x, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwxw`.
    fn wwxw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.w, self.y, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwyx`.
    fn wwyx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.w, self.y, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwyy`.
    fn wwyy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.w, self.y, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwyz`.
    fn wwyz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.w, self.y, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwyw`.
    fn wwyw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.w, self.z, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwzx`.
    fn wwzx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.w, self.z, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwzy`.
    fn wwzy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.w, self.z, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwzz`.
    fn wwzz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.w, self.z, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwzw`.
    fn wwzw(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.w, self.w, self.x)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwwx`.
    fn wwwx(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.w, self.w, self.y)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwwy`.
    fn wwwy(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.w, self.w, self.z)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwwz`.
    fn wwwz(self: UVec4) -> UVec4;
    /// Returns the `UVec4` `(self.w, self.w, self.w, self.w)`.
    ///
    /// Mirrors `glam::Vec4Swizzles::wwww`.
    fn wwww(self: UVec4) -> UVec4;
}

pub impl UVec4SwizzlesImpl of UVec4Swizzles {
    #[inline(always)]
    fn xyzw(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn xx(self: UVec4) -> UVec2 {
        UVec2 { x: self.x, y: self.x }
    }

    #[inline(always)]
    fn xy(self: UVec4) -> UVec2 {
        UVec2 { x: self.x, y: self.y }
    }

    #[inline(always)]
    fn with_xy(self: UVec4, rhs: UVec2) -> UVec4 {
        UVec4 { x: rhs.x, y: rhs.y, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn xz(self: UVec4) -> UVec2 {
        UVec2 { x: self.x, y: self.z }
    }

    #[inline(always)]
    fn with_xz(self: UVec4, rhs: UVec2) -> UVec4 {
        UVec4 { x: rhs.x, y: self.y, z: rhs.y, w: self.w }
    }

    #[inline(always)]
    fn xw(self: UVec4) -> UVec2 {
        UVec2 { x: self.x, y: self.w }
    }

    #[inline(always)]
    fn with_xw(self: UVec4, rhs: UVec2) -> UVec4 {
        UVec4 { x: rhs.x, y: self.y, z: self.z, w: rhs.y }
    }

    #[inline(always)]
    fn yx(self: UVec4) -> UVec2 {
        UVec2 { x: self.y, y: self.x }
    }

    #[inline(always)]
    fn with_yx(self: UVec4, rhs: UVec2) -> UVec4 {
        UVec4 { x: rhs.y, y: rhs.x, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn yy(self: UVec4) -> UVec2 {
        UVec2 { x: self.y, y: self.y }
    }

    #[inline(always)]
    fn yz(self: UVec4) -> UVec2 {
        UVec2 { x: self.y, y: self.z }
    }

    #[inline(always)]
    fn with_yz(self: UVec4, rhs: UVec2) -> UVec4 {
        UVec4 { x: self.x, y: rhs.x, z: rhs.y, w: self.w }
    }

    #[inline(always)]
    fn yw(self: UVec4) -> UVec2 {
        UVec2 { x: self.y, y: self.w }
    }

    #[inline(always)]
    fn with_yw(self: UVec4, rhs: UVec2) -> UVec4 {
        UVec4 { x: self.x, y: rhs.x, z: self.z, w: rhs.y }
    }

    #[inline(always)]
    fn zx(self: UVec4) -> UVec2 {
        UVec2 { x: self.z, y: self.x }
    }

    #[inline(always)]
    fn with_zx(self: UVec4, rhs: UVec2) -> UVec4 {
        UVec4 { x: rhs.y, y: self.y, z: rhs.x, w: self.w }
    }

    #[inline(always)]
    fn zy(self: UVec4) -> UVec2 {
        UVec2 { x: self.z, y: self.y }
    }

    #[inline(always)]
    fn with_zy(self: UVec4, rhs: UVec2) -> UVec4 {
        UVec4 { x: self.x, y: rhs.y, z: rhs.x, w: self.w }
    }

    #[inline(always)]
    fn zz(self: UVec4) -> UVec2 {
        UVec2 { x: self.z, y: self.z }
    }

    #[inline(always)]
    fn zw(self: UVec4) -> UVec2 {
        UVec2 { x: self.z, y: self.w }
    }

    #[inline(always)]
    fn with_zw(self: UVec4, rhs: UVec2) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: rhs.x, w: rhs.y }
    }

    #[inline(always)]
    fn wx(self: UVec4) -> UVec2 {
        UVec2 { x: self.w, y: self.x }
    }

    #[inline(always)]
    fn with_wx(self: UVec4, rhs: UVec2) -> UVec4 {
        UVec4 { x: rhs.y, y: self.y, z: self.z, w: rhs.x }
    }

    #[inline(always)]
    fn wy(self: UVec4) -> UVec2 {
        UVec2 { x: self.w, y: self.y }
    }

    #[inline(always)]
    fn with_wy(self: UVec4, rhs: UVec2) -> UVec4 {
        UVec4 { x: self.x, y: rhs.y, z: self.z, w: rhs.x }
    }

    #[inline(always)]
    fn wz(self: UVec4) -> UVec2 {
        UVec2 { x: self.w, y: self.z }
    }

    #[inline(always)]
    fn with_wz(self: UVec4, rhs: UVec2) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: rhs.y, w: rhs.x }
    }

    #[inline(always)]
    fn ww(self: UVec4) -> UVec2 {
        UVec2 { x: self.w, y: self.w }
    }

    #[inline(always)]
    fn xxx(self: UVec4) -> UVec3 {
        UVec3 { x: self.x, y: self.x, z: self.x }
    }

    #[inline(always)]
    fn xxy(self: UVec4) -> UVec3 {
        UVec3 { x: self.x, y: self.x, z: self.y }
    }

    #[inline(always)]
    fn xxz(self: UVec4) -> UVec3 {
        UVec3 { x: self.x, y: self.x, z: self.z }
    }

    #[inline(always)]
    fn xxw(self: UVec4) -> UVec3 {
        UVec3 { x: self.x, y: self.x, z: self.w }
    }

    #[inline(always)]
    fn xyx(self: UVec4) -> UVec3 {
        UVec3 { x: self.x, y: self.y, z: self.x }
    }

    #[inline(always)]
    fn xyy(self: UVec4) -> UVec3 {
        UVec3 { x: self.x, y: self.y, z: self.y }
    }

    #[inline(always)]
    fn xyz(self: UVec4) -> UVec3 {
        UVec3 { x: self.x, y: self.y, z: self.z }
    }

    #[inline(always)]
    fn with_xyz(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.x, y: rhs.y, z: rhs.z, w: self.w }
    }

    #[inline(always)]
    fn xyw(self: UVec4) -> UVec3 {
        UVec3 { x: self.x, y: self.y, z: self.w }
    }

    #[inline(always)]
    fn with_xyw(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.x, y: rhs.y, z: self.z, w: rhs.z }
    }

    #[inline(always)]
    fn xzx(self: UVec4) -> UVec3 {
        UVec3 { x: self.x, y: self.z, z: self.x }
    }

    #[inline(always)]
    fn xzy(self: UVec4) -> UVec3 {
        UVec3 { x: self.x, y: self.z, z: self.y }
    }

    #[inline(always)]
    fn with_xzy(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.x, y: rhs.z, z: rhs.y, w: self.w }
    }

    #[inline(always)]
    fn xzz(self: UVec4) -> UVec3 {
        UVec3 { x: self.x, y: self.z, z: self.z }
    }

    #[inline(always)]
    fn xzw(self: UVec4) -> UVec3 {
        UVec3 { x: self.x, y: self.z, z: self.w }
    }

    #[inline(always)]
    fn with_xzw(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.x, y: self.y, z: rhs.y, w: rhs.z }
    }

    #[inline(always)]
    fn xwx(self: UVec4) -> UVec3 {
        UVec3 { x: self.x, y: self.w, z: self.x }
    }

    #[inline(always)]
    fn xwy(self: UVec4) -> UVec3 {
        UVec3 { x: self.x, y: self.w, z: self.y }
    }

    #[inline(always)]
    fn with_xwy(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.x, y: rhs.z, z: self.z, w: rhs.y }
    }

    #[inline(always)]
    fn xwz(self: UVec4) -> UVec3 {
        UVec3 { x: self.x, y: self.w, z: self.z }
    }

    #[inline(always)]
    fn with_xwz(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.x, y: self.y, z: rhs.z, w: rhs.y }
    }

    #[inline(always)]
    fn xww(self: UVec4) -> UVec3 {
        UVec3 { x: self.x, y: self.w, z: self.w }
    }

    #[inline(always)]
    fn yxx(self: UVec4) -> UVec3 {
        UVec3 { x: self.y, y: self.x, z: self.x }
    }

    #[inline(always)]
    fn yxy(self: UVec4) -> UVec3 {
        UVec3 { x: self.y, y: self.x, z: self.y }
    }

    #[inline(always)]
    fn yxz(self: UVec4) -> UVec3 {
        UVec3 { x: self.y, y: self.x, z: self.z }
    }

    #[inline(always)]
    fn with_yxz(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.y, y: rhs.x, z: rhs.z, w: self.w }
    }

    #[inline(always)]
    fn yxw(self: UVec4) -> UVec3 {
        UVec3 { x: self.y, y: self.x, z: self.w }
    }

    #[inline(always)]
    fn with_yxw(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.y, y: rhs.x, z: self.z, w: rhs.z }
    }

    #[inline(always)]
    fn yyx(self: UVec4) -> UVec3 {
        UVec3 { x: self.y, y: self.y, z: self.x }
    }

    #[inline(always)]
    fn yyy(self: UVec4) -> UVec3 {
        UVec3 { x: self.y, y: self.y, z: self.y }
    }

    #[inline(always)]
    fn yyz(self: UVec4) -> UVec3 {
        UVec3 { x: self.y, y: self.y, z: self.z }
    }

    #[inline(always)]
    fn yyw(self: UVec4) -> UVec3 {
        UVec3 { x: self.y, y: self.y, z: self.w }
    }

    #[inline(always)]
    fn yzx(self: UVec4) -> UVec3 {
        UVec3 { x: self.y, y: self.z, z: self.x }
    }

    #[inline(always)]
    fn with_yzx(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.z, y: rhs.x, z: rhs.y, w: self.w }
    }

    #[inline(always)]
    fn yzy(self: UVec4) -> UVec3 {
        UVec3 { x: self.y, y: self.z, z: self.y }
    }

    #[inline(always)]
    fn yzz(self: UVec4) -> UVec3 {
        UVec3 { x: self.y, y: self.z, z: self.z }
    }

    #[inline(always)]
    fn yzw(self: UVec4) -> UVec3 {
        UVec3 { x: self.y, y: self.z, z: self.w }
    }

    #[inline(always)]
    fn with_yzw(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: self.x, y: rhs.x, z: rhs.y, w: rhs.z }
    }

    #[inline(always)]
    fn ywx(self: UVec4) -> UVec3 {
        UVec3 { x: self.y, y: self.w, z: self.x }
    }

    #[inline(always)]
    fn with_ywx(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.z, y: rhs.x, z: self.z, w: rhs.y }
    }

    #[inline(always)]
    fn ywy(self: UVec4) -> UVec3 {
        UVec3 { x: self.y, y: self.w, z: self.y }
    }

    #[inline(always)]
    fn ywz(self: UVec4) -> UVec3 {
        UVec3 { x: self.y, y: self.w, z: self.z }
    }

    #[inline(always)]
    fn with_ywz(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: self.x, y: rhs.x, z: rhs.z, w: rhs.y }
    }

    #[inline(always)]
    fn yww(self: UVec4) -> UVec3 {
        UVec3 { x: self.y, y: self.w, z: self.w }
    }

    #[inline(always)]
    fn zxx(self: UVec4) -> UVec3 {
        UVec3 { x: self.z, y: self.x, z: self.x }
    }

    #[inline(always)]
    fn zxy(self: UVec4) -> UVec3 {
        UVec3 { x: self.z, y: self.x, z: self.y }
    }

    #[inline(always)]
    fn with_zxy(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.y, y: rhs.z, z: rhs.x, w: self.w }
    }

    #[inline(always)]
    fn zxz(self: UVec4) -> UVec3 {
        UVec3 { x: self.z, y: self.x, z: self.z }
    }

    #[inline(always)]
    fn zxw(self: UVec4) -> UVec3 {
        UVec3 { x: self.z, y: self.x, z: self.w }
    }

    #[inline(always)]
    fn with_zxw(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.y, y: self.y, z: rhs.x, w: rhs.z }
    }

    #[inline(always)]
    fn zyx(self: UVec4) -> UVec3 {
        UVec3 { x: self.z, y: self.y, z: self.x }
    }

    #[inline(always)]
    fn with_zyx(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.z, y: rhs.y, z: rhs.x, w: self.w }
    }

    #[inline(always)]
    fn zyy(self: UVec4) -> UVec3 {
        UVec3 { x: self.z, y: self.y, z: self.y }
    }

    #[inline(always)]
    fn zyz(self: UVec4) -> UVec3 {
        UVec3 { x: self.z, y: self.y, z: self.z }
    }

    #[inline(always)]
    fn zyw(self: UVec4) -> UVec3 {
        UVec3 { x: self.z, y: self.y, z: self.w }
    }

    #[inline(always)]
    fn with_zyw(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: self.x, y: rhs.y, z: rhs.x, w: rhs.z }
    }

    #[inline(always)]
    fn zzx(self: UVec4) -> UVec3 {
        UVec3 { x: self.z, y: self.z, z: self.x }
    }

    #[inline(always)]
    fn zzy(self: UVec4) -> UVec3 {
        UVec3 { x: self.z, y: self.z, z: self.y }
    }

    #[inline(always)]
    fn zzz(self: UVec4) -> UVec3 {
        UVec3 { x: self.z, y: self.z, z: self.z }
    }

    #[inline(always)]
    fn zzw(self: UVec4) -> UVec3 {
        UVec3 { x: self.z, y: self.z, z: self.w }
    }

    #[inline(always)]
    fn zwx(self: UVec4) -> UVec3 {
        UVec3 { x: self.z, y: self.w, z: self.x }
    }

    #[inline(always)]
    fn with_zwx(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.z, y: self.y, z: rhs.x, w: rhs.y }
    }

    #[inline(always)]
    fn zwy(self: UVec4) -> UVec3 {
        UVec3 { x: self.z, y: self.w, z: self.y }
    }

    #[inline(always)]
    fn with_zwy(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: self.x, y: rhs.z, z: rhs.x, w: rhs.y }
    }

    #[inline(always)]
    fn zwz(self: UVec4) -> UVec3 {
        UVec3 { x: self.z, y: self.w, z: self.z }
    }

    #[inline(always)]
    fn zww(self: UVec4) -> UVec3 {
        UVec3 { x: self.z, y: self.w, z: self.w }
    }

    #[inline(always)]
    fn wxx(self: UVec4) -> UVec3 {
        UVec3 { x: self.w, y: self.x, z: self.x }
    }

    #[inline(always)]
    fn wxy(self: UVec4) -> UVec3 {
        UVec3 { x: self.w, y: self.x, z: self.y }
    }

    #[inline(always)]
    fn with_wxy(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.y, y: rhs.z, z: self.z, w: rhs.x }
    }

    #[inline(always)]
    fn wxz(self: UVec4) -> UVec3 {
        UVec3 { x: self.w, y: self.x, z: self.z }
    }

    #[inline(always)]
    fn with_wxz(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.y, y: self.y, z: rhs.z, w: rhs.x }
    }

    #[inline(always)]
    fn wxw(self: UVec4) -> UVec3 {
        UVec3 { x: self.w, y: self.x, z: self.w }
    }

    #[inline(always)]
    fn wyx(self: UVec4) -> UVec3 {
        UVec3 { x: self.w, y: self.y, z: self.x }
    }

    #[inline(always)]
    fn with_wyx(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.z, y: rhs.y, z: self.z, w: rhs.x }
    }

    #[inline(always)]
    fn wyy(self: UVec4) -> UVec3 {
        UVec3 { x: self.w, y: self.y, z: self.y }
    }

    #[inline(always)]
    fn wyz(self: UVec4) -> UVec3 {
        UVec3 { x: self.w, y: self.y, z: self.z }
    }

    #[inline(always)]
    fn with_wyz(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: self.x, y: rhs.y, z: rhs.z, w: rhs.x }
    }

    #[inline(always)]
    fn wyw(self: UVec4) -> UVec3 {
        UVec3 { x: self.w, y: self.y, z: self.w }
    }

    #[inline(always)]
    fn wzx(self: UVec4) -> UVec3 {
        UVec3 { x: self.w, y: self.z, z: self.x }
    }

    #[inline(always)]
    fn with_wzx(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: rhs.z, y: self.y, z: rhs.y, w: rhs.x }
    }

    #[inline(always)]
    fn wzy(self: UVec4) -> UVec3 {
        UVec3 { x: self.w, y: self.z, z: self.y }
    }

    #[inline(always)]
    fn with_wzy(self: UVec4, rhs: UVec3) -> UVec4 {
        UVec4 { x: self.x, y: rhs.z, z: rhs.y, w: rhs.x }
    }

    #[inline(always)]
    fn wzz(self: UVec4) -> UVec3 {
        UVec3 { x: self.w, y: self.z, z: self.z }
    }

    #[inline(always)]
    fn wzw(self: UVec4) -> UVec3 {
        UVec3 { x: self.w, y: self.z, z: self.w }
    }

    #[inline(always)]
    fn wwx(self: UVec4) -> UVec3 {
        UVec3 { x: self.w, y: self.w, z: self.x }
    }

    #[inline(always)]
    fn wwy(self: UVec4) -> UVec3 {
        UVec3 { x: self.w, y: self.w, z: self.y }
    }

    #[inline(always)]
    fn wwz(self: UVec4) -> UVec3 {
        UVec3 { x: self.w, y: self.w, z: self.z }
    }

    #[inline(always)]
    fn www(self: UVec4) -> UVec3 {
        UVec3 { x: self.w, y: self.w, z: self.w }
    }

    #[inline(always)]
    fn xxxx(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn xxxy(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn xxxz(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn xxxw(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn xxyx(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn xxyy(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn xxyz(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn xxyw(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn xxzx(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn xxzy(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn xxzz(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn xxzw(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn xxwx(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn xxwy(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn xxwz(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn xxww(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.x, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn xyxx(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn xyxy(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn xyxz(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn xyxw(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn xyyx(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn xyyy(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn xyyz(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn xyyw(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn xyzx(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn xyzy(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn xyzz(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn xywx(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn xywy(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn xywz(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn xyww(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.y, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn xzxx(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.z, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn xzxy(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.z, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn xzxz(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.z, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn xzxw(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.z, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn xzyx(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.z, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn xzyy(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.z, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn xzyz(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.z, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn xzyw(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.z, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn xzzx(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.z, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn xzzy(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.z, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn xzzz(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.z, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn xzzw(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.z, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn xzwx(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.z, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn xzwy(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.z, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn xzwz(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.z, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn xzww(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.z, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn xwxx(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.w, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn xwxy(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.w, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn xwxz(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.w, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn xwxw(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.w, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn xwyx(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.w, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn xwyy(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.w, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn xwyz(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.w, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn xwyw(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.w, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn xwzx(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.w, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn xwzy(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.w, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn xwzz(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.w, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn xwzw(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.w, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn xwwx(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.w, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn xwwy(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.w, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn xwwz(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.w, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn xwww(self: UVec4) -> UVec4 {
        UVec4 { x: self.x, y: self.w, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn yxxx(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn yxxy(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn yxxz(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn yxxw(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn yxyx(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn yxyy(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn yxyz(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn yxyw(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn yxzx(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn yxzy(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn yxzz(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn yxzw(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn yxwx(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn yxwy(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn yxwz(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn yxww(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.x, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn yyxx(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn yyxy(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn yyxz(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn yyxw(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn yyyx(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn yyyy(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn yyyz(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn yyyw(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn yyzx(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn yyzy(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn yyzz(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn yyzw(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn yywx(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn yywy(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn yywz(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn yyww(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.y, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn yzxx(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.z, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn yzxy(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.z, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn yzxz(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.z, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn yzxw(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.z, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn yzyx(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.z, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn yzyy(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.z, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn yzyz(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.z, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn yzyw(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.z, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn yzzx(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.z, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn yzzy(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.z, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn yzzz(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.z, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn yzzw(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.z, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn yzwx(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.z, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn yzwy(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.z, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn yzwz(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.z, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn yzww(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.z, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn ywxx(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.w, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn ywxy(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.w, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn ywxz(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.w, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn ywxw(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.w, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn ywyx(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.w, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn ywyy(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.w, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn ywyz(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.w, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn ywyw(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.w, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn ywzx(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.w, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn ywzy(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.w, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn ywzz(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.w, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn ywzw(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.w, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn ywwx(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.w, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn ywwy(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.w, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn ywwz(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.w, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn ywww(self: UVec4) -> UVec4 {
        UVec4 { x: self.y, y: self.w, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn zxxx(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.x, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn zxxy(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.x, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn zxxz(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.x, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn zxxw(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.x, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn zxyx(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.x, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn zxyy(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.x, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn zxyz(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.x, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn zxyw(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.x, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn zxzx(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.x, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn zxzy(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.x, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn zxzz(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.x, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn zxzw(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.x, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn zxwx(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.x, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn zxwy(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.x, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn zxwz(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.x, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn zxww(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.x, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn zyxx(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.y, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn zyxy(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.y, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn zyxz(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.y, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn zyxw(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.y, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn zyyx(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.y, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn zyyy(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.y, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn zyyz(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.y, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn zyyw(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.y, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn zyzx(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.y, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn zyzy(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.y, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn zyzz(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.y, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn zyzw(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.y, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn zywx(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.y, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn zywy(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.y, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn zywz(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.y, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn zyww(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.y, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn zzxx(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.z, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn zzxy(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.z, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn zzxz(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.z, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn zzxw(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.z, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn zzyx(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.z, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn zzyy(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.z, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn zzyz(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.z, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn zzyw(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.z, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn zzzx(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.z, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn zzzy(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.z, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn zzzz(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.z, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn zzzw(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.z, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn zzwx(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.z, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn zzwy(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.z, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn zzwz(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.z, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn zzww(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.z, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn zwxx(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.w, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn zwxy(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.w, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn zwxz(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.w, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn zwxw(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.w, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn zwyx(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.w, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn zwyy(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.w, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn zwyz(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.w, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn zwyw(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.w, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn zwzx(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.w, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn zwzy(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.w, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn zwzz(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.w, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn zwzw(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.w, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn zwwx(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.w, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn zwwy(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.w, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn zwwz(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.w, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn zwww(self: UVec4) -> UVec4 {
        UVec4 { x: self.z, y: self.w, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn wxxx(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.x, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn wxxy(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.x, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn wxxz(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.x, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn wxxw(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.x, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn wxyx(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.x, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn wxyy(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.x, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn wxyz(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.x, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn wxyw(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.x, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn wxzx(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.x, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn wxzy(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.x, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn wxzz(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.x, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn wxzw(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.x, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn wxwx(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.x, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn wxwy(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.x, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn wxwz(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.x, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn wxww(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.x, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn wyxx(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.y, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn wyxy(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.y, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn wyxz(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.y, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn wyxw(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.y, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn wyyx(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.y, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn wyyy(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.y, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn wyyz(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.y, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn wyyw(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.y, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn wyzx(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.y, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn wyzy(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.y, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn wyzz(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.y, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn wyzw(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.y, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn wywx(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.y, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn wywy(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.y, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn wywz(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.y, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn wyww(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.y, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn wzxx(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.z, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn wzxy(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.z, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn wzxz(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.z, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn wzxw(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.z, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn wzyx(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.z, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn wzyy(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.z, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn wzyz(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.z, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn wzyw(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.z, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn wzzx(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.z, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn wzzy(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.z, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn wzzz(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.z, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn wzzw(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.z, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn wzwx(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.z, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn wzwy(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.z, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn wzwz(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.z, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn wzww(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.z, z: self.w, w: self.w }
    }

    #[inline(always)]
    fn wwxx(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.w, z: self.x, w: self.x }
    }

    #[inline(always)]
    fn wwxy(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.w, z: self.x, w: self.y }
    }

    #[inline(always)]
    fn wwxz(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.w, z: self.x, w: self.z }
    }

    #[inline(always)]
    fn wwxw(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.w, z: self.x, w: self.w }
    }

    #[inline(always)]
    fn wwyx(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.w, z: self.y, w: self.x }
    }

    #[inline(always)]
    fn wwyy(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.w, z: self.y, w: self.y }
    }

    #[inline(always)]
    fn wwyz(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.w, z: self.y, w: self.z }
    }

    #[inline(always)]
    fn wwyw(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.w, z: self.y, w: self.w }
    }

    #[inline(always)]
    fn wwzx(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.w, z: self.z, w: self.x }
    }

    #[inline(always)]
    fn wwzy(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.w, z: self.z, w: self.y }
    }

    #[inline(always)]
    fn wwzz(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.w, z: self.z, w: self.z }
    }

    #[inline(always)]
    fn wwzw(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.w, z: self.z, w: self.w }
    }

    #[inline(always)]
    fn wwwx(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.w, z: self.w, w: self.x }
    }

    #[inline(always)]
    fn wwwy(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.w, z: self.w, w: self.y }
    }

    #[inline(always)]
    fn wwwz(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.w, z: self.w, w: self.z }
    }

    #[inline(always)]
    fn wwww(self: UVec4) -> UVec4 {
        UVec4 { x: self.w, y: self.w, z: self.w, w: self.w }
    }
}
