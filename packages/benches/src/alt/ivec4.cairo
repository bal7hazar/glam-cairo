// GENERATED by tools/codegen/intvec.py - do not edit by hand.
//! Alternative implementations benchmarked against `glam::ivec4` (the `alt_*` rows of
//! `gas/ivec4.snap`). The library ships the cheapest formulation; the others stay here so that
//! the comparison is reproducible across compiler upgrades.

use core::integer::i32_diff;
use glam::ivec4::IVec4;

/// Alternative to `IVec4::dot`. The literal glam-rs expression with the panicking corelib
/// operators.
#[inline(always)]
pub fn dot_plain(lhs: IVec4, rhs: IVec4) -> i32 {
    lhs.x * rhs.x + lhs.y * rhs.y + lhs.z * rhs.z + lhs.w * rhs.w
}

/// Alternative to `IVec4::length_squared`. The literal glam-rs expression with the panicking
/// corelib operators.
#[inline(always)]
pub fn length_squared_plain(lhs: IVec4) -> i32 {
    lhs.x * lhs.x + lhs.y * lhs.y + lhs.z * lhs.z + lhs.w * lhs.w
}

/// Alternative to `IVec4::element_sum`. The literal glam-rs expression with the panicking corelib
/// operators.
#[inline(always)]
pub fn element_sum_plain(lhs: IVec4) -> i32 {
    lhs.x + lhs.y + lhs.z + lhs.w
}

/// Alternative to `IVec4::element_product`. The literal glam-rs expression with the panicking
/// corelib operators.
#[inline(always)]
pub fn element_product_plain(lhs: IVec4) -> i32 {
    lhs.x * lhs.y * lhs.z * lhs.w
}

/// Alternative to `IVec4::manhattan_distance`. The literal glam-rs expression with the panicking
/// corelib operators.
#[inline(always)]
pub fn manhattan_distance_plain(lhs: IVec4, rhs: IVec4) -> u32 {
    abs_diff_i32(lhs.x, rhs.x)
        + abs_diff_i32(lhs.y, rhs.y)
        + abs_diff_i32(lhs.z, rhs.z)
        + abs_diff_i32(lhs.w, rhs.w)
}

/// Alternative to `IVec4::distance_squared`. The literal glam-rs expression with the panicking
/// corelib operators.
#[inline(always)]
pub fn distance_squared_plain(lhs: IVec4, rhs: IVec4) -> i32 {
    let d = lhs - rhs;
    d.x * d.x + d.y * d.y + d.z * d.z + d.w * d.w
}

/// `|a - b|` as a `u32` (never overflows). `i32_diff` yields `a - b` directly when `a >= b`.
#[allow(manual_unwrap_or)]
#[inline(always)]
fn abs_diff_i32(a: i32, b: i32) -> u32 {
    match i32_diff(a, b) {
        Ok(d) => d,
        Err(_) => match i32_diff(b, a) {
            Ok(d) | Err(d) => d,
        },
    }
}
