// GENERATED by tools/codegen/intvec.py - do not edit by hand.
//! Alternative implementations benchmarked against `glam::ivec2` (the `alt_*` rows of
//! `gas/ivec2.snap`). The library ships the cheapest formulation; the others stay here so that
//! the comparison is reproducible across compiler upgrades.

use core::integer::i32_diff;
use glam::ivec2::IVec2;

/// Alternative to `IVec2::dot`. The literal glam-rs expression with the panicking corelib
/// operators.
#[inline(always)]
pub fn dot_plain(lhs: IVec2, rhs: IVec2) -> i32 {
    lhs.x * rhs.x + lhs.y * rhs.y
}

/// Alternative to `IVec2::length_squared`. The literal glam-rs expression with the panicking
/// corelib operators.
#[inline(always)]
pub fn length_squared_plain(lhs: IVec2) -> i32 {
    lhs.x * lhs.x + lhs.y * lhs.y
}

/// Alternative to `IVec2::element_sum`. Terms widened to `felt252`, accumulated, converted back
/// once.
#[inline(always)]
pub fn element_sum_fused(lhs: IVec2) -> i32 {
    let s: felt252 = lhs.x.into() + lhs.y.into();
    s.try_into().expect('IVec2: overflow')
}

/// Alternative to `IVec2::element_product`. Terms widened to `felt252`, accumulated, converted back
/// once.
#[inline(always)]
pub fn element_product_fused(lhs: IVec2) -> i32 {
    let p: felt252 = lhs.x.into() * lhs.y.into();
    p.try_into().expect('IVec2: overflow')
}

/// Alternative to `IVec2::manhattan_distance`. Terms widened to `felt252`, accumulated, converted
/// back once.
#[inline(always)]
pub fn manhattan_distance_fused(lhs: IVec2, rhs: IVec2) -> u32 {
    let d: felt252 = abs_diff_i32(lhs.x, rhs.x).into() + abs_diff_i32(lhs.y, rhs.y).into();
    d.try_into().expect('IVec2: overflow')
}

/// Alternative to `IVec2::distance_squared`. The literal glam-rs expression with the panicking
/// corelib operators.
#[inline(always)]
pub fn distance_squared_plain(lhs: IVec2, rhs: IVec2) -> i32 {
    let d = lhs - rhs;
    d.x * d.x + d.y * d.y
}

/// Alternative to `IVec2::perp_dot`. The literal glam-rs expression with the panicking corelib
/// operators.
#[inline(always)]
pub fn perp_dot_plain(lhs: IVec2, rhs: IVec2) -> i32 {
    lhs.x * rhs.y - lhs.y * rhs.x
}

/// Alternative to `IVec2::rotate`. The literal glam-rs expression with the panicking corelib
/// operators.
#[inline(always)]
pub fn rotate_plain(lhs: IVec2, rhs: IVec2) -> IVec2 {
    IVec2 { x: lhs.x * rhs.x - lhs.y * rhs.y, y: lhs.y * rhs.x + lhs.x * rhs.y }
}

/// `|a - b|` as a `u32` (never overflows). `i32_diff` yields `a - b` directly when `a >= b`.
#[allow(manual_unwrap_or)]
#[inline(always)]
fn abs_diff_i32(a: i32, b: i32) -> u32 {
    match i32_diff(a, b) {
        Ok(d) => d,
        Err(_) => match i32_diff(b, a) {
            Ok(d) | Err(d) => d,
        },
    }
}
