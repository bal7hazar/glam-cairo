// GENERATED by tools/codegen/intvec.py - do not edit by hand.
//! Alternative implementations benchmarked against `glam::uvec4` (the `alt_*` rows of
//! `gas/uvec4.snap`). The library ships the cheapest formulation; the others stay here so that
//! the comparison is reproducible across compiler upgrades.

use core::num::traits::CheckedSub;
use glam::uvec4::UVec4;

/// Alternative to `UVec4::dot`. The literal glam-rs expression with the panicking corelib
/// operators.
#[inline(always)]
pub fn dot_plain(lhs: UVec4, rhs: UVec4) -> u32 {
    lhs.x * rhs.x + lhs.y * rhs.y + lhs.z * rhs.z + lhs.w * rhs.w
}

/// Alternative to `UVec4::length_squared`. The literal glam-rs expression with the panicking
/// corelib operators.
#[inline(always)]
pub fn length_squared_plain(lhs: UVec4) -> u32 {
    lhs.x * lhs.x + lhs.y * lhs.y + lhs.z * lhs.z + lhs.w * lhs.w
}

/// Alternative to `UVec4::element_sum`. The literal glam-rs expression with the panicking corelib
/// operators.
#[inline(always)]
pub fn element_sum_plain(lhs: UVec4) -> u32 {
    lhs.x + lhs.y + lhs.z + lhs.w
}

/// Alternative to `UVec4::element_product`. The literal glam-rs expression with the panicking
/// corelib operators.
#[inline(always)]
pub fn element_product_plain(lhs: UVec4) -> u32 {
    lhs.x * lhs.y * lhs.z * lhs.w
}

/// Alternative to `UVec4::manhattan_distance`. The literal glam-rs expression with the panicking
/// corelib operators.
#[inline(always)]
pub fn manhattan_distance_plain(lhs: UVec4, rhs: UVec4) -> u32 {
    abs_diff_u32(lhs.x, rhs.x)
        + abs_diff_u32(lhs.y, rhs.y)
        + abs_diff_u32(lhs.z, rhs.z)
        + abs_diff_u32(lhs.w, rhs.w)
}

/// `|a - b|` (never overflows): `checked_sub` is the comparison and the subtraction at once.
#[allow(manual_unwrap_or)]
#[inline(always)]
fn abs_diff_u32(a: u32, b: u32) -> u32 {
    match a.checked_sub(b) {
        Some(d) => d,
        None => b - a,
    }
}
