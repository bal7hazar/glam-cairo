[package]
name = "fixed"
description = "Signed Q32.32 fixed-point scalar for provable game math (shared by glam, nalgebra and rapier ports)"
version.workspace = true
edition.workspace = true
cairo-version.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"

[dependencies]

[dev-dependencies]
snforge_std.workspace = true
assert_macros.workspace = true

[tool]
fmt.workspace = true
scarb.workspace = true

[scripts]
test = "snforge test"
