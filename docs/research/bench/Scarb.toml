[package]
name = "bench"
version = "0.1.0"
edition = "2024_07"

[dependencies]

[dev-dependencies]
snforge_std = "0.61.0"
assert_macros = "2.19.4"

[scripts]
test = "snforge test"
bench = "python3 scripts/bench.py run"

[tool.scarb]
allow-prebuilt-plugins = ["snforge_std"]

# The tracked resource (sierra-gas / cairo-steps) is passed on the CLI by scripts/bench.py,
# which runs the suite once in each mode.
